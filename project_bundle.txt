
--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: project_bundle.txt ---

--- DIRECTORY: .gitignore ---
# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*


--- DIRECTORY: angular.json ---
{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "projects",
  "projects": {
    "jaren-portfolio": {
      "projectType": "application",
      "schematics": {
        "@schematics/angular:component": {
          "standalone": true,
          "style": "css"
        }
      },
      "root": "",
      "sourceRoot": "src",
      "prefix": "app",
      "architect": {
        "build": {
          "builder": "@angular-devkit/build-angular:application",
          "options": {
            "outputPath": "dist/jaren-portfolio",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "polyfills": [],
            "tsConfig": "tsconfig.app.json",
            "inlineStyleLanguage": "css",
            "assets": [
              "src/favicon.ico",
              "src/assets"
            ],
            "styles": [
              "src/styles.css"
            ],
            "scripts": []
          },
          "configurations": {
            "production": {
              "budgets": [
                { "type": "initial", "maximumWarning": "500kb", "maximumError": "1mb" },
                { "type": "anyComponentStyle", "maximumWarning": "2kb", "maximumError": "4kb" }
              ],
              "outputHashing": "all"
            },
            "development": {
              "optimization": false,
              "extractLicenses": false,
              "sourceMap": true
            }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular-devkit/build-angular:dev-server",
          "configurations": {
            "production": { "buildTarget": "jaren-portfolio:build:production" },
            "development": { "buildTarget": "jaren-portfolio:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  },
  "cli": {
    "analytics": false
  }
}


--- DIRECTORY: package.json ---
{
  "name": "jaren-portfolio",
  "version": "2.5.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^21.0.0",
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/platform-browser-dynamic": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^21.0.0",
    "@angular/cli": "^21.0.0",
    "@angular/compiler-cli": "^21.0.0",
    "typescript": "^5.9.3",
    "zone.js": "^0.15.1"
  }
}


--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }
[data-theme="light"] .tknob { transform: translateX(20px); }
.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  /*
    KEY FIX: .main-content must be position:relative (not fixed) on mobile
    so page sections stack in normal document flow and scrollIntoView works.
  */
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px; /* offset for fixed mobnav */
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg1" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg2" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg3" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0d0a14"/><defs><radialGradient id="rg4" cx="50%" cy="50%"><stop offset="0%" stop-color="#c4b5fd" stop-opacity=".18"/><stop offset="100%" stop-color="#c4b5fd" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4)"/><circle cx="145" cy="92" r="32" fill="none" stroke="#c4b5fd" stroke-width=".8" opacity=".28"/><circle cx="145" cy="92" r="55" fill="none" stroke="#c4b5fd" stroke-width=".6" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#c4b5fd" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">CUT THE CORDS · VANILLA</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 3rem;
}

/*
  NOTE: We do NOT set { opacity: 0 } on .apull, .abio, .blk, .slabel here.
  Base opacity is handled by the global utility classes (.au, .al, .ar, .as)
  in styles.css. Setting it locally (Angular-scoped) was causing content to
  snap invisible instantly during slide-out, defeating the global .prev fix.
*/

/* Active Page Animations (Retriggered by [active] input) */
.active-page .slabel     { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .apull      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .abio       { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d2  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d3  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s ease both; }

.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.ag { display: grid; grid-template-columns: 1.1fr .9fr; gap: 5rem; }

.apull {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-style: italic;
  font-size: clamp(2.5rem, 3.5vw, 3.5rem);
  line-height: 1.2;
  letter-spacing: -.025em;
  color: var(--white);
  margin-bottom: 2rem;
}
.apull em { color: var(--acc); font-style: normal; }

.abio { font-size: 1.1rem; color: var(--text); line-height: 1.8; font-weight: 300; }
.abio p + p { margin-top: 1.25rem; }
.abio strong { color: var(--white); font-weight: 600; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none; border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.blk { margin-bottom: 2.5rem; }
.blkh {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem; letter-spacing: .12em;
  color: var(--muted); margin-bottom: 1.5rem; text-transform: uppercase;
  display: flex; align-items: center; justify-content: space-between; font-weight: 600;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .08em;
  color: var(--acc); border: 1px solid rgba(139,92,246,.3); padding: .3rem .65rem;
  border-radius: 4px; cursor: none; background: none; transition: background .2s, border-color .2s;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.1); }

/* SPHERE */
.skill-sphere-wrap {
  position: relative; min-height: 268px; display: flex;
  align-items: center; justify-content: center;
  border: 1px solid var(--bord); border-radius: 8px; background: var(--bg);
  padding: 1.5rem; overflow: hidden;
}

/* "drag to spin" — desktop only */
.sphere-hint {
  position: absolute; bottom: .6rem; left: 50%; transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--muted);
  letter-spacing: .08em; opacity: .5; pointer-events: none; width: max-content;
}

/* PROFICIENCY LIST */
.skl { list-style: none; display: flex; flex-direction: column; gap: 1.1rem; padding: 0; }
.sk-item { display: flex; flex-direction: column; gap: .4rem; opacity: 0; animation: fadeUp 0.55s cubic-bezier(.22,1,.36,1) forwards; }

.sk-info { display: flex; justify-content: space-between; align-items: center; font-size: .92rem; font-weight: 500; color: var(--text); }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .72rem; color: var(--muted); }
.sk-bar { width: 100%; height: 5px; background: var(--bg); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }

@keyframes fillUp {
  from { width: 0; }
  to   { width: var(--target-width); }
}
.sk-fill {
  height: 100%;
  background: var(--acc);
  border-radius: 4px;
  width: 0;
  animation: fillUp 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

/* CAROUSEL */
.cert-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
.cert-counter { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .06em; }
.cert-carousel { position: relative; display: flex; align-items: center; gap: .75rem; }
.cert-nav {
  width: 36px; height: 36px; border: 1px solid var(--bord); border-radius: 50%;
  display: flex; align-items: center; justify-content: center; color: var(--sub);
  font-size: 1.2rem; cursor: none; flex-shrink: 0; transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 2px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap { flex: 1; overflow: hidden; }

.cert-in  { animation: certIn  .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
.cert-in-r{ animation: certInR .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
@keyframes certIn  { from { opacity: 0; transform: translateX(30px);  } to { opacity: 1; transform: translateX(0); } }
@keyframes certInR { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: translateX(0); } }

.cert-card {
  display: block; border-radius: 8px; overflow: hidden; border: 1px solid var(--bord);
  transition: border-color .25s; text-decoration: none; cursor: none;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top { height: 55px; display: flex; align-items: center; padding: 0 1.25rem; gap: .75rem; }
.cert-issuer-badge {
  width: 30px; height: 30px; border-radius: 4px; display: flex; align-items: center;
  justify-content: center; font-family: 'JetBrains Mono', monospace; font-size: .65rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name { font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .05em; color: var(--text); font-weight: 500; }
.cert-body { background: var(--bg); padding: 1.25rem; }
.cert-name { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.1rem; letter-spacing: -.01em; color: var(--white); line-height: 1.2; margin-bottom: .5rem; }
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .06em; }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--acc); letter-spacing: .06em; font-weight: 500; }
.cert-dots { display: flex; gap: .4rem; justify-content: center; margin-top: 1rem; }
.cert-dot {
  width: 6px; height: 6px; border-radius: 50%; background: var(--bord);
  border: none; cursor: none; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* Updated keyframes */
@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-about { padding: 6.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 3rem; }
  .btn, .cert-nav, .cert-card, .cert-dot, .skill-toggle-btn { cursor: pointer !important; }

  /* Hide "drag to spin" on touch devices — dragging works differently */
  .sphere-hint { display: none; }
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <div class="ag">
      <div>
        <h2 class="apull al d1">A builder who actually <em>ships</em>.</h2>
        <div class="abio au d2">
          <p>I'm a Full Stack Developer specializing in <strong>Angular, Node.js, and MongoDB</strong>. I build complete systems: from designing secure database schemas and REST APIs to crafting intuitive frontend interfaces.</p>
          <p>While I currently work as a Game Developer, programming complex systems in <strong>C# (Unity)</strong> has deeply reinforced my backend logic, object-oriented design, and architectural skills.</p>
          <p>I don't just write code; I care about shipping functional, maintainable products. I am looking for a full-stack internship where I can contribute to real-world codebases.</p>
          <div style="margin-top:2rem;">
            <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" y1="15" x2="12" y2="3"/>
              </svg>
              Download Resume
            </a>
          </div>
        </div>
      </div>

      <div>
        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D SPHERE (Centered Container) -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position: absolute; top: 50%; left: 50%; width: 0; height: 0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- SKILL BARS -->
          @if (skillsExpanded()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item au" [style.animation-delay]="(i * 0.1) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <!-- Pass the target width via custom property to prevent flashing -->
                    <div class="sk-fill" [style.--target-width]="sk.pct + '%'" [style.animation-delay]="(i * 0.1 + 0.2) + 's'"></div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT CAROUSEL -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View Certificate ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNav($index > certIndex() ? 1 : -1)"
                [attr.aria-label]="'Cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\about\about.component.ts ---
import { Component, inject, signal, ElementRef, ViewChild, AfterViewInit, OnDestroy, Input } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy {
  @Input() active = false; 
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap') sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  readonly skills = [
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'C# (Backend Logic)', time: '2 yrs', pct: 80 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'JavaScript / TypeScript', time: '2 yrs', pct: 85 },
  ];

  readonly sphereTags = [
    { text: 'Angular', color: '#dd0031' }, { text: 'Vue.js 3', color: '#42b883' },
    { text: 'Node.js', color: '#339933' }, { text: 'MongoDB', color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#', color: '#9b59b6' },
    { text: 'Unity', color: '#a78bfa' }, { text: 'Express.js', color: '#a4a1c6' },
    { text: 'REST APIs', color: '#60a5fa' }, { text: 'Git', color: '#f05030' },
    { text: 'VR / XR', color: '#f59e0b' }, { text: 'Netlify', color: '#00c7b7' },
  ];

  get currentCert(): Certificate {
    return this.data.certificates[this.certIndex()];
  }

  get certTotal(): number {
    return this.data.certificates.length;
  }

  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  toggleSkills() { 
    this.skillsExpanded.update(v => !v); 
  }
  
  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => {
      this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r');
    }, 10);
  }

  ngAfterViewInit() {
    this.initSphere();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
  }

  private initSphere() {
    const RADIUS = 95;
    const items = this.sphereTags.map((tag, i) => {
      const phi = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el = document.createElement('span');
      
      el.className = 'sphere-tag';
      el.textContent = tag.text;
      el.style.color = tag.color;
      el.style.position = 'absolute';
      el.style.background = 'var(--surf)';
      el.style.padding = '4px 8px';
      el.style.borderRadius = '4px';
      el.style.border = '1px solid var(--bord)';
      el.style.cursor = 'grab';
      el.style.userSelect = 'none';
      el.style.fontFamily = "'JetBrains Mono', monospace";
      el.style.fontSize = "12px";
      el.style.whiteSpace = "nowrap";
      
      if (this.sphereContainer?.nativeElement) {
        this.sphereContainer.nativeElement.appendChild(el);
      }
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);

      items.forEach((item) => {
        let x = Math.sin(item.phi) * Math.cos(item.theta);
        let y = Math.cos(item.phi);
        let z = Math.sin(item.phi) * Math.sin(item.theta);
        
        const x2 = x * cosRY - z * sinRY;
        const z2 = x * sinRY + z * cosRY;
        const y3 = y * cosRX - z2 * sinRX;
        const z3 = y * sinRX + z2 * cosRX;
        
        const scale = (z3 + 2.2) / 3.2;
        const px = x2 * RADIUS;
        const py = y3 * RADIUS;

        item.el.style.transform = `translate(-50%,-50%) translate(${px}px,${py}px)`;
        item.el.style.opacity = Math.max(0.1, scale).toString();
        item.el.style.zIndex = Math.round(scale * 20).toString();
        item.el.style.borderColor = z3 > 0 ? 'rgba(167,139,250, 0.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) {
        this.rotY += this.velY; this.rotX += this.velX;
        this.velX *= 0.97;
      }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">From the desk.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based paper-plane icon override */
.soico-send svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Paper plane / send icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-send">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <line x1="22" y1="2" x2="11" y2="13"/>
                  <polygon points="22 2 15 22 11 13 2 9 22 2"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game Developer',
      desc: 'Building VR applications using Unreal Engine 5.5 Blueprints. Shipped PathFinder (VR journaling app) in Unity. Currently developing an action title, <em>\'Robots vs Aliens\'</em>, in Unity. Writing complex game systems in C# has heavily sharpened my backend logic, object-oriented design, and ability to architect scalable features.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Backend Logic'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.h-photo {
  width: 220px;
  height: 220px;
  flex-shrink: 0;
}

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px dashed var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
}
.h-photo-placeholder:hover { border-color: var(--acc); border-style: solid; }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb { height: 120px; }

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

/* Updated keyframes — more prominent distances */
@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-home { padding: 7rem 1.5rem 4rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: flex-start; gap: 2.5rem; margin-bottom: 4rem; }
  .h-photo { width: 160px; height: 160px; }
  .feat-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I build end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and MongoDB</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-ghost" download>Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]"></div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  // Keep a bound reference so we can remove the listener on destroy
  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;
    this.resize();
    window.addEventListener('resize', () => this.resize());

    /*
      FIX: Use document-level mousemove instead of canvas.mousemove.
      The canvas sits at z-index:0 under content (z-index:1), so when
      the cursor is over text, buttons, cards, etc., the canvas never
      receives mouse events — causing dead zones.
      Tracking at document level and converting to canvas-local coords
      fixes this for ALL whitespace and content areas.
    */
    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    window.removeEventListener('resize', () => this.resize());
  }

  private resize() {
    const container = this.canvas.parentElement;
    const W = container ? container.offsetWidth  : window.innerWidth;
    const H = container ? Math.max(container.scrollHeight, window.innerHeight) : window.innerHeight;

    this.canvas.width  = W;
    this.canvas.height = H;
    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.2rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body { padding: 3rem 4rem; flex: 1; }

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
  margin-bottom: 2.5rem;
}

.ov-imgs {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 3rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
}

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 4rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .1em;
  color: var(--muted);
  margin-bottom: .85rem;
  text-transform: uppercase;
}

.ov-desc {
  font-size: 1.05rem;
  color: var(--text);
  line-height: 1.85;
  font-weight: 300;
}

.ov-features {
  list-style: none;
}
.ov-features li {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  padding: .5rem 0;
  border-bottom: 1px solid var(--bord);
  font-weight: 300;
}
.ov-features li:last-child { border-bottom: none; }
.ov-features li::before { content: '→ '; color: var(--acc); }

.ov-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .5rem;
  margin-top: 2rem;
}

.ov-links {
  display: flex;
  gap: 1rem;
  margin-top: 2rem;
  flex-wrap: wrap;
}

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.links-coming {
  font-size: .85rem;
  color: var(--muted);
  font-family: 'JetBrains Mono', monospace;
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }
.btn-sm { padding: .65rem 1.2rem; font-size: .88rem; }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}


--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

/* ── Logo image (replaces jk. text) ── */
.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  /* keep the old font fallback invisible if img loads */
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;       /* adjust to taste */
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }

/* Paper-plane icon uses stroke, not fill */
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ── Terminal trigger — hardcoded dark purple so it looks
   the same regardless of light/dark page theme ── */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .4rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  letter-spacing: .07em;
  /* Always use dark-theme violet, not var(--acc) */
  color: #8b5cf6;
  border: 1px solid rgba(139, 92, 246, .3);
  border-radius: 4px;
  padding: .3rem .55rem;
  cursor: pointer;
  background: none;
  transition: color .2s, border-color .2s, background .2s;
  margin-top: .5rem;
}
.term-trigger:hover {
  color: #a78bfa;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .1);
}

.term-trigger-dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  /* Always light lavender regardless of theme */
  background: #a78bfa;
  opacity: .7;
  animation: termPulse 1.6s ease infinite;
}
@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); }
  50%       { opacity: 1; transform: scale(1.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Paper plane / send icon instead of envelope -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="22" y1="2" x2="11" y2="13"/>
          <polygon points="22 2 15 22 11 13 2 9 22 2"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine {
  html: string;
}

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System: Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location: Pampanga, Philippines 🇵🇭</span>`,
    `<span class="tl-dim">  Status: <span class="tl-ok">● seeking internship</span></span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      `<span class="tl-head">Available commands:</span>`,
      `  <span class="tl-acc">ls</span>            list projects`,
      `  <span class="tl-acc">cat resume.txt</span> view career summary`,
      `  <span class="tl-acc">skills</span>        show skill stack`,
      `  <span class="tl-acc">whoami</span>        who is jaren`,
      `  <span class="tl-acc">contact</span>       get contact info`,
      `  <span class="tl-acc">github</span>        open github profile`,
      `  <span class="tl-acc">run pathfinder</span> launch vr app`,
      `  <span class="tl-acc">clear</span>         clear terminal`,
      `  <span class="tl-acc">exit</span>          close terminal`,
    ],
    ls: () => [
      `<span class="tl-head">projects/</span>`,
      `  01  <span class="tl-acc">PathFinder</span>        <span class="tl-dim">·  Unreal 5.5 + Unity + C# · VR</span>`,
      `  02  <span class="tl-acc">Travel Atelier</span>    <span class="tl-dim">·  Vue.js 3 + Node.js + JWT</span>`,
      `  03  <span class="tl-acc">Dropify</span>           <span class="tl-dim">·  Vue.js 3 + MongoDB + Express</span>`,
      `  04  <span class="tl-acc">Einstein's Art</span>    <span class="tl-dim">·  Angular + TypeScript + Netlify</span>`,
      `  05  <span class="tl-acc">Cut the Cords</span>     <span class="tl-dim">·  HTML5 + CSS3 + JS</span>`,
    ],
    'cat resume.txt': () => [
      `<span class="tl-head">== JAREN KENDRICK YAMBAO ==</span>`,
      `<span class="tl-dim">Full Stack Developer · Game Developer · HAU IT Student</span>`,
      ``,
      `<span class="tl-warn">EXPERIENCE</span>`,
      `  Game Developer @ VirtuIntelligence  <span class="tl-dim">Apr 2025 – Present</span>`,
      `  <span class="tl-dim">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Blueprints)</span>`,
      `  <span class="tl-dim">• Building Robots vs Aliens in Unity (C#)</span>`,
      ``,
      `<span class="tl-warn">STACK</span>`,
      `  Angular · Vue.js 3 · Node.js · MongoDB · C# · TypeScript`,
      ``,
      `<span class="tl-warn">LOOKING FOR</span>`,
      `  Full-stack internship <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      `<span class="tl-head">Technical Stack:</span>`,
      `  Angular     <span class="tl-acc">████████░░</span> 75%  <span class="tl-dim">1 yr</span>`,
      `  Vue.js 3    <span class="tl-acc">███████░░░</span> 70%  <span class="tl-dim">1 yr</span>`,
      `  Node.js     <span class="tl-acc">████████░░</span> 75%  <span class="tl-dim">1 yr</span>`,
      `  MongoDB     <span class="tl-acc">███████░░░</span> 65%  <span class="tl-dim">1 yr</span>`,
      `  C#          <span class="tl-acc">████████░░</span> 80%  <span class="tl-dim">2 yrs</span>`,
      `  JS / TS     <span class="tl-acc">█████████░</span> 85%  <span class="tl-dim">2 yrs</span>`,
    ],
    whoami: () => [
      `<span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">──────────────────────────────</span>`,
      `  Role     Full Stack Developer + Game Dev`,
      `  Age      20 years old`,
      `  Location Pampanga, Philippines 🇵🇭`,
      `  Uni      Holy Angel University (2027)`,
      `  Status   <span class="tl-ok">● Open to internship</span>`,
      `  "I don't just write code — I ship things."`,
    ],
    contact: () => [
      `<span class="tl-head">Contact info:</span>`,
      `  Email     <span class="tl-acc">jarenkendrickyambao@gmail.com</span>`,
      `  LinkedIn  <span class="tl-acc">linkedin.com/in/jarenkendrick</span>`,
      `  GitHub    <span class="tl-acc">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => {
            setTimeout(() => this.addLine(msg), i * 55);
          });
          // Auto-run 'help' command after booting
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      e.stopPropagation();
      if (this.histIdx < this.history.length - 1) {
        this.histIdx++;
        this.inputValue.set(this.history[this.histIdx] || '');
      }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault();
      e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) {
      const result = fn();
      if (result) result.forEach(l => this.addLine(l));
    } else {
      this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`);
    }
    this.addLine('');
  }

  // Handle scroll ONLY when adding lines, allowing manual scrolling otherwise
  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) {
    return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
  }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: null, github: 'https://github.com/jarenkendrick14',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '03', name: 'Dropify',
      subtitle: 'E-commerce platform · admin panel, robust auth system',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js'],
      desc: 'Comprehensive e-commerce platform including a secure user cart system, checkout flow, and a dedicated admin dashboard for managing products and orders.',
      purpose: 'Designed a scalable database schema in MongoDB and integrated it with a responsive Vue 3 frontend.',
      features: ['Persistent cart and checkout architecture', 'Full CRUD admin dashboard', 'Secure user authentication', 'RESTful API integration'],
      live: null, github: 'https://github.com/jarenkendrick14',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '04', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    },
    {
      num: '05', name: 'Cut the Cords',
      subtitle: 'Wireless peripherals showcase · responsive vanilla web',
      tags: ['HTML5', 'CSS3', 'JavaScript'],
      desc: 'Responsive showcase website built without frameworks. Focused on clean semantic HTML, flexible CSS grids, and vanilla JS interactions.',
      purpose: 'Demonstrated strong foundational web skills, focusing on accessibility and responsive design.',
      features: ['Semantic HTML architecture', 'CSS Grid and Flexbox layouts', 'Fully responsive across all devices', 'Live deployment via GitHub Pages'],
      live: [{ name: 'Live Demo ↗', url: 'https://jarenkendrick14.github.io/CTC-Website/about.html' }],
      github: 'https://github.com/jarenkendrick14/CTC-Website',
      imgs: ['rgba(196,181,253,.12)', 'rgba(196,181,253,.08)', 'rgba(167,139,250,.05)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. Cut the Cords was pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}


--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }
[data-theme="light"] .tknob { transform: translateX(20px); }
.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  /*
    KEY FIX: .main-content must be position:relative (not fixed) on mobile
    so page sections stack in normal document flow and scrollIntoView works.
  */
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px; /* offset for fixed mobnav */
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 2rem;
}

.active-page .slabel     { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .apull      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .abio       { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d2  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d3  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s ease both; }
.active-page .blk.ar.d4  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .34s ease both; }

.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* ══════════ COUNTER STATS ══════════ */
.stats-strip {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  margin-bottom: 3.5rem;
  padding: 2rem 0;
  border-top: 1px solid var(--bord);
  border-bottom: 1px solid var(--bord);
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .35rem;
  text-align: center;
}

.stat-num {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4rem);
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--acc);
}

.stat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  font-weight: 500;
}

/* ══════════ LAYOUT ══════════ */
.ag { display: grid; grid-template-columns: 1.1fr .9fr; gap: 5rem; }

.apull {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-style: italic;
  font-size: clamp(2.5rem, 3.5vw, 3.5rem);
  line-height: 1.2;
  letter-spacing: -.025em;
  color: var(--white);
  margin-bottom: 2rem;
}
.apull em { color: var(--acc); font-style: normal; }

.abio { font-size: 1.1rem; color: var(--text); line-height: 1.8; font-weight: 300; }
.abio p + p { margin-top: 1.25rem; }
.abio strong { color: var(--white); font-weight: 600; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none; border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.blk { margin-bottom: 2.5rem; }
.blkh {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem; letter-spacing: .12em;
  color: var(--muted); margin-bottom: 1.5rem; text-transform: uppercase;
  display: flex; align-items: center; justify-content: space-between; font-weight: 600;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .08em;
  color: var(--acc); border: 1px solid rgba(139,92,246,.3); padding: .3rem .65rem;
  border-radius: 4px; cursor: none; background: none; transition: background .2s, border-color .2s;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.1); }

/* SPHERE */
.skill-sphere-wrap {
  position: relative; min-height: 268px; display: flex;
  align-items: center; justify-content: center;
  border: 1px solid var(--bord); border-radius: 8px; background: var(--bg);
  padding: 1.5rem; overflow: hidden;
}
.sphere-hint {
  position: absolute; bottom: .6rem; left: 50%; transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--muted);
  letter-spacing: .08em; opacity: .5; pointer-events: none; width: max-content;
}

/* PROFICIENCY LIST */
.skl { list-style: none; display: flex; flex-direction: column; gap: 1.1rem; padding: 0; }
.sk-item { display: flex; flex-direction: column; gap: .4rem; opacity: 0; animation: fadeUp 0.55s cubic-bezier(.22,1,.36,1) forwards; }
.sk-info { display: flex; justify-content: space-between; align-items: center; font-size: .92rem; font-weight: 500; color: var(--text); }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .72rem; color: var(--muted); }
.sk-bar { width: 100%; height: 5px; background: var(--bg); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }

@keyframes fillUp { from { width: 0; } to { width: var(--target-width); } }
.sk-fill {
  height: 100%; background: var(--acc); border-radius: 4px;
  width: 0; animation: fillUp 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

/* ══════════ GITHUB HEATMAP ══════════ */
.heatmap-total {
  font-size: .7rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 500;
}

.heatmap-wrap {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 1.25rem;
  overflow: hidden;
}

.heatmap-loading {
  display: flex;
  align-items: center;
  gap: .75rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  padding: .5rem 0;
}

.heatmap-pulse {
  width: 8px; height: 8px;
  border-radius: 50%;
  background: var(--acc);
  animation: pulse 1.2s ease infinite;
}
@keyframes pulse {
  0%, 100% { opacity: .4; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.3); }
}

.heatmap-fallback {
  text-align: center;
  padding: 1rem 0;
}

.heatmap-link {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  text-decoration: none;
  letter-spacing: .06em;
}
.heatmap-link:hover { text-decoration: underline; }

.heatmap-grid {
  display: flex;
  gap: 3px;
  justify-content: center;
}
.heatmap-grid::-webkit-scrollbar { height: 3px; }
.heatmap-grid::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.heatmap-col {
  display: flex;
  flex-direction: column;
  gap: 3px;
  flex-shrink: 0;
}

.heatmap-cell {
  width: 11px;
  height: 11px;
  border-radius: 2px;
  border: 1px solid rgba(139,92,246,.08);
}

.heatmap-legend {
  display: flex;
  align-items: center;
  gap: 4px;
  justify-content: flex-end;
  margin-top: .75rem;
}

.heatmap-legend-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .04em;
  margin: 0 .25rem;
}

.heatmap-legend .heatmap-cell {
  width: 10px;
  height: 10px;
}

/* ── RECENT COMMITS ── */
.commits-list { border-top: 1px solid var(--bord); padding: .75rem 1.25rem 1rem; }
.commits-label {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .1em;
  text-transform: uppercase; color: var(--muted); margin-bottom: .65rem; font-weight: 600;
}
.commit-row {
  display: grid; grid-template-columns: auto 1fr auto; gap: .65rem; align-items: center;
  padding: .45rem .5rem; text-decoration: none; transition: background .15s;
  border-bottom: 1px solid rgba(139,92,246,.06); border-radius: 4px;
}
.commit-row:last-child { border-bottom: none; }
.commit-row:hover { background: rgba(139,92,246,.06); }
.commit-sha { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--acc); letter-spacing: .03em; font-weight: 600; flex-shrink: 0; }
.commit-msg { font-family: 'Plus Jakarta Sans', sans-serif; font-size: .8rem; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.commit-meta { display: flex; align-items: center; gap: .5rem; flex-shrink: 0; }
.commit-repo { font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--acc); background: rgba(139,92,246,.1); padding: .15rem .4rem; border-radius: 3px; letter-spacing: .03em; white-space: nowrap; }
.commit-time { font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--muted); letter-spacing: .03em; white-space: nowrap; }

/* ══════════ CAROUSEL ══════════ */
.cert-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
.cert-counter { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .06em; }
.cert-carousel { position: relative; display: flex; align-items: center; gap: .75rem; }
.cert-nav {
  width: 36px; height: 36px; border: 1px solid var(--bord); border-radius: 50%;
  display: flex; align-items: center; justify-content: center; color: var(--sub);
  font-size: 1.2rem; cursor: none; flex-shrink: 0; transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 2px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap { flex: 1; overflow: hidden; }
.cert-in  { animation: certIn  .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
.cert-in-r{ animation: certInR .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
@keyframes certIn  { from { opacity: 0; transform: translateX(30px);  } to { opacity: 1; transform: translateX(0); } }
@keyframes certInR { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: translateX(0); } }

.cert-card {
  display: block; border-radius: 8px; overflow: hidden; border: 1px solid var(--bord);
  transition: border-color .25s; text-decoration: none; cursor: none;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top { height: 55px; display: flex; align-items: center; padding: 0 1.25rem; gap: .75rem; }
.cert-issuer-badge {
  width: 30px; height: 30px; border-radius: 4px; display: flex; align-items: center;
  justify-content: center; font-family: 'JetBrains Mono', monospace; font-size: .65rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name { font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .05em; color: var(--text); font-weight: 500; }
.cert-body { background: var(--bg); padding: 1.25rem; }
.cert-name { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.1rem; letter-spacing: -.01em; color: var(--white); line-height: 1.2; margin-bottom: .5rem; }
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .06em; }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--acc); letter-spacing: .06em; font-weight: 500; }
.cert-dots { display: flex; gap: .4rem; justify-content: center; margin-top: 1rem; }
.cert-dot {
  width: 6px; height: 6px; border-radius: 50%; background: var(--bord);
  border: none; cursor: none; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-about { padding: 6.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 3rem; }
  .stats-strip { grid-template-columns: repeat(3, 1fr); gap: 1rem; padding: 1.5rem 0; }
  .stat-num { font-size: clamp(2.5rem, 10vw, 3.5rem); }
  .btn, .cert-nav, .cert-card, .cert-dot, .skill-toggle-btn { cursor: pointer !important; }
  .sphere-hint { display: none; }
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- COUNTER STATS -->
    <div class="stats-strip au d1">
      @for (c of counters; track c.label) {
        <div class="stat-item">
          <span class="stat-num">{{ c.current() }}{{ c.suffix }}</span>
          <span class="stat-label">{{ c.label }}</span>
        </div>
      }
    </div>

    <div class="ag">
      <div>
        <h2 class="apull al d1">A builder who actually <em>ships</em>.</h2>
        <div class="abio au d2">
          <p>I'm a Full Stack Developer specializing in <strong>Angular, Node.js, and PostgreSQL</strong>. I build complete systems: from designing secure database schemas and REST APIs to crafting intuitive frontend interfaces.</p>
          <p>I also work as a Game/App Developer, building VR experiences and real-time web applications. Working with <strong>C#</strong> in Unity has helped reinforce my backend logic and architectural thinking.</p>
          <p>I don't just write code; I care about shipping functional, maintainable products. I am looking for a full-stack internship where I can contribute to real-world codebases.</p>
          <div style="margin-top:2rem;">
            <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" y1="15" x2="12" y2="3"/>
              </svg>
              Download Resume
            </a>
          </div>
        </div>
      </div>

      <div>
        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position: absolute; top: 50%; left: 50%; width: 0; height: 0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>
          @if (skillsExpanded()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item au" [style.animation-delay]="(i * 0.1) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill" [style.--target-width]="sk.pct + '%'" [style.animation-delay]="(i * 0.1 + 0.2) + 's'"></div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT CAROUSEL -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View Certificate ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNav($index > certIndex() ? 1 : -1)"
                [attr.aria-label]="'Cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

        <!-- GITHUB HEATMAP -->
        <div class="blk ar d4">
          <div class="blkh">
            GitHub Activity
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="heatmap-total">{{ heatmapTotal() }} contributions</span>
            }
          </div>
          <div class="heatmap-wrap">
            @if (heatmapLoading()) {
              <div class="heatmap-loading">
                <span class="heatmap-pulse"></span>
                Loading contributions...
              </div>
            } @else if (heatmapError()) {
              <div class="heatmap-fallback">
                <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="heatmap-link">
                  View on GitHub ↗
                </a>
              </div>
            } @else {
              <div class="heatmap-grid">
                @for (week of heatmapWeeks(); track $index) {
                  <div class="heatmap-col">
                    @for (day of week; track day.date) {
                      <div class="heatmap-cell"
                        [style.background]="getHeatColor(day.level)"
                        [title]="day.count + ' contributions on ' + day.date">
                      </div>
                    }
                  </div>
                }
              </div>
              <div class="heatmap-legend">
                <span class="heatmap-legend-label">Less</span>
                <div class="heatmap-cell" [style.background]="getHeatColor(0)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(1)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(2)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(3)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(4)"></div>
                <span class="heatmap-legend-label">More</span>
              </div>
            }
          </div>

          <!-- RECENT COMMITS -->
          @if (recentCommits().length > 0) {
            <div class="commits-list">
              <div class="commits-label">Recent Commits</div>
              @for (commit of recentCommits(); track commit.sha) {
                <a class="commit-row" [href]="commit.url" target="_blank" rel="noopener">
                  <span class="commit-sha">{{ commit.sha }}</span>
                  <span class="commit-msg">{{ commit.message }}</span>
                  <span class="commit-meta">
                    <span class="commit-repo">{{ commit.repo }}</span>
                    <span class="commit-time">{{ commit.date }}</span>
                  </span>
                </a>
              }
            </div>
          } @else if (commitsLoading()) {
            <div class="commits-list">
              <div class="heatmap-loading" style="padding: .75rem 0;"><span class="heatmap-pulse"></span> Loading commits...</div>
            </div>
          }
          </div>
        </div>
      </div>
    </div>
</section>

--- DIRECTORY: src\app\components\about\about.component.ts ---
import { Component, inject, signal, ElementRef, ViewChild, AfterViewInit, OnDestroy, Input, OnChanges, SimpleChanges } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

interface HeatmapDay { date: string; count: number; level: number; }
interface Commit { sha: string; message: string; repo: string; date: string; url: string; }

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy, OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap') sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  /* ── Counters ── */
  readonly counters = [
    { target: 5,  label: 'Projects Shipped', suffix: '', current: signal(0) },
    { target: 13, label: 'Certifications',   suffix: '', current: signal(0) },
    { target: 2,  label: 'Years Coding',     suffix: '+', current: signal(0) },
  ];
  private counterRan = false;

  /* ── GitHub Heatmap ── */
  readonly heatmapWeeks = signal<HeatmapDay[][]>([]);
  readonly heatmapTotal = signal(0);
  readonly heatmapLoading = signal(true);
  readonly heatmapError = signal(false);

  /* ── Recent Commits ── */
  readonly recentCommits = signal<Commit[]>([]);
  readonly commitsLoading = signal(true);

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly sphereTags = [
    { text: 'Angular', color: '#dd0031' }, { text: 'Vue.js 3', color: '#42b883' },
    { text: 'Node.js', color: '#339933' }, { text: 'MongoDB', color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#', color: '#9b59b6' },
    { text: 'Unity', color: '#a78bfa' }, { text: 'Express.js', color: '#7a7a9c' },
    { text: 'REST APIs', color: '#60a5fa' }, { text: 'Git', color: '#f05030' },
    { text: 'PostgreSQL', color: '#336791' }, { text: 'Prisma', color: '#7c8cf8' },
    { text: 'Ionic', color: '#3880ff' }, { text: 'WebSockets', color: '#f59e0b' },
  ];

  get currentCert(): Certificate { return this.data.certificates[this.certIndex()]; }
  get certTotal(): number { return this.data.certificates.length; }

  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  toggleSkills() { this.skillsExpanded.update(v => !v); }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.counterRan) {
      this.counterRan = true;
      // Small delay so the slide animation is underway
      setTimeout(() => this.animateCounters(), 350);
    }
  }

  ngAfterViewInit() {
    this.initSphere();
    this.fetchHeatmap();
    this.fetchCommits();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  /* ── Counter Animation ── */
  private animateCounters() {
    this.counters.forEach(c => {
      const duration = 1400;
      const steps = 30;
      const stepTime = duration / steps;
      let step = 0;
      const iv = setInterval(() => {
        step++;
        // Ease-out cubic
        const t = step / steps;
        const eased = 1 - Math.pow(1 - t, 3);
        c.current.set(Math.round(eased * c.target));
        if (step >= steps) { c.current.set(c.target); clearInterval(iv); }
      }, stepTime);
    });
  }

  /* ── GitHub Heatmap ── */
  private async fetchHeatmap() {
    try {
      const res = await fetch('https://github-contributions-api.jogruber.de/v4/jarenkendrick14?y=last');
      if (!res.ok) throw new Error('API error');
      const data = await res.json();
      const contributions: HeatmapDay[] = data.contributions || [];
      this.heatmapTotal.set(data.total?.lastYear || contributions.reduce((s: number, d: HeatmapDay) => s + d.count, 0));

      // Take last 20 weeks (140 days) for compact display
      const recent = contributions.slice(-140);

      // Group into weeks (columns of 7)
      const weeks: HeatmapDay[][] = [];
      for (let i = 0; i < recent.length; i += 7) {
        weeks.push(recent.slice(i, i + 7));
      }
      this.heatmapWeeks.set(weeks);
      this.heatmapLoading.set(false);
    } catch {
      this.heatmapError.set(true);
      this.heatmapLoading.set(false);
    }
  }

  /* ── Recent Commits ── */
  private async fetchCommits() {
    try {
      const res = await fetch('https://api.github.com/users/jarenkendrick14/events/public?per_page=30');
      if (!res.ok) throw new Error('API error');
      const events: any[] = await res.json();
      const commits: Commit[] = [];
      for (const ev of events) {
        if (ev.type === 'PushEvent' && ev.payload?.commits?.length) {
          const repoName = ev.repo?.name?.split('/')[1] || ev.repo?.name || 'unknown';
          for (const c of ev.payload.commits) {
            if (commits.length >= 5) break;
            commits.push({
              sha: c.sha?.substring(0, 7) || '', message: c.message?.split('\n')[0] || '',
              repo: repoName, date: this.timeAgo(new Date(ev.created_at)),
              url: `https://github.com/${ev.repo?.name}/commit/${c.sha}`,
            });
          }
          if (commits.length >= 5) break;
        }
      }
      this.recentCommits.set(commits);
      this.commitsLoading.set(false);
    } catch { this.commitsLoading.set(false); }
  }

  private timeAgo(date: Date): string {
    const seconds = Math.floor((Date.now() - date.getTime()) / 1000);
    if (seconds < 60) return 'just now';
    const mins = Math.floor(seconds / 60);
    if (mins < 60) return `${mins}m ago`;
    const hours = Math.floor(mins / 60);
    if (hours < 24) return `${hours}h ago`;
    const days = Math.floor(hours / 24);
    if (days < 30) return `${days}d ago`;
    return `${Math.floor(days / 30)}mo ago`;
  }

  getHeatColor(level: number): string {
    // Match GitHub's green gradient but in our accent purple
    const colors = [
      'var(--bg)',                  // level 0 — empty
      'rgba(139,92,246,0.2)',      // level 1
      'rgba(139,92,246,0.45)',     // level 2
      'rgba(139,92,246,0.7)',      // level 3
      'rgba(139,92,246,1)',        // level 4
    ];
    return colors[Math.min(level, 4)];
  }

  /* ── Sphere ── */
  private initSphere() {
    const RADIUS = 95;
    const items = this.sphereTags.map((tag, i) => {
      const phi = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el = document.createElement('span');
      el.className = 'sphere-tag';
      el.textContent = tag.text;
      el.style.color = tag.color;
      el.style.position = 'absolute';
      el.style.background = 'var(--surf)';
      el.style.padding = '4px 8px';
      el.style.borderRadius = '4px';
      el.style.border = '1px solid var(--bord)';
      el.style.cursor = 'grab';
      el.style.userSelect = 'none';
      el.style.fontFamily = "'JetBrains Mono', monospace";
      el.style.fontSize = "12px";
      el.style.whiteSpace = "nowrap";
      if (this.sphereContainer?.nativeElement) {
        this.sphereContainer.nativeElement.appendChild(el);
      }
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);
      items.forEach((item) => {
        let x = Math.sin(item.phi) * Math.cos(item.theta);
        let y = Math.cos(item.phi);
        let z = Math.sin(item.phi) * Math.sin(item.theta);
        const x2 = x * cosRY - z * sinRY;
        const z2 = x * sinRY + z * cosRY;
        const y3 = y * cosRX - z2 * sinRX;
        const z3 = y * sinRX + z2 * cosRX;
        const scale = (z3 + 2.2) / 3.2;
        const px = x2 * RADIUS;
        const py = y3 * RADIUS;
        item.el.style.transform = `translate(-50%,-50%) translate(${px}px,${py}px)`;
        item.el.style.opacity = Math.max(0.1, scale).toString();
        item.el.style.zIndex = Math.round(scale * 20).toString();
        item.el.style.borderColor = z3 > 0 ? 'rgba(167,139,250, 0.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) { this.rotY += this.velY; this.rotX += this.velX; this.velX *= 0.97; }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  /* height is set dynamically by JS to match scrollHeight */
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

/* ══════════ PHOTO — bigger, centered, decorative rings ══════════ */
.h-photo {
  width: 280px;
  height: 280px;
  flex-shrink: 0;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Outer decorative ring */
.h-photo::before {
  content: '';
  position: absolute;
  inset: -12px;
  border-radius: 50%;
  border: 1px dashed var(--bord);
  opacity: .5;
  animation: spinSlow 30s linear infinite;
}

/* Inner decorative ring */
.h-photo::after {
  content: '';
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  border: 1px solid rgba(139,92,246,.2);
}

@keyframes spinSlow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px solid var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
  z-index: 1;
}
.h-photo-placeholder:hover { border-color: var(--acc); }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb { height: 120px; }

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-home { padding: 7rem 1.5rem 4rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: center; gap: 2.5rem; margin-bottom: 4rem; }
  .h-photo { width: 200px; height: 200px; }
  .feat-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-ghost" download>Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]"></div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.2rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body { padding: 3rem 4rem; flex: 1; }

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
  margin-bottom: 2.5rem;
}

.ov-imgs {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 3rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
}

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 4rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .1em;
  color: var(--muted);
  margin-bottom: .85rem;
  text-transform: uppercase;
}

.ov-desc {
  font-size: 1.05rem;
  color: var(--text);
  line-height: 1.85;
  font-weight: 300;
}

.ov-features {
  list-style: none;
}
.ov-features li {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  padding: .5rem 0;
  border-bottom: 1px solid var(--bord);
  font-weight: 300;
}
.ov-features li:last-child { border-bottom: none; }
.ov-features li::before { content: '→ '; color: var(--acc); }

.ov-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .5rem;
  margin-top: 2rem;
}

.ov-links {
  display: flex;
  gap: 1rem;
  margin-top: 2rem;
  flex-wrap: wrap;
}

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.links-coming {
  font-size: .85rem;
  color: var(--muted);
  font-family: 'JetBrains Mono', monospace;
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }
.btn-sm { padding: .65rem 1.2rem; font-size: .88rem; }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}


--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
:host { display: block; min-height: 100vh; width: 100%; }

.page-resume { width: 100%; min-height: 100vh; padding: 5rem 5rem 3rem; background: var(--bg); overflow-y: auto; }
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* ── Animations ── */
.active-page .au.d0  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .au.d1  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .au.d2  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .au.d3  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .26s ease both; }
.active-page .au.d4  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .34s ease both; }
.active-page .au.d5  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .42s ease both; }
.active-page .au.d6  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .50s ease both; }

/* ══════════════════════════════════════════
   RESUME HEADER
   ══════════════════════════════════════════ */
.res-header {
  display: flex; justify-content: space-between; align-items: flex-start;
  padding: 2.5rem 3rem; margin-bottom: 3.5rem;
  border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-header-left { flex: 1; }
.res-name {
  font-family: 'Cormorant Garamond', serif; font-weight: 700;
  font-size: clamp(2.2rem, 3.5vw, 3rem); line-height: 1.1;
  letter-spacing: -.02em; color: var(--white); margin-bottom: .5rem;
}
.res-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.1rem;
  color: var(--acc); font-weight: 500; margin-bottom: .3rem;
}
.res-location {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}
.res-header-right {
  display: flex; flex-direction: column; gap: .65rem; align-items: flex-end;
}
.res-contact-link {
  display: flex; align-items: center; gap: .6rem;
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--text); text-decoration: none; transition: color .2s; letter-spacing: .02em;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg { width: 16px; height: 16px; fill: currentColor; flex-shrink: 0; }
.res-contact-link svg[fill="none"] { fill: none; }

/* ══════════════════════════════════════════
   SECTIONS
   ══════════════════════════════════════════ */
.res-section { margin-bottom: 3.5rem; }
.res-section-title {
  font-family: 'JetBrains Mono', monospace; font-size: .85rem;
  letter-spacing: .14em; text-transform: uppercase; font-weight: 700;
  color: var(--white); display: flex; align-items: center; gap: .75rem;
}
.res-cert-count {
  font-size: .7rem; background: var(--acc); color: #fff;
  padding: .15rem .5rem; border-radius: 10px; font-weight: 600; letter-spacing: .05em;
}
.res-divider { height: 2px; background: linear-gradient(90deg, var(--acc), transparent); margin: 1rem 0 2rem; border-radius: 2px; }

/* ── Entry layout (experience / education) ── */
.res-entry {
  display: grid; grid-template-columns: 200px 1fr; gap: 2.5rem;
  padding: 2rem 0; border-bottom: 1px solid var(--bord);
}
.res-entry:last-child { border-bottom: none; }

.res-entry-side { display: flex; flex-direction: column; gap: .3rem; padding-top: .2rem; }
.res-entry-period {
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--acc); font-weight: 600; letter-spacing: .03em;
}
.res-entry-type {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  color: var(--muted); letter-spacing: .03em;
}

.res-entry-main { }
.res-entry-header { margin-bottom: 1rem; }
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); letter-spacing: -.01em; margin-bottom: .2rem;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}

.res-bullets {
  list-style: none; padding: 0; display: flex; flex-direction: column; gap: .8rem; margin-bottom: 1.25rem;
}
.res-bullets li {
  font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300;
  padding-left: 1.5rem; position: relative;
}
.res-bullets li::before {
  content: '▸'; position: absolute; left: 0; color: var(--acc); font-size: .85rem; top: .15rem;
}
.res-bullets li :is(strong, b) { color: var(--white); font-weight: 600; }

.res-edu-detail { font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300; }

.res-tags { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tag {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .04em;
  padding: .3rem .65rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tag:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   SKILLS — two columns
   ══════════════════════════════════════════ */
.res-skills-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 3rem; }

/* Left: bars */
.res-skills-bars { display: flex; flex-direction: column; gap: 1.2rem; }
.sk-row { display: flex; flex-direction: column; gap: .4rem; }
.sk-info { display: flex; justify-content: space-between; align-items: center; }
.sk-name { font-size: .95rem; font-weight: 500; color: var(--text); }
.sk-meta { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .03em; }
.sk-bar { width: 100%; height: 6px; background: var(--surf); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }
.sk-fill {
  height: 100%; background: var(--acc); border-radius: 4px;
  width: 0%; transition: width 1.2s cubic-bezier(0.22, 1, 0.36, 1);
}

/* Right: tools chips */
.res-tools { }
.res-tools-label {
  font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .1em;
  text-transform: uppercase; color: var(--muted); font-weight: 600; margin-bottom: 1rem;
}
.res-tools-grid { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem; letter-spacing: .03em;
  padding: .4rem .75rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS — FULL GRID
   ══════════════════════════════════════════ */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1rem;
}

.cert-card {
  display: flex; overflow: hidden;
  border: 1px solid var(--bord); border-radius: 8px;
  text-decoration: none; transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--surf);
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}
.cert-card:hover {
  border-color: var(--acc);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(139,92,246,.12);
}

.cert-card-accent { width: 4px; flex-shrink: 0; }
.cert-card-body { padding: 1.25rem 1.5rem; flex: 1; display: flex; flex-direction: column; gap: .65rem; }

.cert-card-issuer { display: flex; align-items: center; gap: .65rem; }
.cert-badge {
  width: 28px; height: 28px; border-radius: 5px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem;
  letter-spacing: .04em; color: var(--muted); font-weight: 500;
}

.cert-card-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: 1.05rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3;
}

.cert-card-footer { display: flex; align-items: center; justify-content: space-between; margin-top: auto; }
.cert-card-date { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); letter-spacing: .05em; }
.cert-card-view { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--acc); letter-spacing: .05em; font-weight: 500; }

/* ══════════════════════════════════════════
   DOWNLOAD CTA
   ══════════════════════════════════════════ */
.res-download { margin-top: 2rem; }
.res-download-inner {
  display: flex; align-items: center; justify-content: space-between;
  padding: 2.5rem 3rem; border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-download-text h4 {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); margin-bottom: .3rem;
}
.res-download-text p {
  font-size: .95rem; color: var(--muted); font-weight: 300;
}

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s, box-shadow .2s;
  text-decoration: none; border: none; white-space: nowrap;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════════════════════════════════════
   RESPONSIVE
   ══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume { padding: 6.5rem 1.5rem 4rem; }
  .res-header { flex-direction: column; gap: 1.5rem; padding: 2rem 1.5rem; }
  .res-header-right { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: 1rem; }
  .res-skills-grid { grid-template-columns: 1fr; gap: 2rem; }
  .cert-grid { grid-template-columns: 1fr; }
  .res-download-inner { flex-direction: column; gap: 1.5rem; text-align: center; padding: 2rem 1.5rem; }
  .btn, .cert-card, .res-tag, .res-tool-chip { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="pin">

    <!-- SECTION LABEL -->
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ resume</span>
      <div class="shr"></div>
    </div>

    <!-- ═══════════ RESUME HEADER ═══════════ -->
    <div class="res-header au d1">
      <div class="res-header-left">
        <h2 class="res-name">Jaren Kendrick Yambao</h2>
        <p class="res-title">Full Stack Developer · Game/App Developer</p>
        <p class="res-location">Pampanga, Philippines</p>
      </div>
      <div class="res-header-right">
        <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
          jarenkendrickyambao&#64;gmail.com
        </a>
        <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
          linkedin.com/in/jarenkendrick
        </a>
        <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
          github.com/jarenkendrick14
        </a>
      </div>
    </div>

    <!-- ═══════════ EXPERIENCE ═══════════ -->
    <div class="res-section au d2">
      <h3 class="res-section-title">Experience</h3>
      <div class="res-divider"></div>
      @for (job of experience; track job.company) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ job.period }}</span>
            <span class="res-entry-type">{{ job.type }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ job.title }}</h4>
              <span class="res-entry-company">{{ job.company }}</span>
            </div>
            <ul class="res-bullets">
              @for (bullet of job.bullets; track $index) {
                <li [innerHTML]="bullet"></li>
              }
            </ul>
            <div class="res-tags">
              @for (tag of job.tags; track tag) {
                <span class="res-tag">{{ tag }}</span>
              }
            </div>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ EDUCATION ═══════════ -->
    <div class="res-section au d3">
      <h3 class="res-section-title">Education</h3>
      <div class="res-divider"></div>
      @for (edu of education; track edu.school) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ edu.period }}</span>
            <span class="res-entry-type">{{ edu.location }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ edu.degree }}</h4>
              <span class="res-entry-company">{{ edu.school }}</span>
            </div>
            <p class="res-edu-detail">{{ edu.detail }}</p>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ SKILLS — two columns ═══════════ -->
    <div class="res-section au d4">
      <h3 class="res-section-title">Technical Skills</h3>
      <div class="res-divider"></div>
      <div class="res-skills-grid">
        <!-- LEFT: Proficiency bars -->
        <div class="res-skills-bars">
          @for (sk of skills; track sk.name; let i = $index) {
            <div class="sk-row">
              <div class="sk-info">
                <span class="sk-name">{{ sk.name }}</span>
                <span class="sk-meta">{{ sk.time }} · {{ sk.pct }}%</span>
              </div>
              <div class="sk-bar">
                <div class="sk-fill"
                  [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                  [style.transition-delay]="(i * 0.08) + 's'">
                </div>
              </div>
            </div>
          }
        </div>
        <!-- RIGHT: Tools & technologies -->
        <div class="res-tools">
          <div class="res-tools-label">Tools & Technologies</div>
          <div class="res-tools-grid">
            @for (tool of toolsList; track tool) {
              <span class="res-tool-chip">{{ tool }}</span>
            }
          </div>
        </div>
      </div>
    </div>

    <!-- ═══════════ CERTIFICATIONS — FULL GRID ═══════════ -->
    <div class="res-section au d5">
      <h3 class="res-section-title">Certifications<span class="res-cert-count">{{ data.certificates.length }}</span></h3>
      <div class="res-divider"></div>
      <div class="cert-grid">
        @for (cert of data.certificates; track cert.name; let i = $index) {
          <a class="cert-card" [href]="cert.url" target="_blank" rel="noopener"
             [style.animation-delay]="(i * 0.04) + 's'">
            <div class="cert-card-accent" [style.background]="cert.color"></div>
            <div class="cert-card-body">
              <div class="cert-card-issuer">
                <div class="cert-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                <span class="cert-issuer-name">{{ cert.issuer }}</span>
              </div>
              <div class="cert-card-name">{{ cert.name }}</div>
              <div class="cert-card-footer">
                <span class="cert-card-date">{{ cert.date }}</span>
                <span class="cert-card-view">View ↗</span>
              </div>
            </div>
          </a>
        }
      </div>
    </div>

    <!-- ═══════════ DOWNLOAD BUTTON ═══════════ -->
    <div class="res-download au d6">
      <div class="res-download-inner">
        <div class="res-download-text">
          <h4>Want a copy?</h4>
          <p>Download the full PDF version of my resume.</p>
        </div>
        <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
          <svg style="width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
          Download Resume (PDF)
        </a>
      </div>
    </div>

  </div>
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong>, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular 21 + Ionic 8 + Node.js + PostgreSQL) with WebSocket messaging and streaming xAI Grok responses.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Collaborate asynchronously with remote team across multiple time zones, managing own sprint tasks and code reviews.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking fundamentals, and deployment workflows. Maintaining strong academic standing while working part-time as a developer.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }
[data-theme="light"] .tknob { transform: translateX(20px); }
.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  /*
    KEY FIX: .main-content must be position:relative (not fixed) on mobile
    so page sections stack in normal document flow and scrollIntoView works.
  */
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px; /* offset for fixed mobnav */
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host { display: block; min-height: 100vh; width: 100%; }

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 5rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* Animations */
.active-page .au.d0 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .04s both; }
.active-page .au.d1 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .au.d2 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .au.d3 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .26s both; }
.active-page .al.d1 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .al.d2 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d2 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d3 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s both; }

@keyframes fadeUp    { from { opacity:0; transform:translateY(36px); } to { opacity:1; transform:none; } }
@keyframes fadeLeft  { from { opacity:0; transform:translateX(-36px); } to { opacity:1; transform:none; } }
@keyframes fadeRight { from { opacity:0; transform:translateX(36px); } to { opacity:1; transform:none; } }

/* ═══════════════════════════════════════════════════════════
   MAIN TWO-COLUMN GRID
   ═══════════════════════════════════════════════════════════ */
.ag {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

/* ═══════════════════════════════════════════════════════════
   BIO — PHOTO + BADGES
   ═══════════════════════════════════════════════════════════ */
.bio-header { margin-bottom: 2rem; }

.bio-photo-wrap {
  display: flex;
  align-items: flex-end;
  gap: 1rem;
  margin-bottom: 1rem;
}

.bio-photo {
  width: 72px; height: 72px;
  border-radius: 50%;
  border: 2px solid var(--bord);
  overflow: hidden;
  flex-shrink: 0;
  transition: border-color .3s;
  position: relative;
}
.bio-photo:hover { border-color: var(--acc); }
.bio-photo img { width: 100%; height: 100%; object-fit: cover; }
.bio-photo-fallback {
  display: none;
  width: 100%; height: 100%;
  align-items: center; justify-content: center;
  background: var(--surf);
  color: var(--muted);
}

.bio-badges { display: flex; flex-wrap: wrap; gap: .5rem; }

.bio-badge {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--muted);
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 4px;
  padding: .3rem .65rem;
  letter-spacing: .03em;
}
.bio-badge.accent { color: var(--acc); border-color: rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   BIO TEXT
   ═══════════════════════════════════════════════════════════ */
.apull {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.5rem, 2.3vw, 2.1rem);
  font-weight: 600;
  line-height: 1.25;
  color: var(--white);
  letter-spacing: -.02em;
  margin-bottom: 1.5rem;
}
.apull em { color: var(--acc); font-style: italic; }

.abio { display: flex; flex-direction: column; gap: 1rem; margin-bottom: 1.5rem; }
.abio p { font-size: 1rem; color: var(--text); line-height: 1.75; font-weight: 300; }
.abio strong { color: var(--white); font-weight: 600; }

.bio-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: .5rem; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .75rem 1.5rem;
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border-radius: 6px; border: none; cursor: pointer;
  text-decoration: none; transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 24px rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   GITHUB ACTIVITY GRID
   ═══════════════════════════════════════════════════════════ */
.gh-section {
  margin-top: 2.5rem;
}

.gh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1rem;
}

.gh-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .12em;
  color: var(--muted);
  font-weight: 600;
  text-transform: uppercase;
}

.gh-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 600;
}

.gh-wrap {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 8px;
  padding: 1rem 1.25rem 1rem;
  overflow-x: auto;
}

/* Month labels row */
.gh-months {
  display: flex;
  margin-bottom: .35rem;
}

.gh-row-spacer { width: 28px; flex-shrink: 0; }

.gh-month-labels {
  display: grid;
  grid-template-columns: repeat(52, 13px);
  gap: 2px;
  flex: 1;
}

.gh-month-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

/* Grid + day labels */
.gh-grid-wrap {
  display: flex;
  gap: 4px;
}

.gh-day-labels {
  display: flex;
  flex-direction: column;
  gap: 2px;
  width: 24px;
  flex-shrink: 0;
}

.gh-day-labels span {
  height: 13px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 13px;
  text-align: right;
  padding-right: 2px;
}

.gh-grid {
  display: flex;
  gap: 2px;
}

.gh-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

/* Cells */
.gh-cell {
  width: 13px; height: 13px;
  border-radius: 2px;
  background: var(--surf);
  border: 1px solid var(--bord);
  transition: transform .15s;
}
.gh-cell:hover { transform: scale(1.4); z-index: 1; position: relative; }

/* Contribution intensity — dark mode */
.gh-cell.l1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-cell.l2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-cell.l3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-cell.l4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

/* Light mode overrides */
:host-context(.light) .gh-cell        { background: #eaedf0; border-color: #d1d5db; }
:host-context(.light) .gh-cell.l1    { background: #d1c4e9; border-color: #b39ddb; }
:host-context(.light) .gh-cell.l2    { background: #9575cd; border-color: #7e57c2; }
:host-context(.light) .gh-cell.l3    { background: #7c3aed; border-color: #6d28d9; }
:host-context(.light) .gh-cell.l4    { background: #5b21b6; border-color: #4c1d95; }

/* Legend specific cells (for the static legend items) */
.gh-legend .gh-0 { background: var(--surf); }
.gh-legend .gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-legend .gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-legend .gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-legend .gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

:host-context(.light) .gh-legend .gh-0 { background: #eaedf0; border-color: #d1d5db; }
:host-context(.light) .gh-legend .gh-1 { background: #d1c4e9; border-color: #b39ddb; }
:host-context(.light) .gh-legend .gh-2 { background: #9575cd; border-color: #7e57c2; }
:host-context(.light) .gh-legend .gh-3 { background: #7c3aed; border-color: #6d28d9; }
:host-context(.light) .gh-legend .gh-4 { background: #5b21b6; border-color: #4c1d95; }

/* Legend row */
.gh-legend {
  display: flex;
  align-items: center;
  gap: 3px;
  margin-top: .75rem;
  justify-content: flex-end;
}

.gh-legend-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  margin: 0 2px;
}

/* ═══════════════════════════════════════════════════════════
   SKILLS BLOCK
   ═══════════════════════════════════════════════════════════ */
.blk {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 10px;
  padding: 2rem;
  margin-bottom: 1.5rem;
}

.blkh {
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--white);
  font-weight: 700;
  margin-bottom: 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  color: var(--acc);
  background: transparent;
  border: 1px solid rgba(139,92,246,.4);
  border-radius: 4px;
  padding: .25rem .6rem;
  cursor: pointer;
  transition: background .2s, color .2s;
  flex-shrink: 0;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.15); }

/* Sphere */
.skill-sphere-wrap {
  width: 100%; height: 220px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.sphere-hint {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .08em;
  pointer-events: none;
}

/* Skill bars */
.skl { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 1rem; }

.sk-item {
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}

.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .3rem;
}

.sk-info span:first-child { font-size: .9rem; color: var(--text); font-weight: 500; }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); }

.sk-bar {
  width: 100%; height: 6px;
  background: var(--bg);
  border-radius: 4px;
  overflow: hidden;
  border: 1px solid var(--bord);
}

.sk-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--acc), rgba(167,139,250,.7));
  border-radius: 4px;
  width: 0;
  animation: barFill 1.2s cubic-bezier(.22,1,.36,1) forwards;
}

@keyframes barFill {
  from { width: 0; }
  to   { width: var(--target-width, 0%); }
}

/* ═══════════════════════════════════════════════════════════
   CERTIFICATIONS GRID
   ═══════════════════════════════════════════════════════════ */
.cert-count-badge {
  font-size: .65rem;
  background: var(--acc);
  color: #fff;
  padding: .1rem .45rem;
  border-radius: 10px;
  font-weight: 700;
  letter-spacing: .05em;
}

.cert-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: .75rem;
}

.cert-card-img {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  overflow: hidden;
  text-decoration: none;
  transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--bg);
}
.cert-card-img:hover {
  border-color: var(--acc);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(139,92,246,.12);
}

.cci-header {
  display: flex;
  align-items: center;
  gap: .6rem;
  padding: .6rem .8rem;
  border-bottom: 1px solid var(--bord);
}

.cci-badge {
  width: 24px; height: 24px;
  border-radius: 4px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem; font-weight: 700; color: #fff;
  flex-shrink: 0;
}

.cci-issuer {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  color: var(--muted);
  letter-spacing: .03em;
}

.cci-body { padding: .65rem .8rem; flex: 1; }

.cci-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .82rem;
  font-weight: 600;
  color: var(--white);
  line-height: 1.3;
  margin-bottom: .45rem;
}

.cci-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.cci-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
}

.cci-view {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--acc);
}

/* ═══════════════════════════════════════════════════════════
   RESUME OVERLAY
   ═══════════════════════════════════════════════════════════ */
.resume-overlay {
  position: fixed;
  inset: 0;
  z-index: 8000;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  animation: slideUp .44s cubic-bezier(.22,1,.36,1) both;
}

.resume-overlay.resume-closing {
  animation: slideDown .44s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideUp   { from { transform: translateY(100%); opacity:0; } to { transform: none; opacity:1; } }
@keyframes slideDown { from { transform: none; opacity:1; } to { transform: translateY(100%); opacity:0; } }

/* Header bar */
.resume-hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 2rem;
  border-bottom: 1px solid var(--bord);
  background: var(--surf);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

.resume-hd-left { }
.resume-hd-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  color: var(--muted);
  letter-spacing: .08em;
  font-weight: 600;
  text-transform: uppercase;
}

.resume-hd-right { display: flex; align-items: center; gap: 1rem; }

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .5rem 1.1rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  background: var(--acc);
  color: #fff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s, box-shadow .2s;
}
.resume-actions:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139,92,246,.35); }

.resume-close-btn {
  width: 36px; height: 36px;
  border: 1px solid var(--bord);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  color: var(--muted);
  font-size: 1rem;
  background: transparent;
  cursor: pointer;
  transition: border-color .2s, color .2s;
}
.resume-close-btn:hover { border-color: var(--acc); color: var(--acc); }

/* Resume scrolling body */
.resume-body {
  flex: 1;
  overflow-y: auto;
  display: flex;
  justify-content: center;
  padding: 3rem 2rem 4rem;
  background: var(--bg);
}

/* White paper-like resume page */
.resume-page {
  width: 100%;
  max-width: 860px;
  background: #fff;
  color: #1a1a2e;
  border-radius: 4px;
  padding: 3rem 3.5rem;
  box-shadow: 0 4px 60px rgba(0,0,0,.25);
  font-family: 'Plus Jakarta Sans', sans-serif;
}

/* Resume internals */
.res-name-block { margin-bottom: 2rem; border-bottom: 2px solid #1a1a2e; padding-bottom: 1.25rem; }
.res-name { font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; font-weight: 700; color: #1a1a2e; margin-bottom: .25rem; letter-spacing: -.02em; }
.res-role { font-size: 1.05rem; color: #5c5c7a; font-weight: 500; margin-bottom: .5rem; }
.res-contact { display: flex; flex-wrap: wrap; gap: 1rem; font-size: .8rem; color: #5c5c7a; }
.res-contact a { color: #5b21b6; text-decoration: none; }
.res-contact a:hover { text-decoration: underline; }

.res-cols { display: grid; grid-template-columns: 1.4fr .9fr; gap: 3rem; }

.resume-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: #5b21b6;
  margin-bottom: .65rem;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: .35rem;
}

.res-section { margin-bottom: 1.75rem; }
.res-text { font-size: .9rem; color: #4b5563; line-height: 1.7; font-weight: 400; }

.res-exp-item { margin-bottom: 1.25rem; }
.res-exp-header { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .2rem; }
.res-exp-title { font-weight: 700; font-size: .92rem; color: #1a1a2e; }
.res-exp-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: #9ca3af; flex-shrink: 0; }
.res-exp-org { font-size: .8rem; color: #6b7280; margin-bottom: .5rem; }
.res-exp-list { padding-left: 1rem; margin: 0; display: flex; flex-direction: column; gap: .3rem; }
.res-exp-list li { font-size: .85rem; color: #4b5563; line-height: 1.6; }

.res-proj-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px dashed #e5e7eb; }
.res-proj-item:last-child { border-bottom: none; }
.res-proj-header { display: flex; justify-content: space-between; align-items: center; gap: .5rem; margin-bottom: .25rem; }
.res-proj-name { font-weight: 700; font-size: .9rem; color: #1a1a2e; }
.res-proj-tags { display: flex; gap: .3rem; flex-wrap: wrap; }
.res-tag { font-family: 'JetBrains Mono', monospace; font-size: .6rem; padding: .15rem .4rem; border-radius: 3px; background: #ede9fe; color: #5b21b6; }
.res-proj-desc { font-size: .82rem; color: #6b7280; line-height: 1.55; }

.res-skill-chips { display: flex; flex-wrap: wrap; gap: .4rem; }
.res-chip { font-family: 'JetBrains Mono', monospace; font-size: .68rem; padding: .25rem .55rem; background: #f3f4f6; border: 1px solid #e5e7eb; border-radius: 4px; color: #374151; }

.res-cert-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
.res-cert-item { display: flex; align-items: center; gap: .55rem; font-size: .82rem; }
.res-cert-issuer { font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700; padding: .15rem .4rem; border-radius: 3px; flex-shrink: 0; }
.res-cert-name { flex: 1; color: #1a1a2e; font-weight: 500; font-size: .82rem; }
.res-cert-date { font-family: 'JetBrains Mono', monospace; font-size: .62rem; color: #9ca3af; flex-shrink: 0; }
.res-cert-more { font-style: italic; color: #9ca3af; font-size: .8rem; }
.res-cert-count { font-size: .68rem; background: #5b21b6; color: #fff; padding: .1rem .4rem; border-radius: 8px; }

/* Print styles */
@media print {
  .resume-hd, .page-about, app-nav { display: none !important; }
  .resume-overlay { position: static !important; animation: none !important; }
  .resume-body { padding: 0; overflow: visible; }
  .resume-page { box-shadow: none; border-radius: 0; padding: 2rem; }
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-about { padding: 6rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 2rem; }
  .cert-grid { grid-template-columns: 1fr; }
  .res-cols { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            <span class="gh-count">{{ githubTotal }} CONTRIBUTIONS</span>
          </div>

          <div class="gh-wrap">
            <!-- Month labels -->
            <div class="gh-months">
              <div class="gh-row-spacer"></div>
              <div class="gh-month-labels">
                @for (m of monthLabels; track m.col) {
                  <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                }
              </div>
            </div>

            <!-- Day labels + grid -->
            <div class="gh-grid-wrap">
              <div class="gh-day-labels">
                <span></span>
                <span>Mon</span>
                <span></span>
                <span>Wed</span>
                <span></span>
                <span>Fri</span>
                <span></span>
              </div>
              <div class="gh-grid">
                @for (week of githubWeeks; track $index) {
                  <div class="gh-col">
                    @for (day of week; track $index) {
                      <div class="gh-cell" [class]="getCellClass(day)"
                           [title]="day > 0 ? day + ' contribution' + (day > 1 ? 's' : '') : 'No contributions'">
                      </div>
                    }
                  </div>
                }
              </div>
            </div>

            <!-- Legend -->
            <div class="gh-legend">
              <span class="gh-legend-lbl">Less</span>
              <div class="gh-cell gh-0"></div>
              <div class="gh-cell gh-1"></div>
              <div class="gh-cell gh-2"></div>
              <div class="gh-cell gh-3"></div>
              <div class="gh-cell gh-4"></div>
              <span class="gh-legend-lbl">More</span>
            </div>
          </div>
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars (remount on every visit to retrigger CSS animation) -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — image grid -->
        <div class="blk ar d3">
          <div class="blkh">Certifications <span class="cert-count-badge">{{ data.certificates.length }}</span></div>
          <div class="cert-grid">
            @for (cert of data.certificates; track cert.name) {
              <a class="cert-card-img" [href]="cert.url" target="_blank" rel="noopener"
                 [title]="cert.name + ' — ' + cert.issuer">
                <div class="cci-header" [style.background]="cert.color + '22'" [style.border-color]="cert.color + '44'">
                  <div class="cci-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                  <span class="cci-issuer">{{ cert.issuer }}</span>
                </div>
                <div class="cci-body">
                  <div class="cci-name">{{ cert.name }}</div>
                  <div class="cci-footer">
                    <span class="cci-date">{{ cert.date }}</span>
                    <span class="cci-view">View ↗</span>
                  </div>
                </div>
              </a>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: src\app\components\about\about.component.ts ---
import {
  Component, inject, signal, ElementRef, ViewChild,
  AfterViewInit, OnDestroy, OnChanges, Input, SimpleChanges
} from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy, OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap')      sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex      = signal(0);
  readonly certAnimClass  = signal('');
  readonly barsReady      = signal(true);
  readonly showResume     = signal(false);
  readonly resumeClosing  = signal(false);

  // ── GitHub activity ────────────────────────────────────────────────────────
  readonly githubWeeks: number[][] = this.generateGithubData();
  readonly githubTotal = 84;
  readonly monthLabels  = this.buildMonthLabels();

  private generateGithubData(): number[][] {
    const weeks = 52;
    const grid: number[][] = Array.from({ length: weeks }, () => new Array(7).fill(0));
    let seed = 137;
    const rng = () => { seed = (seed * 1664525 + 1013904223) & 0x7fffffff; return seed / 0x7fffffff; };
    let placed = 0;
    const target = 84;

    for (let w = 0; w < weeks && placed < target; w++) {
      const recency   = w / weeks;
      const weekProb  = 0.04 + recency * 0.38;
      for (let d = 0; d < 7 && placed < target; d++) {
        const isWeekend = d === 0 || d === 6;
        const prob = isWeekend ? weekProb * 0.35 : weekProb;
        if (rng() < prob) {
          const r = rng();
          const intensity = r < 0.50 ? 1 : r < 0.78 ? 2 : r < 0.92 ? 3 : 4;
          const actual    = Math.min(intensity, target - placed);
          if (actual > 0) { grid[w][d] = actual; placed += actual; }
        }
      }
    }
    return grid;
  }

  private buildMonthLabels(): { label: string; col: number }[] {
    const names = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    const now = new Date();
    const out: { label: string; col: number }[] = [];
    let last = -1;
    for (let w = 0; w < 52; w++) {
      const d = new Date(now);
      d.setDate(d.getDate() - (51 - w) * 7);
      const m = d.getMonth();
      if (m !== last) { out.push({ label: names[m], col: w }); last = m; }
    }
    return out;
  }

  getCellClass(v: number): string {
    if (v <= 0) return 'gh-0';
    if (v === 1) return 'gh-1';
    if (v === 2) return 'gh-2';
    if (v === 3) return 'gh-3';
    return 'gh-4';
  }

  // ── Skills ────────────────────────────────────────────────────────────────
  readonly skills = [
    { name: 'JavaScript / TypeScript', time: '2 yrs', pct: 85 },
    { name: 'C# (Backend Logic)',       time: '2 yrs', pct: 80 },
    { name: 'Angular',                  time: '1 yr',  pct: 75 },
    { name: 'Node.js & Express',        time: '1 yr',  pct: 75 },
    { name: 'Vue.js 3',                 time: '1 yr',  pct: 70 },
    { name: 'MongoDB',                  time: '1 yr',  pct: 65 },
  ];

  readonly sphereTags = [
    { text: 'Angular',    color: '#dd0031' }, { text: 'Vue.js 3',  color: '#42b883' },
    { text: 'Node.js',    color: '#339933' }, { text: 'MongoDB',   color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#',        color: '#9b59b6' },
    { text: 'Unity',      color: '#a78bfa' }, { text: 'Express.js',color: '#a4a1c6' },
    { text: 'REST APIs',  color: '#60a5fa' }, { text: 'Git',       color: '#f05030' },
    { text: 'VR / XR',   color: '#f59e0b' }, { text: 'Netlify',   color: '#00c7b7' },
  ];

  get currentCert(): Certificate { return this.data.certificates[this.certIndex()]; }
  get certTotal(): number        { return this.data.certificates.length; }

  // ── Lifecycle ─────────────────────────────────────────────────────────────
  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active) {
      // Retrigger fill animations every time the page becomes active
      this.barsReady.set(false);
      setTimeout(() => this.barsReady.set(true), 300);
    }
  }

  ngAfterViewInit() { this.initSphere(); }
  ngOnDestroy()     { cancelAnimationFrame(this.animId); }

  // ── Resume overlay ────────────────────────────────────────────────────────
  openResume()  { this.resumeClosing.set(false); this.showResume.set(true); }

  closeResume() {
    this.resumeClosing.set(true);
    setTimeout(() => { this.showResume.set(false); this.resumeClosing.set(false); }, 440);
  }

  printResume() {
    const style = document.createElement('style');
    style.id = '__resume-print-style';
    style.textContent = `
      @media print {
        body > * { display: none !important; }
        body > app-root { display: block !important; height: auto !important; overflow: visible !important; }
        body { overflow: visible !important; height: auto !important; }
        app-root .resume-overlay {
          display: flex !important; position: static !important;
          background: #fff !important; color: #111 !important;
          height: auto !important; overflow: visible !important;
          animation: none !important; transform: none !important;
        }
        .resume-close-btn, .resume-actions { display: none !important; }
        .resume-hd { background: #f8f8f8 !important; color: #111 !important; }
        .resume-section-title { color: #222 !important; }
        .res-chip, .res-tag { background: #eee !important; color: #333 !important; border-color: #ccc !important; }
      }
    `;
    document.head.appendChild(style);
    window.addEventListener('afterprint', () => {
      document.getElementById('__resume-print-style')?.remove();
    }, { once: true });
    setTimeout(() => window.print(), 60);
  }

  // ── Cert carousel ─────────────────────────────────────────────────────────
  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  toggleSkills() { this.skillsExpanded.update(v => !v); }

  // ── 3D Sphere ─────────────────────────────────────────────────────────────
  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  private initSphere() {
    const RADIUS = 95;
    const items = this.sphereTags.map((tag, i) => {
      const phi   = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el    = document.createElement('span');
      el.className   = 'sphere-tag';
      el.textContent = tag.text;
      el.style.cssText = `color:${tag.color};position:absolute;background:var(--surf);padding:4px 8px;border-radius:4px;border:1px solid var(--bord);cursor:grab;user-select:none;font-family:'JetBrains Mono',monospace;font-size:12px;white-space:nowrap;`;
      this.sphereContainer?.nativeElement.appendChild(el);
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);
      items.forEach(({ el, phi, theta }) => {
        const x = Math.sin(phi)*Math.cos(theta), y = Math.cos(phi), z = Math.sin(phi)*Math.sin(theta);
        const x2 = x*cosRY - z*sinRY, z2 = x*sinRY + z*cosRY;
        const y3 = y*cosRX - z2*sinRX, z3 = y*sinRX + z2*cosRX;
        const scale = (z3 + 2.2) / 3.2;
        el.style.transform   = `translate(-50%,-50%) translate(${x2*RADIUS}px,${y3*RADIUS}px)`;
        el.style.opacity     = Math.max(0.1, scale).toString();
        el.style.zIndex      = Math.round(scale*20).toString();
        el.style.borderColor = z3 > 0 ? 'rgba(167,139,250,.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) { this.rotY += this.velY; this.rotX += this.velX; this.velX *= 0.97; }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  /* height is set dynamically by JS to match scrollHeight */
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

/* ══════════ PHOTO — bigger, centered, decorative rings ══════════ */
.h-photo {
  width: 280px;
  height: 280px;
  flex-shrink: 0;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Outer decorative ring */
.h-photo::before {
  content: '';
  position: absolute;
  inset: -12px;
  border-radius: 50%;
  border: 1px dashed var(--bord);
  opacity: .5;
  animation: spinSlow 30s linear infinite;
}

/* Inner decorative ring */
.h-photo::after {
  content: '';
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  border: 1px solid rgba(139,92,246,.2);
}

@keyframes spinSlow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px solid var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
  z-index: 1;
}
.h-photo-placeholder:hover { border-color: var(--acc); }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb { height: 120px; }

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-home { padding: 7rem 1.5rem 4rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: center; gap: 2.5rem; margin-bottom: 4rem; }
  .h-photo { width: 200px; height: 200px; }
  .feat-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-ghost" download>Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]"></div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
:host { display: block; min-height: 100vh; width: 100%; }

.page-resume { width: 100%; min-height: 100vh; padding: 5rem 5rem 3rem; background: var(--bg); overflow-y: auto; }
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* ── Animations ── */
.active-page .au.d0  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .au.d1  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .au.d2  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .au.d3  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .26s ease both; }
.active-page .au.d4  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .34s ease both; }
.active-page .au.d5  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .42s ease both; }
.active-page .au.d6  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .50s ease both; }

/* ══════════════════════════════════════════
   RESUME HEADER
   ══════════════════════════════════════════ */
.res-header {
  display: flex; justify-content: space-between; align-items: flex-start;
  padding: 2.5rem 3rem; margin-bottom: 3.5rem;
  border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-header-left { flex: 1; }
.res-name {
  font-family: 'Cormorant Garamond', serif; font-weight: 700;
  font-size: clamp(2.2rem, 3.5vw, 3rem); line-height: 1.1;
  letter-spacing: -.02em; color: var(--white); margin-bottom: .5rem;
}
.res-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.1rem;
  color: var(--acc); font-weight: 500; margin-bottom: .3rem;
}
.res-location {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}
.res-header-right {
  display: flex; flex-direction: column; gap: .65rem; align-items: flex-end;
}
.res-contact-link {
  display: flex; align-items: center; gap: .6rem;
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--text); text-decoration: none; transition: color .2s; letter-spacing: .02em;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg { width: 16px; height: 16px; fill: currentColor; flex-shrink: 0; }
.res-contact-link svg[fill="none"] { fill: none; }

/* ══════════════════════════════════════════
   SECTIONS
   ══════════════════════════════════════════ */
.res-section { margin-bottom: 3.5rem; }
.res-section-title {
  font-family: 'JetBrains Mono', monospace; font-size: .85rem;
  letter-spacing: .14em; text-transform: uppercase; font-weight: 700;
  color: var(--white); display: flex; align-items: center; gap: .75rem;
}
.res-cert-count {
  font-size: .7rem; background: var(--acc); color: #fff;
  padding: .15rem .5rem; border-radius: 10px; font-weight: 600; letter-spacing: .05em;
}
.res-divider { height: 2px; background: linear-gradient(90deg, var(--acc), transparent); margin: 1rem 0 2rem; border-radius: 2px; }

/* ── Entry layout (experience / education) ── */
.res-entry {
  display: grid; grid-template-columns: 200px 1fr; gap: 2.5rem;
  padding: 2rem 0; border-bottom: 1px solid var(--bord);
}
.res-entry:last-child { border-bottom: none; }

.res-entry-side { display: flex; flex-direction: column; gap: .3rem; padding-top: .2rem; }
.res-entry-period {
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--acc); font-weight: 600; letter-spacing: .03em;
}
.res-entry-type {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  color: var(--muted); letter-spacing: .03em;
}

.res-entry-main { }
.res-entry-header { margin-bottom: 1rem; }
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); letter-spacing: -.01em; margin-bottom: .2rem;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}

.res-bullets {
  list-style: none; padding: 0; display: flex; flex-direction: column; gap: .8rem; margin-bottom: 1.25rem;
}
.res-bullets li {
  font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300;
  padding-left: 1.5rem; position: relative;
}
.res-bullets li::before {
  content: '▸'; position: absolute; left: 0; color: var(--acc); font-size: .85rem; top: .15rem;
}
.res-bullets li :is(strong, b) { color: var(--white); font-weight: 600; }

.res-edu-detail { font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300; }

.res-tags { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tag {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .04em;
  padding: .3rem .65rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tag:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   SKILLS — two columns
   ══════════════════════════════════════════ */
.res-skills-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 3rem; }

/* Left: bars */
.res-skills-bars { display: flex; flex-direction: column; gap: 1.2rem; }
.sk-row { display: flex; flex-direction: column; gap: .4rem; }
.sk-info { display: flex; justify-content: space-between; align-items: center; }
.sk-name { font-size: .95rem; font-weight: 500; color: var(--text); }
.sk-meta { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .03em; }
.sk-bar { width: 100%; height: 6px; background: var(--surf); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }
.sk-fill {
  height: 100%; background: var(--acc); border-radius: 4px;
  width: 0%; transition: width 1.2s cubic-bezier(0.22, 1, 0.36, 1);
}

/* Right: tools chips */
.res-tools { }
.res-tools-label {
  font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .1em;
  text-transform: uppercase; color: var(--muted); font-weight: 600; margin-bottom: 1rem;
}
.res-tools-grid { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem; letter-spacing: .03em;
  padding: .4rem .75rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS — FULL GRID
   ══════════════════════════════════════════ */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1rem;
}

.cert-card {
  display: flex; overflow: hidden;
  border: 1px solid var(--bord); border-radius: 8px;
  text-decoration: none; transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--surf);
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}
.cert-card:hover {
  border-color: var(--acc);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(139,92,246,.12);
}

.cert-card-accent { width: 4px; flex-shrink: 0; }
.cert-card-body { padding: 1.25rem 1.5rem; flex: 1; display: flex; flex-direction: column; gap: .65rem; }

.cert-card-issuer { display: flex; align-items: center; gap: .65rem; }
.cert-badge {
  width: 28px; height: 28px; border-radius: 5px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem;
  letter-spacing: .04em; color: var(--muted); font-weight: 500;
}

.cert-card-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: 1.05rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3;
}

.cert-card-footer { display: flex; align-items: center; justify-content: space-between; margin-top: auto; }
.cert-card-date { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); letter-spacing: .05em; }
.cert-card-view { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--acc); letter-spacing: .05em; font-weight: 500; }

/* ══════════════════════════════════════════
   DOWNLOAD CTA
   ══════════════════════════════════════════ */
.res-download { margin-top: 2rem; }
.res-download-inner {
  display: flex; align-items: center; justify-content: space-between;
  padding: 2.5rem 3rem; border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-download-text h4 {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); margin-bottom: .3rem;
}
.res-download-text p {
  font-size: .95rem; color: var(--muted); font-weight: 300;
}

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s, box-shadow .2s;
  text-decoration: none; border: none; white-space: nowrap;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════════════════════════════════════
   RESPONSIVE
   ══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume { padding: 6.5rem 1.5rem 4rem; }
  .res-header { flex-direction: column; gap: 1.5rem; padding: 2rem 1.5rem; }
  .res-header-right { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: 1rem; }
  .res-skills-grid { grid-template-columns: 1fr; gap: 2rem; }
  .cert-grid { grid-template-columns: 1fr; }
  .res-download-inner { flex-direction: column; gap: 1.5rem; text-align: center; padding: 2rem 1.5rem; }
  .btn, .cert-card, .res-tag, .res-tool-chip { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="pin">

    <!-- SECTION LABEL -->
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ resume</span>
      <div class="shr"></div>
    </div>

    <!-- ═══════════ RESUME HEADER ═══════════ -->
    <div class="res-header au d1">
      <div class="res-header-left">
        <h2 class="res-name">Jaren Kendrick Yambao</h2>
        <p class="res-title">Full Stack Developer · Game/App Developer</p>
        <p class="res-location">Pampanga, Philippines</p>
      </div>
      <div class="res-header-right">
        <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
          jarenkendrickyambao&#64;gmail.com
        </a>
        <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
          linkedin.com/in/jarenkendrick
        </a>
        <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
          github.com/jarenkendrick14
        </a>
      </div>
    </div>

    <!-- ═══════════ EXPERIENCE ═══════════ -->
    <div class="res-section au d2">
      <h3 class="res-section-title">Experience</h3>
      <div class="res-divider"></div>
      @for (job of experience; track job.company) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ job.period }}</span>
            <span class="res-entry-type">{{ job.type }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ job.title }}</h4>
              <span class="res-entry-company">{{ job.company }}</span>
            </div>
            <ul class="res-bullets">
              @for (bullet of job.bullets; track $index) {
                <li [innerHTML]="bullet"></li>
              }
            </ul>
            <div class="res-tags">
              @for (tag of job.tags; track tag) {
                <span class="res-tag">{{ tag }}</span>
              }
            </div>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ EDUCATION ═══════════ -->
    <div class="res-section au d3">
      <h3 class="res-section-title">Education</h3>
      <div class="res-divider"></div>
      @for (edu of education; track edu.school) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ edu.period }}</span>
            <span class="res-entry-type">{{ edu.location }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ edu.degree }}</h4>
              <span class="res-entry-company">{{ edu.school }}</span>
            </div>
            <p class="res-edu-detail">{{ edu.detail }}</p>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ SKILLS — two columns ═══════════ -->
    <div class="res-section au d4">
      <h3 class="res-section-title">Technical Skills</h3>
      <div class="res-divider"></div>
      <div class="res-skills-grid">
        <!-- LEFT: Proficiency bars -->
        <div class="res-skills-bars">
          @for (sk of skills; track sk.name; let i = $index) {
            <div class="sk-row">
              <div class="sk-info">
                <span class="sk-name">{{ sk.name }}</span>
                <span class="sk-meta">{{ sk.time }} · {{ sk.pct }}%</span>
              </div>
              <div class="sk-bar">
                <div class="sk-fill"
                  [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                  [style.transition-delay]="(i * 0.08) + 's'">
                </div>
              </div>
            </div>
          }
        </div>
        <!-- RIGHT: Tools & technologies -->
        <div class="res-tools">
          <div class="res-tools-label">Tools & Technologies</div>
          <div class="res-tools-grid">
            @for (tool of toolsList; track tool) {
              <span class="res-tool-chip">{{ tool }}</span>
            }
          </div>
        </div>
      </div>
    </div>

    <!-- ═══════════ CERTIFICATIONS — FULL GRID ═══════════ -->
    <div class="res-section au d5">
      <h3 class="res-section-title">Certifications<span class="res-cert-count">{{ data.certificates.length }}</span></h3>
      <div class="res-divider"></div>
      <div class="cert-grid">
        @for (cert of data.certificates; track cert.name; let i = $index) {
          <a class="cert-card" [href]="cert.url" target="_blank" rel="noopener"
             [style.animation-delay]="(i * 0.04) + 's'">
            <div class="cert-card-accent" [style.background]="cert.color"></div>
            <div class="cert-card-body">
              <div class="cert-card-issuer">
                <div class="cert-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                <span class="cert-issuer-name">{{ cert.issuer }}</span>
              </div>
              <div class="cert-card-name">{{ cert.name }}</div>
              <div class="cert-card-footer">
                <span class="cert-card-date">{{ cert.date }}</span>
                <span class="cert-card-view">View ↗</span>
              </div>
            </div>
          </a>
        }
      </div>
    </div>

    <!-- ═══════════ DOWNLOAD BUTTON ═══════════ -->
    <div class="res-download au d6">
      <div class="res-download-inner">
        <div class="res-download-text">
          <h4>Want a copy?</h4>
          <p>Download the full PDF version of my resume.</p>
        </div>
        <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
          <svg style="width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
          Download Resume (PDF)
        </a>
      </div>
    </div>

  </div>
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong>, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular 21 + Ionic 8 + Node.js + PostgreSQL) with WebSocket messaging and streaming xAI Grok responses.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Collaborate asynchronously with remote team across multiple time zones, managing own sprint tasks and code reviews.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking fundamentals, and deployment workflows. Maintaining strong academic standing while working part-time as a developer.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }
[data-theme="light"] .tknob { transform: translateX(20px); }
.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  /*
    KEY FIX: .main-content must be position:relative (not fixed) on mobile
    so page sections stack in normal document flow and scrollIntoView works.
  */
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px; /* offset for fixed mobnav */
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host { display: block; min-height: 100vh; width: 100%; }

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 4rem 5rem 5rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 2.5rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* Animations */
.active-page .au.d0 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .04s both; }
.active-page .au.d1 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .au.d2 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .au.d3 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .26s both; }
.active-page .al.d1 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .al.d2 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d2 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d3 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s both; }

@keyframes fadeUp    { from { opacity:0; transform:translateY(36px); } to { opacity:1; transform:none; } }
@keyframes fadeLeft  { from { opacity:0; transform:translateX(-36px); } to { opacity:1; transform:none; } }
@keyframes fadeRight { from { opacity:0; transform:translateX(36px); } to { opacity:1; transform:none; } }

/* ═══════════════════════════════════════════════════════════
   MAIN TWO-COLUMN GRID
   ═══════════════════════════════════════════════════════════ */
.ag {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

/* ═══════════════════════════════════════════════════════════
   BIO — PHOTO + BADGES
   ═══════════════════════════════════════════════════════════ */
.bio-header { margin-bottom: 1.75rem; }

.bio-photo-wrap {
  display: flex;
  align-items: flex-end;
  gap: 1rem;
  margin-bottom: 1rem;
}

.bio-photo {
  width: 72px; height: 72px;
  border-radius: 50%;
  border: 2px solid var(--bord);
  overflow: hidden;
  flex-shrink: 0;
  transition: border-color .3s;
  position: relative;
}
.bio-photo:hover { border-color: var(--acc); }
.bio-photo img { width: 100%; height: 100%; object-fit: cover; }
.bio-photo-fallback {
  display: none;
  width: 100%; height: 100%;
  align-items: center; justify-content: center;
  background: var(--surf);
  color: var(--muted);
}

.bio-badges { display: flex; flex-wrap: wrap; gap: .4rem; }

.bio-badge {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--muted);
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 4px;
  padding: .3rem .65rem;
  letter-spacing: .03em;
}
.bio-badge.accent { color: var(--acc); border-color: rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   BIO TEXT
   ═══════════════════════════════════════════════════════════ */
.apull {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.6rem, 2.4vw, 2.2rem);
  font-weight: 600;
  line-height: 1.25;
  color: var(--white);
  letter-spacing: -.02em;
  margin-bottom: 1.25rem;
}
.apull em { color: var(--acc); font-style: italic; }

.abio { display: flex; flex-direction: column; gap: .9rem; margin-bottom: 1.5rem; }
.abio p { font-size: 1rem; color: var(--text); line-height: 1.75; font-weight: 300; }
.abio strong { color: var(--white); font-weight: 600; }

.bio-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: .25rem; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .75rem 1.5rem;
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border-radius: 6px; border: none; cursor: pointer;
  text-decoration: none; transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 24px rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   GITHUB ACTIVITY GRID
   FIX: cell classes are gh-0..gh-4 (match getCellClass() in TS)
   ═══════════════════════════════════════════════════════════ */
.gh-section {
  margin-top: 2rem;
}

.gh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .85rem;
}

.gh-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .12em;
  color: var(--muted);
  font-weight: 600;
  text-transform: uppercase;
}

.gh-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 600;
}

.gh-wrap {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 8px;
  padding: 1rem 1.25rem;
  overflow-x: auto;
}

/* Month labels row */
.gh-months {
  display: flex;
  margin-bottom: .35rem;
}

.gh-row-spacer { width: 28px; flex-shrink: 0; }

.gh-month-labels {
  display: grid;
  /* 26 weeks × 13px + gaps */
  grid-template-columns: repeat(26, 13px);
  gap: 2px;
  flex: 1;
}

.gh-month-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

/* Grid + day labels */
.gh-grid-wrap {
  display: flex;
  gap: 4px;
}

.gh-day-labels {
  display: flex;
  flex-direction: column;
  gap: 2px;
  width: 24px;
  flex-shrink: 0;
}

.gh-day-labels span {
  height: 13px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 13px;
  text-align: right;
  padding-right: 2px;
}

.gh-grid {
  display: flex;
  gap: 2px;
}

.gh-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

/* ─── Cells ─── */
.gh-cell {
  width: 13px; height: 13px;
  border-radius: 2px;
  background: var(--surf);
  border: 1px solid var(--bord);
  transition: transform .15s;
}
.gh-cell:hover { transform: scale(1.4); z-index: 1; position: relative; }

/* ✅ FIX: match class names returned by getCellClass() in TS */
.gh-cell.gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-cell.gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-cell.gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-cell.gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

/* Light mode */
[data-theme="light"] .gh-cell        { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-cell.gh-1   { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-cell.gh-2   { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-cell.gh-3   { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-cell.gh-4   { background: #5b21b6; border-color: #4c1d95; }

/* Legend row */
.gh-legend {
  display: flex;
  align-items: center;
  gap: 3px;
  margin-top: .75rem;
  justify-content: flex-end;
}

.gh-legend-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  margin: 0 2px;
}

/* Legend static cells */
.gh-legend .gh-0 { background: var(--surf); }
.gh-legend .gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-legend .gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-legend .gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-legend .gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

[data-theme="light"] .gh-legend .gh-0 { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-legend .gh-1 { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-legend .gh-2 { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-legend .gh-3 { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-legend .gh-4 { background: #5b21b6; border-color: #4c1d95; }

/* ═══════════════════════════════════════════════════════════
   SKILLS BLOCK
   ═══════════════════════════════════════════════════════════ */
.blk {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 10px;
  padding: 1.75rem;
  margin-bottom: 1.25rem;
}

.blkh {
  font-family: 'JetBrains Mono', monospace;
  font-size: .78rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--white);
  font-weight: 700;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  color: var(--acc);
  background: transparent;
  border: 1px solid rgba(139,92,246,.4);
  border-radius: 4px;
  padding: .25rem .6rem;
  cursor: pointer;
  transition: background .2s, color .2s;
  flex-shrink: 0;
  white-space: nowrap;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.15); }

/* Sphere */
.skill-sphere-wrap {
  width: 100%; height: 210px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.sphere-hint {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .08em;
  pointer-events: none;
}

/* Skill bars */
.skl { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .9rem; }

.sk-item {
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}

.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .3rem;
}

.sk-info span:first-child { font-size: .88rem; color: var(--text); font-weight: 500; }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); }

.sk-bar {
  width: 100%; height: 5px;
  background: var(--bg);
  border-radius: 4px;
  overflow: hidden;
  border: 1px solid var(--bord);
}

.sk-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--acc), rgba(167,139,250,.7));
  border-radius: 4px;
  width: 0;
  animation: barFill 1.2s cubic-bezier(.22,1,.36,1) forwards;
}

@keyframes barFill {
  from { width: 0; }
  to   { width: var(--target-width, 0%); }
}

/* ═══════════════════════════════════════════════════════════
   CERTIFICATIONS GRID
   ═══════════════════════════════════════════════════════════ */
.cert-count-badge {
  font-size: .62rem;
  background: var(--acc);
  color: #fff;
  padding: .1rem .45rem;
  border-radius: 10px;
  font-weight: 700;
  letter-spacing: .05em;
}

.cert-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: .65rem;
  max-height: 520px;
  overflow-y: auto;
  padding-right: 2px;
}
.cert-grid::-webkit-scrollbar { width: 4px; }
.cert-grid::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.cert-card-img {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  overflow: hidden;
  text-decoration: none;
  transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--bg);
  cursor: pointer;
}
.cert-card-img:hover {
  border-color: var(--acc);
  transform: translateY(-2px);
  box-shadow: 0 6px 18px rgba(139,92,246,.12);
}

.cci-header {
  display: flex;
  align-items: center;
  gap: .55rem;
  padding: .55rem .75rem;
  border-bottom: 1px solid var(--bord);
}

.cci-badge {
  width: 22px; height: 22px;
  border-radius: 3px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: .5rem; font-weight: 700; color: #fff;
  flex-shrink: 0;
}

.cci-issuer {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  letter-spacing: .03em;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.cci-body { padding: .6rem .75rem; flex: 1; }

.cci-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .78rem;
  font-weight: 600;
  color: var(--white);
  line-height: 1.3;
  margin-bottom: .4rem;
}

.cci-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: .4rem;
}

.cci-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  color: var(--muted);
}

.cci-view {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  color: var(--acc);
  white-space: nowrap;
}

/* ═══════════════════════════════════════════════════════════
   RESUME OVERLAY
   ═══════════════════════════════════════════════════════════ */
.resume-overlay {
  position: fixed;
  inset: 0;
  z-index: 8000;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  animation: slideUp .44s cubic-bezier(.22,1,.36,1) both;
}

.resume-overlay.resume-closing {
  animation: slideDown .44s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideUp   { from { transform: translateY(100%); opacity:0; } to { transform: none; opacity:1; } }
@keyframes slideDown { from { transform: none; opacity:1; } to { transform: translateY(100%); opacity:0; } }

/* Header bar */
.resume-hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.1rem 2rem;
  border-bottom: 1px solid var(--bord);
  background: var(--surf);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

.resume-hd-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--muted);
  letter-spacing: .08em;
  font-weight: 600;
  text-transform: uppercase;
}

.resume-hd-right { display: flex; align-items: center; gap: 1rem; }

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .5rem 1.1rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  background: var(--acc);
  color: #fff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s, box-shadow .2s;
}
.resume-actions:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139,92,246,.35); }

.resume-close-btn {
  width: 36px; height: 36px;
  border: 1px solid var(--bord);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  color: var(--muted);
  font-size: 1rem;
  background: transparent;
  cursor: pointer;
  transition: border-color .2s, color .2s;
}
.resume-close-btn:hover { border-color: var(--acc); color: var(--acc); }

/* Scrollable body */
.resume-body {
  flex: 1;
  overflow-y: auto;
  display: flex;
  justify-content: center;
  padding: 3rem 2rem 4rem;
  background: var(--bg);
}

/* White paper resume card */
.resume-page {
  width: 100%;
  max-width: 860px;
  background: #fff;
  color: #1a1a2e;
  border-radius: 4px;
  padding: 3rem 3.5rem;
  box-shadow: 0 4px 60px rgba(0,0,0,.25);
  font-family: 'Plus Jakarta Sans', sans-serif;
}

/* Resume internals */
.res-name-block { margin-bottom: 2rem; border-bottom: 2px solid #1a1a2e; padding-bottom: 1.25rem; }
.res-name { font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; font-weight: 700; color: #1a1a2e; margin-bottom: .25rem; letter-spacing: -.02em; }
.res-role { font-size: 1.05rem; color: #5c5c7a; font-weight: 500; margin-bottom: .5rem; }
.res-contact { display: flex; flex-wrap: wrap; gap: 1rem; font-size: .8rem; color: #5c5c7a; }
.res-contact a { color: #5b21b6; text-decoration: none; }
.res-contact a:hover { text-decoration: underline; }

.res-cols { display: grid; grid-template-columns: 1.4fr .9fr; gap: 3rem; }

.resume-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: #5b21b6;
  margin-bottom: .65rem;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: .35rem;
}

.res-section { margin-bottom: 1.75rem; }
.res-text { font-size: .9rem; color: #4b5563; line-height: 1.7; font-weight: 400; }

.res-exp-item { margin-bottom: 1.25rem; }
.res-exp-header { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .2rem; }
.res-exp-title { font-weight: 700; font-size: .92rem; color: #1a1a2e; }
.res-exp-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: #9ca3af; flex-shrink: 0; }
.res-exp-org { font-size: .8rem; color: #6b7280; margin-bottom: .5rem; }
.res-exp-list { padding-left: 1rem; margin: 0; display: flex; flex-direction: column; gap: .3rem; }
.res-exp-list li { font-size: .85rem; color: #4b5563; line-height: 1.6; }

.res-proj-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px dashed #e5e7eb; }
.res-proj-item:last-child { border-bottom: none; }
.res-proj-header { display: flex; justify-content: space-between; align-items: center; gap: .5rem; margin-bottom: .25rem; }
.res-proj-name { font-weight: 700; font-size: .9rem; color: #1a1a2e; }
.res-proj-tags { display: flex; gap: .3rem; flex-wrap: wrap; }
.res-tag { font-family: 'JetBrains Mono', monospace; font-size: .6rem; padding: .15rem .4rem; border-radius: 3px; background: #ede9fe; color: #5b21b6; }
.res-proj-desc { font-size: .82rem; color: #6b7280; line-height: 1.55; }

.res-skill-chips { display: flex; flex-wrap: wrap; gap: .4rem; }
.res-chip { font-family: 'JetBrains Mono', monospace; font-size: .68rem; padding: .25rem .55rem; background: #f3f4f6; border: 1px solid #e5e7eb; border-radius: 4px; color: #374151; }

.res-cert-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
.res-cert-item { display: flex; align-items: center; gap: .55rem; font-size: .82rem; }
.res-cert-issuer { font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700; padding: .15rem .4rem; border-radius: 3px; flex-shrink: 0; }
.res-cert-name { flex: 1; color: #1a1a2e; font-weight: 500; font-size: .82rem; }
.res-cert-date { font-family: 'JetBrains Mono', monospace; font-size: .62rem; color: #9ca3af; flex-shrink: 0; }
.res-cert-more { font-style: italic; color: #9ca3af; font-size: .8rem; }
.res-cert-count { font-size: .68rem; background: #5b21b6; color: #fff; padding: .1rem .4rem; border-radius: 8px; }

/* Print */
@media print {
  .resume-hd, app-root > * { display: none !important; }
  .resume-overlay { position: static !important; animation: none !important; }
  .resume-body { padding: 0; overflow: visible; }
  .resume-page { box-shadow: none; border-radius: 0; padding: 2rem; }
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-about { padding: 5.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 2.5rem; }
  .cert-grid { grid-template-columns: 1fr 1fr; max-height: none; }
  .res-cols { grid-template-columns: 1fr; }
  .resume-page { padding: 2rem 1.5rem; }
  .resume-hd { padding: 1rem 1.25rem; }
  .skill-toggle-btn { cursor: pointer !important; }
}

@media (max-width: 480px) {
  .cert-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            <span class="gh-count">{{ githubTotal }} CONTRIBUTIONS</span>
          </div>

          <div class="gh-wrap">
            <!-- Month labels -->
            <div class="gh-months">
              <div class="gh-row-spacer"></div>
              <div class="gh-month-labels">
                @for (m of monthLabels; track m.col) {
                  <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                }
              </div>
            </div>

            <!-- Day labels + grid -->
            <div class="gh-grid-wrap">
              <div class="gh-day-labels">
                <span></span>
                <span>Mon</span>
                <span></span>
                <span>Wed</span>
                <span></span>
                <span>Fri</span>
                <span></span>
              </div>
              <div class="gh-grid">
                @for (week of githubWeeks; track $index) {
                  <div class="gh-col">
                    @for (day of week; track $index) {
                      <div class="gh-cell" [class]="getCellClass(day)"
                           [title]="day > 0 ? day + ' contribution' + (day > 1 ? 's' : '') : 'No contributions'">
                      </div>
                    }
                  </div>
                }
              </div>
            </div>

            <!-- Legend -->
            <div class="gh-legend">
              <span class="gh-legend-lbl">Less</span>
              <div class="gh-cell gh-0"></div>
              <div class="gh-cell gh-1"></div>
              <div class="gh-cell gh-2"></div>
              <div class="gh-cell gh-3"></div>
              <div class="gh-cell gh-4"></div>
              <span class="gh-legend-lbl">More</span>
            </div>
          </div>
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars (remount on every visit to retrigger CSS animation) -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — image grid -->
        <div class="blk ar d3">
          <div class="blkh">Certifications <span class="cert-count-badge">{{ data.certificates.length }}</span></div>
          <div class="cert-grid">
            @for (cert of data.certificates; track cert.name) {
              <a class="cert-card-img" [href]="cert.url" target="_blank" rel="noopener"
                 [title]="cert.name + ' — ' + cert.issuer">
                <div class="cci-header" [style.background]="cert.color + '22'" [style.border-color]="cert.color + '44'">
                  <div class="cci-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                  <span class="cci-issuer">{{ cert.issuer }}</span>
                </div>
                <div class="cci-body">
                  <div class="cci-name">{{ cert.name }}</div>
                  <div class="cci-footer">
                    <span class="cci-date">{{ cert.date }}</span>
                    <span class="cci-view">View ↗</span>
                  </div>
                </div>
              </a>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: src\app\components\about\about.component.ts ---
import {
  Component, inject, signal, ElementRef, ViewChild,
  AfterViewInit, OnDestroy, OnChanges, Input, SimpleChanges
} from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy, OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap')      sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex      = signal(0);
  readonly certAnimClass  = signal('');
  readonly barsReady      = signal(true);
  readonly showResume     = signal(false);
  readonly resumeClosing  = signal(false);

  // ── GitHub activity — 26 weeks so it fits nicely in the left column ────────
  readonly githubWeeks: number[][] = this.generateGithubData();
  readonly githubTotal = 84;
  readonly monthLabels = this.buildMonthLabels();

  /** Generate a deterministic-looking contribution grid (26 weeks × 7 days). */
  private generateGithubData(): number[][] {
    const WEEKS = 26;
    const grid: number[][] = Array.from({ length: WEEKS }, () => new Array(7).fill(0));
    let seed = 137;
    const rng = () => { seed = (seed * 1664525 + 1013904223) & 0x7fffffff; return seed / 0x7fffffff; };
    let placed = 0;
    const target = 84;

    for (let w = 0; w < WEEKS && placed < target; w++) {
      const recency  = w / WEEKS;
      const weekProb = 0.08 + recency * 0.55;            // ramp up towards recent weeks
      for (let d = 0; d < 7 && placed < target; d++) {
        const isWeekend = d === 0 || d === 6;
        const prob = isWeekend ? weekProb * 0.35 : weekProb;
        if (rng() < prob) {
          const r = rng();
          const intensity = r < 0.50 ? 1 : r < 0.78 ? 2 : r < 0.92 ? 3 : 4;
          const actual    = Math.min(intensity, target - placed);
          if (actual > 0) { grid[w][d] = actual; placed += actual; }
        }
      }
    }
    return grid;
  }

  /** Build month labels anchored to the last 26 weeks. */
  private buildMonthLabels(): { label: string; col: number }[] {
    const names = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    const WEEKS = 26;
    const now   = new Date();
    const out: { label: string; col: number }[] = [];
    let last = -1;
    for (let w = 0; w < WEEKS; w++) {
      const d = new Date(now);
      d.setDate(d.getDate() - (WEEKS - 1 - w) * 7);
      const m = d.getMonth();
      if (m !== last) { out.push({ label: names[m], col: w }); last = m; }
    }
    return out;
  }

  /** Returns CSS class for a cell value — must match .gh-cell.gh-N in CSS. */
  getCellClass(v: number): string {
    if (v <= 0) return 'gh-0';
    if (v === 1) return 'gh-1';
    if (v === 2) return 'gh-2';
    if (v === 3) return 'gh-3';
    return 'gh-4';
  }

  // ── Skills ────────────────────────────────────────────────────────────────
  readonly skills = [
    { name: 'JavaScript / TypeScript', time: '2 yrs', pct: 85 },
    { name: 'C# (Backend Logic)',       time: '2 yrs', pct: 80 },
    { name: 'Angular',                  time: '1 yr',  pct: 75 },
    { name: 'Node.js & Express',        time: '1 yr',  pct: 75 },
    { name: 'Vue.js 3',                 time: '1 yr',  pct: 70 },
    { name: 'MongoDB',                  time: '1 yr',  pct: 65 },
  ];

  readonly sphereTags = [
    { text: 'Angular',    color: '#dd0031' }, { text: 'Vue.js 3',  color: '#42b883' },
    { text: 'Node.js',    color: '#339933' }, { text: 'MongoDB',   color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#',        color: '#9b59b6' },
    { text: 'Unity',      color: '#a78bfa' }, { text: 'Express.js',color: '#a4a1c6' },
    { text: 'REST APIs',  color: '#60a5fa' }, { text: 'Git',       color: '#f05030' },
    { text: 'VR / XR',   color: '#f59e0b' }, { text: 'Netlify',   color: '#00c7b7' },
  ];

  get currentCert(): Certificate { return this.data.certificates[this.certIndex()]; }
  get certTotal(): number        { return this.data.certificates.length; }

  // ── Lifecycle ─────────────────────────────────────────────────────────────
  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active) {
      this.barsReady.set(false);
      setTimeout(() => this.barsReady.set(true), 300);
    }
  }

  ngAfterViewInit() { this.initSphere(); }
  ngOnDestroy()     { cancelAnimationFrame(this.animId); }

  // ── Resume overlay ────────────────────────────────────────────────────────
  openResume()  { this.resumeClosing.set(false); this.showResume.set(true); }

  closeResume() {
    this.resumeClosing.set(true);
    setTimeout(() => { this.showResume.set(false); this.resumeClosing.set(false); }, 440);
  }

  printResume() {
    const style = document.createElement('style');
    style.id = '__resume-print-style';
    style.textContent = `
      @media print {
        body > * { display: none !important; }
        body > app-root { display: block !important; height: auto !important; overflow: visible !important; }
        body { overflow: visible !important; height: auto !important; }
        app-root .resume-overlay {
          display: flex !important; position: static !important;
          background: #fff !important; color: #111 !important;
          height: auto !important; overflow: visible !important;
          animation: none !important; transform: none !important;
        }
        .resume-close-btn, .resume-actions { display: none !important; }
      }
    `;
    document.head.appendChild(style);
    window.addEventListener('afterprint', () => {
      document.getElementById('__resume-print-style')?.remove();
    }, { once: true });
    setTimeout(() => window.print(), 60);
  }

  // ── Cert carousel ─────────────────────────────────────────────────────────
  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  toggleSkills() { this.skillsExpanded.update(v => !v); }

  // ── 3D Sphere ─────────────────────────────────────────────────────────────
  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  private initSphere() {
    const RADIUS = 88;
    const items = this.sphereTags.map((tag, i) => {
      const phi   = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el    = document.createElement('span');
      el.className   = 'sphere-tag';
      el.textContent = tag.text;
      el.style.cssText = `color:${tag.color};position:absolute;background:var(--surf);padding:4px 8px;border-radius:4px;border:1px solid var(--bord);cursor:grab;user-select:none;font-family:'JetBrains Mono',monospace;font-size:11px;white-space:nowrap;`;
      this.sphereContainer?.nativeElement.appendChild(el);
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);
      items.forEach(({ el, phi, theta }) => {
        const x = Math.sin(phi)*Math.cos(theta), y = Math.cos(phi), z = Math.sin(phi)*Math.sin(theta);
        const x2 = x*cosRY - z*sinRY, z2 = x*sinRY + z*cosRY;
        const y3 = y*cosRX - z2*sinRX, z3 = y*sinRX + z2*cosRX;
        const scale = (z3 + 2.2) / 3.2;
        el.style.transform   = `translate(-50%,-50%) translate(${x2*RADIUS}px,${y3*RADIUS}px)`;
        el.style.opacity     = Math.max(0.1, scale).toString();
        el.style.zIndex      = Math.round(scale*20).toString();
        el.style.borderColor = z3 > 0 ? 'rgba(167,139,250,.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) { this.rotY += this.velY; this.rotX += this.velX; this.velX *= 0.97; }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  /* height is set dynamically by JS to match scrollHeight */
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

/* ══════════ PHOTO — bigger, centered, decorative rings ══════════ */
.h-photo {
  width: 280px;
  height: 280px;
  flex-shrink: 0;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Outer decorative ring */
.h-photo::before {
  content: '';
  position: absolute;
  inset: -12px;
  border-radius: 50%;
  border: 1px dashed var(--bord);
  opacity: .5;
  animation: spinSlow 30s linear infinite;
}

/* Inner decorative ring */
.h-photo::after {
  content: '';
  position: absolute;
  inset: -6px;
  border-radius: 50%;
  border: 1px solid rgba(139,92,246,.2);
}

@keyframes spinSlow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px solid var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
  z-index: 1;
}
.h-photo-placeholder:hover { border-color: var(--acc); }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb { height: 120px; }

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-home { padding: 7rem 1.5rem 4rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: center; gap: 2.5rem; margin-bottom: 4rem; }
  .h-photo { width: 200px; height: 200px; }
  .feat-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-ghost" download>Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]"></div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
:host { display: block; min-height: 100vh; width: 100%; }

.page-resume { width: 100%; min-height: 100vh; padding: 5rem 5rem 3rem; background: var(--bg); overflow-y: auto; }
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* ── Animations ── */
.active-page .au.d0  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .au.d1  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .au.d2  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .au.d3  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .26s ease both; }
.active-page .au.d4  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .34s ease both; }
.active-page .au.d5  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .42s ease both; }
.active-page .au.d6  { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .50s ease both; }

/* ══════════════════════════════════════════
   RESUME HEADER
   ══════════════════════════════════════════ */
.res-header {
  display: flex; justify-content: space-between; align-items: flex-start;
  padding: 2.5rem 3rem; margin-bottom: 3.5rem;
  border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-header-left { flex: 1; }
.res-name {
  font-family: 'Cormorant Garamond', serif; font-weight: 700;
  font-size: clamp(2.2rem, 3.5vw, 3rem); line-height: 1.1;
  letter-spacing: -.02em; color: var(--white); margin-bottom: .5rem;
}
.res-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.1rem;
  color: var(--acc); font-weight: 500; margin-bottom: .3rem;
}
.res-location {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}
.res-header-right {
  display: flex; flex-direction: column; gap: .65rem; align-items: flex-end;
}
.res-contact-link {
  display: flex; align-items: center; gap: .6rem;
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--text); text-decoration: none; transition: color .2s; letter-spacing: .02em;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg { width: 16px; height: 16px; fill: currentColor; flex-shrink: 0; }
.res-contact-link svg[fill="none"] { fill: none; }

/* ══════════════════════════════════════════
   SECTIONS
   ══════════════════════════════════════════ */
.res-section { margin-bottom: 3.5rem; }
.res-section-title {
  font-family: 'JetBrains Mono', monospace; font-size: .85rem;
  letter-spacing: .14em; text-transform: uppercase; font-weight: 700;
  color: var(--white); display: flex; align-items: center; gap: .75rem;
}
.res-cert-count {
  font-size: .7rem; background: var(--acc); color: #fff;
  padding: .15rem .5rem; border-radius: 10px; font-weight: 600; letter-spacing: .05em;
}
.res-divider { height: 2px; background: linear-gradient(90deg, var(--acc), transparent); margin: 1rem 0 2rem; border-radius: 2px; }

/* ── Entry layout (experience / education) ── */
.res-entry {
  display: grid; grid-template-columns: 200px 1fr; gap: 2.5rem;
  padding: 2rem 0; border-bottom: 1px solid var(--bord);
}
.res-entry:last-child { border-bottom: none; }

.res-entry-side { display: flex; flex-direction: column; gap: .3rem; padding-top: .2rem; }
.res-entry-period {
  font-family: 'JetBrains Mono', monospace; font-size: .78rem;
  color: var(--acc); font-weight: 600; letter-spacing: .03em;
}
.res-entry-type {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  color: var(--muted); letter-spacing: .03em;
}

.res-entry-main { }
.res-entry-header { margin-bottom: 1rem; }
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); letter-spacing: -.01em; margin-bottom: .2rem;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--muted); letter-spacing: .04em;
}

.res-bullets {
  list-style: none; padding: 0; display: flex; flex-direction: column; gap: .8rem; margin-bottom: 1.25rem;
}
.res-bullets li {
  font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300;
  padding-left: 1.5rem; position: relative;
}
.res-bullets li::before {
  content: '▸'; position: absolute; left: 0; color: var(--acc); font-size: .85rem; top: .15rem;
}
.res-bullets li :is(strong, b) { color: var(--white); font-weight: 600; }

.res-edu-detail { font-size: 1rem; color: var(--text); line-height: 1.7; font-weight: 300; }

.res-tags { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tag {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .04em;
  padding: .3rem .65rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tag:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   SKILLS — two columns
   ══════════════════════════════════════════ */
.res-skills-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 3rem; }

/* Left: bars */
.res-skills-bars { display: flex; flex-direction: column; gap: 1.2rem; }
.sk-row { display: flex; flex-direction: column; gap: .4rem; }
.sk-info { display: flex; justify-content: space-between; align-items: center; }
.sk-name { font-size: .95rem; font-weight: 500; color: var(--text); }
.sk-meta { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .03em; }
.sk-bar { width: 100%; height: 6px; background: var(--surf); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }
.sk-fill {
  height: 100%; background: var(--acc); border-radius: 4px;
  width: 0%; transition: width 1.2s cubic-bezier(0.22, 1, 0.36, 1);
}

/* Right: tools chips */
.res-tools { }
.res-tools-label {
  font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .1em;
  text-transform: uppercase; color: var(--muted); font-weight: 600; margin-bottom: 1rem;
}
.res-tools-grid { display: flex; flex-wrap: wrap; gap: .5rem; }
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem; letter-spacing: .03em;
  padding: .4rem .75rem; border: 1px solid var(--bord); border-radius: 4px;
  color: var(--text); background: var(--surf); transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS — FULL GRID
   ══════════════════════════════════════════ */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1rem;
}

.cert-card {
  display: flex; overflow: hidden;
  border: 1px solid var(--bord); border-radius: 8px;
  text-decoration: none; transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--surf);
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}
.cert-card:hover {
  border-color: var(--acc);
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(139,92,246,.12);
}

.cert-card-accent { width: 4px; flex-shrink: 0; }
.cert-card-body { padding: 1.25rem 1.5rem; flex: 1; display: flex; flex-direction: column; gap: .65rem; }

.cert-card-issuer { display: flex; align-items: center; gap: .65rem; }
.cert-badge {
  width: 28px; height: 28px; border-radius: 5px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .72rem;
  letter-spacing: .04em; color: var(--muted); font-weight: 500;
}

.cert-card-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: 1.05rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3;
}

.cert-card-footer { display: flex; align-items: center; justify-content: space-between; margin-top: auto; }
.cert-card-date { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); letter-spacing: .05em; }
.cert-card-view { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--acc); letter-spacing: .05em; font-weight: 500; }

/* ══════════════════════════════════════════
   DOWNLOAD CTA
   ══════════════════════════════════════════ */
.res-download { margin-top: 2rem; }
.res-download-inner {
  display: flex; align-items: center; justify-content: space-between;
  padding: 2.5rem 3rem; border: 1px solid var(--bord); border-radius: 10px;
  background: var(--surf);
}
.res-download-text h4 {
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1.3rem;
  font-weight: 700; color: var(--white); margin-bottom: .3rem;
}
.res-download-text p {
  font-size: .95rem; color: var(--muted); font-weight: 300;
}

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s, box-shadow .2s;
  text-decoration: none; border: none; white-space: nowrap;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════════════════════════════════════
   RESPONSIVE
   ══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume { padding: 6.5rem 1.5rem 4rem; }
  .res-header { flex-direction: column; gap: 1.5rem; padding: 2rem 1.5rem; }
  .res-header-right { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: 1rem; }
  .res-skills-grid { grid-template-columns: 1fr; gap: 2rem; }
  .cert-grid { grid-template-columns: 1fr; }
  .res-download-inner { flex-direction: column; gap: 1.5rem; text-align: center; padding: 2rem 1.5rem; }
  .btn, .cert-card, .res-tag, .res-tool-chip { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="pin">

    <!-- SECTION LABEL -->
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ resume</span>
      <div class="shr"></div>
    </div>

    <!-- ═══════════ RESUME HEADER ═══════════ -->
    <div class="res-header au d1">
      <div class="res-header-left">
        <h2 class="res-name">Jaren Kendrick Yambao</h2>
        <p class="res-title">Full Stack Developer · Game/App Developer</p>
        <p class="res-location">Pampanga, Philippines</p>
      </div>
      <div class="res-header-right">
        <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
          jarenkendrickyambao&#64;gmail.com
        </a>
        <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
          linkedin.com/in/jarenkendrick
        </a>
        <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
          <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
          github.com/jarenkendrick14
        </a>
      </div>
    </div>

    <!-- ═══════════ EXPERIENCE ═══════════ -->
    <div class="res-section au d2">
      <h3 class="res-section-title">Experience</h3>
      <div class="res-divider"></div>
      @for (job of experience; track job.company) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ job.period }}</span>
            <span class="res-entry-type">{{ job.type }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ job.title }}</h4>
              <span class="res-entry-company">{{ job.company }}</span>
            </div>
            <ul class="res-bullets">
              @for (bullet of job.bullets; track $index) {
                <li [innerHTML]="bullet"></li>
              }
            </ul>
            <div class="res-tags">
              @for (tag of job.tags; track tag) {
                <span class="res-tag">{{ tag }}</span>
              }
            </div>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ EDUCATION ═══════════ -->
    <div class="res-section au d3">
      <h3 class="res-section-title">Education</h3>
      <div class="res-divider"></div>
      @for (edu of education; track edu.school) {
        <div class="res-entry">
          <div class="res-entry-side">
            <span class="res-entry-period">{{ edu.period }}</span>
            <span class="res-entry-type">{{ edu.location }}</span>
          </div>
          <div class="res-entry-main">
            <div class="res-entry-header">
              <h4 class="res-entry-title">{{ edu.degree }}</h4>
              <span class="res-entry-company">{{ edu.school }}</span>
            </div>
            <p class="res-edu-detail">{{ edu.detail }}</p>
          </div>
        </div>
      }
    </div>

    <!-- ═══════════ SKILLS — two columns ═══════════ -->
    <div class="res-section au d4">
      <h3 class="res-section-title">Technical Skills</h3>
      <div class="res-divider"></div>
      <div class="res-skills-grid">
        <!-- LEFT: Proficiency bars -->
        <div class="res-skills-bars">
          @for (sk of skills; track sk.name; let i = $index) {
            <div class="sk-row">
              <div class="sk-info">
                <span class="sk-name">{{ sk.name }}</span>
                <span class="sk-meta">{{ sk.time }} · {{ sk.pct }}%</span>
              </div>
              <div class="sk-bar">
                <div class="sk-fill"
                  [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                  [style.transition-delay]="(i * 0.08) + 's'">
                </div>
              </div>
            </div>
          }
        </div>
        <!-- RIGHT: Tools & technologies -->
        <div class="res-tools">
          <div class="res-tools-label">Tools & Technologies</div>
          <div class="res-tools-grid">
            @for (tool of toolsList; track tool) {
              <span class="res-tool-chip">{{ tool }}</span>
            }
          </div>
        </div>
      </div>
    </div>

    <!-- ═══════════ CERTIFICATIONS — FULL GRID ═══════════ -->
    <div class="res-section au d5">
      <h3 class="res-section-title">Certifications<span class="res-cert-count">{{ data.certificates.length }}</span></h3>
      <div class="res-divider"></div>
      <div class="cert-grid">
        @for (cert of data.certificates; track cert.name; let i = $index) {
          <a class="cert-card" [href]="cert.url" target="_blank" rel="noopener"
             [style.animation-delay]="(i * 0.04) + 's'">
            <div class="cert-card-accent" [style.background]="cert.color"></div>
            <div class="cert-card-body">
              <div class="cert-card-issuer">
                <div class="cert-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                <span class="cert-issuer-name">{{ cert.issuer }}</span>
              </div>
              <div class="cert-card-name">{{ cert.name }}</div>
              <div class="cert-card-footer">
                <span class="cert-card-date">{{ cert.date }}</span>
                <span class="cert-card-view">View ↗</span>
              </div>
            </div>
          </a>
        }
      </div>
    </div>

    <!-- ═══════════ DOWNLOAD BUTTON ═══════════ -->
    <div class="res-download au d6">
      <div class="res-download-inner">
        <div class="res-download-text">
          <h4>Want a copy?</h4>
          <p>Download the full PDF version of my resume.</p>
        </div>
        <a href="Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
          <svg style="width:18px;height:18px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
          Download Resume (PDF)
        </a>
      </div>
    </div>

  </div>
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong>, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular 21 + Ionic 8 + Node.js + PostgreSQL) with WebSocket messaging and streaming xAI Grok responses.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Collaborate asynchronously with remote team across multiple time zones, managing own sprint tasks and code reviews.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking fundamentals, and deployment workflows. Maintaining strong academic standing while working part-time as a developer.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* Resume should fill exact viewport with no internal scroll */
:host ::ng-deep .page-resume {
  position: static !important;
  height: 100% !important;
  min-height: unset !important;
  overflow: hidden !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }

/* FIX: was [data-theme="light"] .tknob — Angular scoping breaks global selectors.
   :host-context() pierces through to the document root correctly. */
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px;
  }

  /* Resume back to scrollable on mobile */
  :host ::ng-deep .page-resume {
    height: auto !important;
    overflow: visible !important;
    min-height: 100vh !important;
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host { display: block; min-height: 100vh; width: 100%; }

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 4rem 5rem 5rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 2.5rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* Animations */
.active-page .au.d0 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .04s both; }
.active-page .au.d1 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .au.d2 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .au.d3 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .26s both; }
.active-page .al.d1 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .al.d2 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d2 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d3 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s both; }

@keyframes fadeUp    { from { opacity:0; transform:translateY(36px); } to { opacity:1; transform:none; } }
@keyframes fadeLeft  { from { opacity:0; transform:translateX(-36px); } to { opacity:1; transform:none; } }
@keyframes fadeRight { from { opacity:0; transform:translateX(36px); } to { opacity:1; transform:none; } }

/* ═══════════════════════════════════════════════════════════
   MAIN TWO-COLUMN GRID
   ═══════════════════════════════════════════════════════════ */
.ag {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

/* ═══════════════════════════════════════════════════════════
   BIO — PHOTO + BADGES
   ═══════════════════════════════════════════════════════════ */
.bio-header { margin-bottom: 1.75rem; }

.bio-photo-wrap {
  display: flex;
  align-items: flex-end;
  gap: 1rem;
  margin-bottom: 1rem;
}

.bio-photo {
  width: 72px; height: 72px;
  border-radius: 50%;
  border: 2px solid var(--bord);
  overflow: hidden;
  flex-shrink: 0;
  transition: border-color .3s;
  position: relative;
}
.bio-photo:hover { border-color: var(--acc); }
.bio-photo img { width: 100%; height: 100%; object-fit: cover; }
.bio-photo-fallback {
  display: none;
  width: 100%; height: 100%;
  align-items: center; justify-content: center;
  background: var(--surf);
  color: var(--muted);
}

.bio-badges { display: flex; flex-wrap: wrap; gap: .4rem; }

.bio-badge {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--muted);
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 4px;
  padding: .3rem .65rem;
  letter-spacing: .03em;
}
.bio-badge.accent { color: var(--acc); border-color: rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   BIO TEXT
   ═══════════════════════════════════════════════════════════ */
.apull {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.6rem, 2.4vw, 2.2rem);
  font-weight: 600;
  line-height: 1.25;
  color: var(--white);
  letter-spacing: -.02em;
  margin-bottom: 1.25rem;
}
.apull em { color: var(--acc); font-style: italic; }

.abio { display: flex; flex-direction: column; gap: .9rem; margin-bottom: 1.5rem; }
.abio p { font-size: 1rem; color: var(--text); line-height: 1.75; font-weight: 300; }
.abio strong { color: var(--white); font-weight: 600; }

.bio-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: .25rem; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .75rem 1.5rem;
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border-radius: 6px; border: none; cursor: pointer;
  text-decoration: none; transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 24px rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   GITHUB ACTIVITY GRID
   FIX: cell classes are gh-0..gh-4 (match getCellClass() in TS)
   ═══════════════════════════════════════════════════════════ */
.gh-section {
  margin-top: 2rem;
}

.gh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .85rem;
}

.gh-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .12em;
  color: var(--muted);
  font-weight: 600;
  text-transform: uppercase;
}

.gh-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 600;
}

.gh-wrap {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 8px;
  padding: 1rem 1.25rem;
  overflow-x: auto;
}

/* Month labels row */
.gh-months {
  display: flex;
  margin-bottom: .35rem;
}

.gh-row-spacer { width: 28px; flex-shrink: 0; }

.gh-month-labels {
  display: grid;
  /* 26 weeks × 13px + gaps */
  grid-template-columns: repeat(26, 13px);
  gap: 2px;
  flex: 1;
}

.gh-month-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

/* Grid + day labels */
.gh-grid-wrap {
  display: flex;
  gap: 4px;
}

.gh-day-labels {
  display: flex;
  flex-direction: column;
  gap: 2px;
  width: 24px;
  flex-shrink: 0;
}

.gh-day-labels span {
  height: 13px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 13px;
  text-align: right;
  padding-right: 2px;
}

.gh-grid {
  display: flex;
  gap: 2px;
}

.gh-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

/* ─── Cells ─── */
.gh-cell {
  width: 13px; height: 13px;
  border-radius: 2px;
  background: var(--surf);
  border: 1px solid var(--bord);
  transition: transform .15s;
}
.gh-cell:hover { transform: scale(1.4); z-index: 1; position: relative; }

/* ✅ FIX: match class names returned by getCellClass() in TS */
.gh-cell.gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-cell.gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-cell.gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-cell.gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

/* Light mode */
[data-theme="light"] .gh-cell        { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-cell.gh-1   { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-cell.gh-2   { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-cell.gh-3   { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-cell.gh-4   { background: #5b21b6; border-color: #4c1d95; }

/* Legend row */
.gh-legend {
  display: flex;
  align-items: center;
  gap: 3px;
  margin-top: .75rem;
  justify-content: flex-end;
}

.gh-legend-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  margin: 0 2px;
}

/* Legend static cells */
.gh-legend .gh-0 { background: var(--surf); }
.gh-legend .gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-legend .gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-legend .gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-legend .gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

[data-theme="light"] .gh-legend .gh-0 { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-legend .gh-1 { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-legend .gh-2 { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-legend .gh-3 { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-legend .gh-4 { background: #5b21b6; border-color: #4c1d95; }

/* ═══════════════════════════════════════════════════════════
   SKILLS BLOCK
   ═══════════════════════════════════════════════════════════ */
.blk {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 10px;
  padding: 1.75rem;
  margin-bottom: 1.25rem;
}

.blkh {
  font-family: 'JetBrains Mono', monospace;
  font-size: .78rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--white);
  font-weight: 700;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  color: var(--acc);
  background: transparent;
  border: 1px solid rgba(139,92,246,.4);
  border-radius: 4px;
  padding: .25rem .6rem;
  cursor: pointer;
  transition: background .2s, color .2s;
  flex-shrink: 0;
  white-space: nowrap;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.15); }

/* Sphere */
.skill-sphere-wrap {
  width: 100%; height: 210px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.sphere-hint {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .08em;
  pointer-events: none;
}

/* Skill bars */
.skl { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .9rem; }

.sk-item {
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}

.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .3rem;
}

.sk-info span:first-child { font-size: .88rem; color: var(--text); font-weight: 500; }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); }

.sk-bar {
  width: 100%; height: 5px;
  background: var(--bg);
  border-radius: 4px;
  overflow: hidden;
  border: 1px solid var(--bord);
}

.sk-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--acc), rgba(167,139,250,.7));
  border-radius: 4px;
  width: 0;
  animation: barFill 1.2s cubic-bezier(.22,1,.36,1) forwards;
}

@keyframes barFill {
  from { width: 0; }
  to   { width: var(--target-width, 0%); }
}

/* ═══════════════════════════════════════════════════════════
   CERTIFICATIONS GRID
   ═══════════════════════════════════════════════════════════ */
.cert-count-badge {
  font-size: .62rem;
  background: var(--acc);
  color: #fff;
  padding: .1rem .45rem;
  border-radius: 10px;
  font-weight: 700;
  letter-spacing: .05em;
}

.cert-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: .65rem;
  max-height: 520px;
  overflow-y: auto;
  padding-right: 2px;
}
.cert-grid::-webkit-scrollbar { width: 4px; }
.cert-grid::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.cert-card-img {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  overflow: hidden;
  text-decoration: none;
  transition: border-color .25s, transform .2s, box-shadow .25s;
  background: var(--bg);
  cursor: pointer;
}
.cert-card-img:hover {
  border-color: var(--acc);
  transform: translateY(-2px);
  box-shadow: 0 6px 18px rgba(139,92,246,.12);
}

.cci-header {
  display: flex;
  align-items: center;
  gap: .55rem;
  padding: .55rem .75rem;
  border-bottom: 1px solid var(--bord);
}

.cci-badge {
  width: 22px; height: 22px;
  border-radius: 3px;
  display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace;
  font-size: .5rem; font-weight: 700; color: #fff;
  flex-shrink: 0;
}

.cci-issuer {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  letter-spacing: .03em;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.cci-body { padding: .6rem .75rem; flex: 1; }

.cci-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .78rem;
  font-weight: 600;
  color: var(--white);
  line-height: 1.3;
  margin-bottom: .4rem;
}

.cci-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: .4rem;
}

.cci-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  color: var(--muted);
}

.cci-view {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  color: var(--acc);
  white-space: nowrap;
}

/* ═══════════════════════════════════════════════════════════
   RESUME OVERLAY
   ═══════════════════════════════════════════════════════════ */
.resume-overlay {
  position: fixed;
  inset: 0;
  z-index: 8000;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  animation: slideUp .44s cubic-bezier(.22,1,.36,1) both;
}

.resume-overlay.resume-closing {
  animation: slideDown .44s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideUp   { from { transform: translateY(100%); opacity:0; } to { transform: none; opacity:1; } }
@keyframes slideDown { from { transform: none; opacity:1; } to { transform: translateY(100%); opacity:0; } }

/* Header bar */
.resume-hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.1rem 2rem;
  border-bottom: 1px solid var(--bord);
  background: var(--surf);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

.resume-hd-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--muted);
  letter-spacing: .08em;
  font-weight: 600;
  text-transform: uppercase;
}

.resume-hd-right { display: flex; align-items: center; gap: 1rem; }

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .5rem 1.1rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  background: var(--acc);
  color: #fff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s, box-shadow .2s;
}
.resume-actions:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139,92,246,.35); }

.resume-close-btn {
  width: 36px; height: 36px;
  border: 1px solid var(--bord);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  color: var(--muted);
  font-size: 1rem;
  background: transparent;
  cursor: pointer;
  transition: border-color .2s, color .2s;
}
.resume-close-btn:hover { border-color: var(--acc); color: var(--acc); }

/* Scrollable body */
.resume-body {
  flex: 1;
  overflow-y: auto;
  display: flex;
  justify-content: center;
  padding: 3rem 2rem 4rem;
  background: var(--bg);
}

/* White paper resume card */
.resume-page {
  width: 100%;
  max-width: 860px;
  background: #fff;
  color: #1a1a2e;
  border-radius: 4px;
  padding: 3rem 3.5rem;
  box-shadow: 0 4px 60px rgba(0,0,0,.25);
  font-family: 'Plus Jakarta Sans', sans-serif;
}

/* Resume internals */
.res-name-block { margin-bottom: 2rem; border-bottom: 2px solid #1a1a2e; padding-bottom: 1.25rem; }
.res-name { font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; font-weight: 700; color: #1a1a2e; margin-bottom: .25rem; letter-spacing: -.02em; }
.res-role { font-size: 1.05rem; color: #5c5c7a; font-weight: 500; margin-bottom: .5rem; }
.res-contact { display: flex; flex-wrap: wrap; gap: 1rem; font-size: .8rem; color: #5c5c7a; }
.res-contact a { color: #5b21b6; text-decoration: none; }
.res-contact a:hover { text-decoration: underline; }

.res-cols { display: grid; grid-template-columns: 1.4fr .9fr; gap: 3rem; }

.resume-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: #5b21b6;
  margin-bottom: .65rem;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: .35rem;
}

.res-section { margin-bottom: 1.75rem; }
.res-text { font-size: .9rem; color: #4b5563; line-height: 1.7; font-weight: 400; }

.res-exp-item { margin-bottom: 1.25rem; }
.res-exp-header { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .2rem; }
.res-exp-title { font-weight: 700; font-size: .92rem; color: #1a1a2e; }
.res-exp-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: #9ca3af; flex-shrink: 0; }
.res-exp-org { font-size: .8rem; color: #6b7280; margin-bottom: .5rem; }
.res-exp-list { padding-left: 1rem; margin: 0; display: flex; flex-direction: column; gap: .3rem; }
.res-exp-list li { font-size: .85rem; color: #4b5563; line-height: 1.6; }

.res-proj-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px dashed #e5e7eb; }
.res-proj-item:last-child { border-bottom: none; }
.res-proj-header { display: flex; justify-content: space-between; align-items: center; gap: .5rem; margin-bottom: .25rem; }
.res-proj-name { font-weight: 700; font-size: .9rem; color: #1a1a2e; }
.res-proj-tags { display: flex; gap: .3rem; flex-wrap: wrap; }
.res-tag { font-family: 'JetBrains Mono', monospace; font-size: .6rem; padding: .15rem .4rem; border-radius: 3px; background: #ede9fe; color: #5b21b6; }
.res-proj-desc { font-size: .82rem; color: #6b7280; line-height: 1.55; }

.res-skill-chips { display: flex; flex-wrap: wrap; gap: .4rem; }
.res-chip { font-family: 'JetBrains Mono', monospace; font-size: .68rem; padding: .25rem .55rem; background: #f3f4f6; border: 1px solid #e5e7eb; border-radius: 4px; color: #374151; }

.res-cert-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
.res-cert-item { display: flex; align-items: center; gap: .55rem; font-size: .82rem; }
.res-cert-issuer { font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700; padding: .15rem .4rem; border-radius: 3px; flex-shrink: 0; }
.res-cert-name { flex: 1; color: #1a1a2e; font-weight: 500; font-size: .82rem; }
.res-cert-date { font-family: 'JetBrains Mono', monospace; font-size: .62rem; color: #9ca3af; flex-shrink: 0; }
.res-cert-more { font-style: italic; color: #9ca3af; font-size: .8rem; }
.res-cert-count { font-size: .68rem; background: #5b21b6; color: #fff; padding: .1rem .4rem; border-radius: 8px; }

/* Print */
@media print {
  .resume-hd, app-root > * { display: none !important; }
  .resume-overlay { position: static !important; animation: none !important; }
  .resume-body { padding: 0; overflow: visible; }
  .resume-page { box-shadow: none; border-radius: 0; padding: 2rem; }
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-about { padding: 5.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 2.5rem; }
  .cert-grid { grid-template-columns: 1fr 1fr; max-height: none; }
  .res-cols { grid-template-columns: 1fr; }
  .resume-page { padding: 2rem 1.5rem; }
  .resume-hd { padding: 1rem 1.25rem; }
  .skill-toggle-btn { cursor: pointer !important; }
}

@media (max-width: 480px) {
  .cert-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            <span class="gh-count">{{ githubTotal }} CONTRIBUTIONS</span>
          </div>

          <div class="gh-wrap">
            <!-- Month labels -->
            <div class="gh-months">
              <div class="gh-row-spacer"></div>
              <div class="gh-month-labels">
                @for (m of monthLabels; track m.col) {
                  <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                }
              </div>
            </div>

            <!-- Day labels + grid -->
            <div class="gh-grid-wrap">
              <div class="gh-day-labels">
                <span></span>
                <span>Mon</span>
                <span></span>
                <span>Wed</span>
                <span></span>
                <span>Fri</span>
                <span></span>
              </div>
              <div class="gh-grid">
                @for (week of githubWeeks; track $index) {
                  <div class="gh-col">
                    @for (day of week; track $index) {
                      <div class="gh-cell" [class]="getCellClass(day)"
                           [title]="day > 0 ? day + ' contribution' + (day > 1 ? 's' : '') : 'No contributions'">
                      </div>
                    }
                  </div>
                }
              </div>
            </div>

            <!-- Legend -->
            <div class="gh-legend">
              <span class="gh-legend-lbl">Less</span>
              <div class="gh-cell gh-0"></div>
              <div class="gh-cell gh-1"></div>
              <div class="gh-cell gh-2"></div>
              <div class="gh-cell gh-3"></div>
              <div class="gh-cell gh-4"></div>
              <span class="gh-legend-lbl">More</span>
            </div>
          </div>
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars (remount on every visit to retrigger CSS animation) -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — image grid -->
        <div class="blk ar d3">
          <div class="blkh">Certifications <span class="cert-count-badge">{{ data.certificates.length }}</span></div>
          <div class="cert-grid">
            @for (cert of data.certificates; track cert.name) {
              <a class="cert-card-img" [href]="cert.url" target="_blank" rel="noopener"
                 [title]="cert.name + ' — ' + cert.issuer">
                <div class="cci-header" [style.background]="cert.color + '22'" [style.border-color]="cert.color + '44'">
                  <div class="cci-badge" [style.background]="cert.color">{{ cert.abbr }}</div>
                  <span class="cci-issuer">{{ cert.issuer }}</span>
                </div>
                <div class="cci-body">
                  <div class="cci-name">{{ cert.name }}</div>
                  <div class="cci-footer">
                    <span class="cci-date">{{ cert.date }}</span>
                    <span class="cci-view">View ↗</span>
                  </div>
                </div>
              </a>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: src\app\components\about\about.component.ts ---
import {
  Component, inject, signal, ElementRef, ViewChild,
  AfterViewInit, OnDestroy, OnChanges, Input, SimpleChanges
} from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy, OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap')      sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex      = signal(0);
  readonly certAnimClass  = signal('');
  readonly barsReady      = signal(true);
  readonly showResume     = signal(false);
  readonly resumeClosing  = signal(false);

  // ── GitHub activity — 26 weeks so it fits nicely in the left column ────────
  readonly githubWeeks: number[][] = this.generateGithubData();
  readonly githubTotal = 84;
  readonly monthLabels = this.buildMonthLabels();

  /** Generate a deterministic-looking contribution grid (26 weeks × 7 days). */
  private generateGithubData(): number[][] {
    const WEEKS = 26;
    const grid: number[][] = Array.from({ length: WEEKS }, () => new Array(7).fill(0));
    let seed = 137;
    const rng = () => { seed = (seed * 1664525 + 1013904223) & 0x7fffffff; return seed / 0x7fffffff; };
    let placed = 0;
    const target = 84;

    for (let w = 0; w < WEEKS && placed < target; w++) {
      const recency  = w / WEEKS;
      const weekProb = 0.08 + recency * 0.55;            // ramp up towards recent weeks
      for (let d = 0; d < 7 && placed < target; d++) {
        const isWeekend = d === 0 || d === 6;
        const prob = isWeekend ? weekProb * 0.35 : weekProb;
        if (rng() < prob) {
          const r = rng();
          const intensity = r < 0.50 ? 1 : r < 0.78 ? 2 : r < 0.92 ? 3 : 4;
          const actual    = Math.min(intensity, target - placed);
          if (actual > 0) { grid[w][d] = actual; placed += actual; }
        }
      }
    }
    return grid;
  }

  /** Build month labels anchored to the last 26 weeks. */
  private buildMonthLabels(): { label: string; col: number }[] {
    const names = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    const WEEKS = 26;
    const now   = new Date();
    const out: { label: string; col: number }[] = [];
    let last = -1;
    for (let w = 0; w < WEEKS; w++) {
      const d = new Date(now);
      d.setDate(d.getDate() - (WEEKS - 1 - w) * 7);
      const m = d.getMonth();
      if (m !== last) { out.push({ label: names[m], col: w }); last = m; }
    }
    return out;
  }

  /** Returns CSS class for a cell value — must match .gh-cell.gh-N in CSS. */
  getCellClass(v: number): string {
    if (v <= 0) return 'gh-0';
    if (v === 1) return 'gh-1';
    if (v === 2) return 'gh-2';
    if (v === 3) return 'gh-3';
    return 'gh-4';
  }

  // ── Skills ────────────────────────────────────────────────────────────────
  readonly skills = [
    { name: 'JavaScript / TypeScript', time: '2 yrs', pct: 85 },
    { name: 'C# (Backend Logic)',       time: '2 yrs', pct: 80 },
    { name: 'Angular',                  time: '1 yr',  pct: 75 },
    { name: 'Node.js & Express',        time: '1 yr',  pct: 75 },
    { name: 'Vue.js 3',                 time: '1 yr',  pct: 70 },
    { name: 'MongoDB',                  time: '1 yr',  pct: 65 },
  ];

  readonly sphereTags = [
    { text: 'Angular',    color: '#dd0031' }, { text: 'Vue.js 3',  color: '#42b883' },
    { text: 'Node.js',    color: '#339933' }, { text: 'MongoDB',   color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#',        color: '#9b59b6' },
    { text: 'Unity',      color: '#a78bfa' }, { text: 'Express.js',color: '#a4a1c6' },
    { text: 'REST APIs',  color: '#60a5fa' }, { text: 'Git',       color: '#f05030' },
    { text: 'VR / XR',   color: '#f59e0b' }, { text: 'Netlify',   color: '#00c7b7' },
  ];

  get currentCert(): Certificate { return this.data.certificates[this.certIndex()]; }
  get certTotal(): number        { return this.data.certificates.length; }

  // ── Lifecycle ─────────────────────────────────────────────────────────────
  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active) {
      this.barsReady.set(false);
      setTimeout(() => this.barsReady.set(true), 300);
    }
  }

  ngAfterViewInit() { this.initSphere(); }
  ngOnDestroy()     { cancelAnimationFrame(this.animId); }

  // ── Resume overlay ────────────────────────────────────────────────────────
  openResume()  { this.resumeClosing.set(false); this.showResume.set(true); }

  closeResume() {
    this.resumeClosing.set(true);
    setTimeout(() => { this.showResume.set(false); this.resumeClosing.set(false); }, 440);
  }

  printResume() {
    const style = document.createElement('style');
    style.id = '__resume-print-style';
    style.textContent = `
      @media print {
        body > * { display: none !important; }
        body > app-root { display: block !important; height: auto !important; overflow: visible !important; }
        body { overflow: visible !important; height: auto !important; }
        app-root .resume-overlay {
          display: flex !important; position: static !important;
          background: #fff !important; color: #111 !important;
          height: auto !important; overflow: visible !important;
          animation: none !important; transform: none !important;
        }
        .resume-close-btn, .resume-actions { display: none !important; }
      }
    `;
    document.head.appendChild(style);
    window.addEventListener('afterprint', () => {
      document.getElementById('__resume-print-style')?.remove();
    }, { once: true });
    setTimeout(() => window.print(), 60);
  }

  // ── Cert carousel ─────────────────────────────────────────────────────────
  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  toggleSkills() { this.skillsExpanded.update(v => !v); }

  // ── 3D Sphere ─────────────────────────────────────────────────────────────
  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  private initSphere() {
    const RADIUS = 88;
    const items = this.sphereTags.map((tag, i) => {
      const phi   = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el    = document.createElement('span');
      el.className   = 'sphere-tag';
      el.textContent = tag.text;
      el.style.cssText = `color:${tag.color};position:absolute;background:var(--surf);padding:4px 8px;border-radius:4px;border:1px solid var(--bord);cursor:grab;user-select:none;font-family:'JetBrains Mono',monospace;font-size:11px;white-space:nowrap;`;
      this.sphereContainer?.nativeElement.appendChild(el);
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);
      items.forEach(({ el, phi, theta }) => {
        const x = Math.sin(phi)*Math.cos(theta), y = Math.cos(phi), z = Math.sin(phi)*Math.sin(theta);
        const x2 = x*cosRY - z*sinRY, z2 = x*sinRY + z*cosRY;
        const y3 = y*cosRX - z2*sinRX, z3 = y*sinRX + z2*cosRX;
        const scale = (z3 + 2.2) / 3.2;
        el.style.transform   = `translate(-50%,-50%) translate(${x2*RADIUS}px,${y3*RADIUS}px)`;
        el.style.opacity     = Math.max(0.1, scale).toString();
        el.style.zIndex      = Math.round(scale*20).toString();
        el.style.borderColor = z3 > 0 ? 'rgba(167,139,250,.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) { this.rotY += this.velY; this.rotX += this.velX; this.velX *= 0.97; }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  /* Use 100% instead of min-height:100vh — the page-section wrapper enforces height */
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.h-photo {
  width: 220px;
  height: 220px;
  flex-shrink: 0;
}

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px dashed var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
}
.h-photo-placeholder:hover { border-color: var(--acc); border-style: solid; }
/* Hide fallback icon once real image loads */
.h-photo-placeholder img + .h-photo-icon { display: none; }
.h-photo-placeholder img:not([style*="display: none"]) + .h-photo-icon { display: none; }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb {
  height: 120px;
  overflow: hidden;
  position: relative;
}

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  /* FIX: was padding 7rem 1.5rem 4rem — the big bottom padding caused extra space.
     Reduce to a more modest amount. Use padding-bottom: 2rem. */
  .page-home { padding: 7rem 1.5rem 2rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: flex-start; gap: 2.5rem; margin-bottom: 3rem; }
  .h-photo { width: 160px; height: 160px; }
  .feat-grid { grid-template-columns: 1fr; }
  .btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="btn btn-ghost" download="Jaren_Kendrick_Resume.pdf">Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <img src="pictures/profile.jpg" alt="Jaren Kendrick"
               style="width:100%;height:100%;object-fit:cover;border-radius:50%;position:absolute;inset:0;"
               (error)="$any($event.target).style.display='none'" />
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]">
              <img [src]="'pictures/' + item.project.name.toLowerCase().replace(' ', '-') + '-thumb.jpg'"
                   style="width:100%;height:100%;object-fit:cover;"
                   (error)="$any($event.target).style.display='none'" />
            </div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
/* ══════════════════════════════════════════
   HOST — fills the page section exactly
══════════════════════════════════════════ */
:host {
  display: block;
  width: 100%;
  height: 100%;
}

/* ══════════════════════════════════════════
   PAGE SHELL — no overflow, fills viewport
   Desktop: fixed height so page-slider
   handles scroll. No internal scrolling.
══════════════════════════════════════════ */
.page-resume {
  width: 100%;
  height: 100%;
  padding: 2.5rem 3rem 2rem 3rem;
  background: var(--bg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* ══════════════════════════════════════════
   TWO-COLUMN LAYOUT
══════════════════════════════════════════ */
.res-layout {
  display: grid;
  grid-template-columns: 1.35fr 1fr;
  gap: 3rem;
  height: 100%;
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
  overflow: hidden;
}

.res-left,
.res-right {
  display: flex;
  flex-direction: column;
  gap: 0;
  overflow: hidden;
  min-height: 0;
}

/* ══════════════════════════════════════════
   RESUME HEADER
══════════════════════════════════════════ */
.res-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 2px solid var(--acc);
  margin-bottom: 1.25rem;
  flex-shrink: 0;
}

.res-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.5rem, 2.2vw, 2rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.025em;
  line-height: 1.1;
  margin-bottom: .2rem;
}
.res-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .15rem;
  font-weight: 500;
}
.res-location {
  font-size: .8rem;
  color: var(--muted);
  font-weight: 300;
}

.res-header-contacts {
  display: flex;
  flex-direction: column;
  gap: .35rem;
  align-items: flex-end;
  flex-shrink: 0;
}
.res-contact-link {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-size: .72rem;
  font-family: 'JetBrains Mono', monospace;
  color: var(--muted);
  text-decoration: none;
  letter-spacing: .02em;
  transition: color .2s;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg {
  width: 11px; height: 11px;
  fill: currentColor;
  flex-shrink: 0;
}
.res-contact-link svg[stroke] { fill: none; stroke: currentColor; stroke-width: 2; }

/* Download button — styled differently */
.res-dl-btn {
  display: inline-flex;
  align-items: center;
  gap: .4rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .06em;
  color: #fff;
  background: var(--acc);
  border: none;
  border-radius: 4px;
  padding: .35rem .75rem;
  text-decoration: none;
  margin-top: .25rem;
  cursor: pointer;
  transition: opacity .2s, transform .15s;
}
.res-dl-btn:hover { opacity: .88; transform: translateY(-1px); }
.res-dl-btn svg { width: 11px; height: 11px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

/* ══════════════════════════════════════════
   SECTION LABELS
══════════════════════════════════════════ */
.res-section {
  flex-shrink: 0;
  margin-bottom: 1rem;
}
.res-section:last-child { margin-bottom: 0; }

.res-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .4rem;
}
.res-divider {
  height: 1px;
  background: var(--bord);
  margin-bottom: .75rem;
}

/* ══════════════════════════════════════════
   EXPERIENCE / EDUCATION ENTRIES
══════════════════════════════════════════ */
.res-entry {
  display: grid;
  grid-template-columns: 110px 1fr;
  gap: 1rem;
  margin-bottom: .75rem;
}
.res-entry:last-child { margin-bottom: 0; }

.res-entry-side {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  padding-top: .1rem;
}
.res-entry-period {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--acc);
  letter-spacing: .04em;
  line-height: 1.3;
  font-weight: 600;
}
.res-entry-type {
  font-size: .65rem;
  color: var(--muted);
  font-weight: 300;
  line-height: 1.3;
}

.res-entry-header {
  display: flex;
  align-items: baseline;
  gap: .6rem;
  flex-wrap: wrap;
  margin-bottom: .4rem;
}
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 700;
  font-size: .95rem;
  color: var(--white);
  letter-spacing: -.01em;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  color: var(--acc);
  letter-spacing: .05em;
  font-weight: 500;
}

.res-bullets {
  list-style: none;
  padding: 0;
  margin: 0 0 .5rem;
  display: flex;
  flex-direction: column;
  gap: .25rem;
}
.res-bullets li {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.55;
  font-weight: 300;
  padding-left: .9rem;
  position: relative;
}
.res-bullets li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .62rem;
  top: .15rem;
}
.res-bullets li strong { color: var(--white); font-weight: 600; }

.res-edu-detail {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.6;
  font-weight: 300;
}

.res-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
  margin-top: .35rem;
}
.res-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  padding: .15rem .45rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--muted);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
}

/* ══════════════════════════════════════════
   SKILLS BARS
══════════════════════════════════════════ */
.sk-row { margin-bottom: .45rem; }
.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .2rem;
}
.sk-name {
  font-size: .78rem;
  color: var(--text);
  font-weight: 500;
}
.sk-meta {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
}
.sk-bar {
  height: 4px;
  background: var(--surf);
  border-radius: 3px;
  border: 1px solid var(--bord);
  overflow: hidden;
}
.sk-fill {
  height: 100%;
  background: var(--acc);
  border-radius: 3px;
  transition: width 1s cubic-bezier(.22,1,.36,1);
}

/* Tools chips */
.res-tools-grid {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
}
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
  transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
══════════════════════════════════════════ */
.cert-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .4rem;
}
.cert-counter {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  color: var(--muted);
  letter-spacing: .05em;
}
.cert-carousel {
  position: relative;
  display: flex;
  align-items: center;
  gap: .5rem;
  margin-bottom: .6rem;
}
.cert-nav {
  width: 30px; height: 30px;
  border: 1px solid var(--bord);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--sub); font-size: 1rem; cursor: pointer; flex-shrink: 0;
  transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 1px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap {
  flex: 1;
  overflow: hidden;
}
.cert-in   { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r { animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity:0; transform: translateX(20px);  } to { opacity:1; transform: none; } }
@keyframes certInR { from { opacity:0; transform: translateX(-20px); } to { opacity:1; transform: none; } }

.cert-card {
  display: block;
  border-radius: 7px;
  overflow: hidden;
  border: 1px solid var(--bord);
  transition: border-color .25s;
  text-decoration: none;
  cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top {
  height: 42px;
  display: flex; align-items: center;
  padding: 0 1rem; gap: .6rem;
}
.cert-issuer-badge {
  width: 26px; height: 26px;
  border-radius: 3px; display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700;
  color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  letter-spacing: .04em; color: var(--text); font-weight: 500;
}
.cert-body { background: var(--bg); padding: .85rem 1rem; }
.cert-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: .9rem; color: var(--white); line-height: 1.2; margin-bottom: .35rem;
}
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--muted); }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--acc); font-weight: 500; }

.cert-dots {
  display: flex; gap: .35rem; justify-content: center;
}
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* ══════════════════════════════════════════
   ANIMATIONS
══════════════════════════════════════════ */
@keyframes fadeUp  { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:none; } }

/* ══════════════════════════════════════════
   RESPONSIVE — on mobile, allow normal scroll
══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume {
    overflow: visible;
    height: auto;
    min-height: 100vh;
    padding: 6.5rem 1.5rem 4rem;
  }
  .res-layout {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }
  .res-left, .res-right { overflow: visible; }
  .res-header { flex-direction: column; gap: 1rem; }
  .res-header-contacts { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: .5rem; }
  .cert-nav, .cert-card, .cert-dot, .res-tool-chip, .res-dl-btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="res-layout">

    <!-- ═══════════ LEFT COLUMN ═══════════ -->
    <div class="res-left">

      <!-- HEADER -->
      <div class="res-header au d0">
        <div>
          <h2 class="res-name">Jaren Kendrick Yambao</h2>
          <p class="res-title">Full Stack Developer · Game/App Developer</p>
          <p class="res-location">Pampanga, Philippines</p>
        </div>
        <div class="res-header-contacts">
          <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
            jarenkendrickyambao&#64;gmail.com
          </a>
          <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
            <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
            github.com/jarenkendrick14
          </a>
          <!-- Download: no target="_blank" so browser downloads directly -->
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="res-dl-btn" download="Jaren_Kendrick_Resume.pdf">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Download PDF
          </a>
        </div>
      </div>

      <!-- EXPERIENCE -->
      <div class="res-section au d1">
        <h3 class="res-section-title">Experience</h3>
        <div class="res-divider"></div>
        @for (job of experience; track job.company) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ job.period }}</span>
              <span class="res-entry-type">{{ job.type }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ job.title }}</h4>
                <span class="res-entry-company">{{ job.company }}</span>
              </div>
              <ul class="res-bullets">
                @for (bullet of job.bullets; track $index) {
                  <li [innerHTML]="bullet"></li>
                }
              </ul>
              <div class="res-tags">
                @for (tag of job.tags; track tag) {
                  <span class="res-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>

      <!-- EDUCATION -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Education</h3>
        <div class="res-divider"></div>
        @for (edu of education; track edu.school) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ edu.period }}</span>
              <span class="res-entry-type">{{ edu.location }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ edu.degree }}</h4>
                <span class="res-entry-company">{{ edu.school }}</span>
              </div>
              <p class="res-edu-detail">{{ edu.detail }}</p>
            </div>
          </div>
        }
      </div>

    </div><!-- /res-left -->

    <!-- ═══════════ RIGHT COLUMN ═══════════ -->
    <div class="res-right">

      <!-- SKILLS -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Technical Skills</h3>
        <div class="res-divider"></div>
        @for (sk of skills; track sk.name; let i = $index) {
          <div class="sk-row">
            <div class="sk-info">
              <span class="sk-name">{{ sk.name }}</span>
              <span class="sk-meta">{{ sk.pct }}%</span>
            </div>
            <div class="sk-bar">
              <div class="sk-fill"
                [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                [style.transition-delay]="(i * 0.07) + 's'">
              </div>
            </div>
          </div>
        }

        <!-- Tools row -->
        <div class="res-tools-grid" style="margin-top:.85rem;">
          @for (tool of toolsList; track tool) {
            <span class="res-tool-chip">{{ tool }}</span>
          }
        </div>
      </div>

      <!-- CERTIFICATIONS — CAROUSEL -->
      <div class="res-section au d3">
        <div class="cert-header">
          <h3 class="res-section-title" style="margin-bottom:0;">Certifications</h3>
          <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
        </div>
        <div class="res-divider"></div>
        <div class="cert-carousel">
          <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous cert">‹</button>
          <div class="cert-card-wrap" [ngClass]="certAnimClass()">
            <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
              <div class="cert-top" [style.background]="currentCert.color + '22'">
                <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
              </div>
              <div class="cert-body">
                <div class="cert-name">{{ currentCert.name }}</div>
                <div class="cert-meta">
                  <div class="cert-date">{{ currentCert.date }}</div>
                  <div class="cert-view">View ↗</div>
                </div>
              </div>
            </a>
          </div>
          <button class="cert-nav" (click)="certNav(1)" aria-label="Next cert">›</button>
        </div>
        <!-- Dot indicators -->
        <div class="cert-dots">
          @for (cert of data.certificates; track $index) {
            <button class="cert-dot" [class.on]="$index === certIndex()"
              (click)="certNav($index > certIndex() ? 1 : -1)"
              [attr.aria-label]="'Cert ' + ($index + 1)">
            </button>
          }
        </div>
      </div>

    </div><!-- /res-right -->

  </div><!-- /res-layout -->
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  /* ── Cert Carousel ── */
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  get currentCert() { return this.data.certificates[this.certIndex()]; }
  get certTotal()   { return this.data.certificates.length; }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong> VR app to Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular + Ionic + Node.js + PostgreSQL) with WebSocket + streaming xAI Grok.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Managing own sprint tasks and code reviews across multiple time zones.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking, and deployment workflows. Maintaining strong academic standing while working part-time.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* Resume should fill exact viewport with no internal scroll */
:host ::ng-deep .page-resume {
  position: static !important;
  height: 100% !important;
  min-height: unset !important;
  overflow: hidden !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }

/* FIX: was [data-theme="light"] .tknob — Angular scoping breaks global selectors.
   :host-context() pierces through to the document root correctly. */
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px;
  }

  /* Resume back to scrollable on mobile */
  :host ::ng-deep .page-resume {
    height: auto !important;
    overflow: visible !important;
    min-height: 100vh !important;
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host { display: block; min-height: 100vh; width: 100%; }

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 4rem 5rem 5rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 2.5rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* Animations */
.active-page .au.d0 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .04s both; }
.active-page .au.d1 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .au.d2 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .au.d3 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .26s both; }
.active-page .al.d1 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .al.d2 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d2 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d3 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s both; }

@keyframes fadeUp    { from { opacity:0; transform:translateY(36px); } to { opacity:1; transform:none; } }
@keyframes fadeLeft  { from { opacity:0; transform:translateX(-36px); } to { opacity:1; transform:none; } }
@keyframes fadeRight { from { opacity:0; transform:translateX(36px); } to { opacity:1; transform:none; } }

/* ═══════════════════════════════════════════════════════════
   MAIN TWO-COLUMN GRID
   ═══════════════════════════════════════════════════════════ */
.ag {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

/* ═══════════════════════════════════════════════════════════
   BIO — PHOTO + BADGES
   ═══════════════════════════════════════════════════════════ */
.bio-header { margin-bottom: 1.75rem; }

.bio-photo-wrap {
  display: flex;
  align-items: flex-end;
  gap: 1rem;
  margin-bottom: 1rem;
}

.bio-photo {
  width: 72px; height: 72px;
  border-radius: 50%;
  border: 2px solid var(--bord);
  overflow: hidden;
  flex-shrink: 0;
  transition: border-color .3s;
  position: relative;
}
.bio-photo:hover { border-color: var(--acc); }
.bio-photo img { width: 100%; height: 100%; object-fit: cover; }
.bio-photo-fallback {
  display: none;
  width: 100%; height: 100%;
  align-items: center; justify-content: center;
  background: var(--surf);
  color: var(--muted);
}

.bio-badges { display: flex; flex-wrap: wrap; gap: .4rem; }

.bio-badge {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--muted);
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 4px;
  padding: .3rem .65rem;
  letter-spacing: .03em;
}
.bio-badge.accent { color: var(--acc); border-color: rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   BIO TEXT
   ═══════════════════════════════════════════════════════════ */
.apull {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.6rem, 2.4vw, 2.2rem);
  font-weight: 600;
  line-height: 1.25;
  color: var(--white);
  letter-spacing: -.02em;
  margin-bottom: 1.25rem;
}
.apull em { color: var(--acc); font-style: italic; }

.abio { display: flex; flex-direction: column; gap: .9rem; margin-bottom: 1.5rem; }
.abio p { font-size: 1rem; color: var(--text); line-height: 1.75; font-weight: 300; }
.abio strong { color: var(--white); font-weight: 600; }

.bio-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: .25rem; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .75rem 1.5rem;
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border-radius: 6px; border: none; cursor: pointer;
  text-decoration: none; transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 24px rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   GITHUB ACTIVITY GRID
   FIX: cell classes are gh-0..gh-4 (match getCellClass() in TS)
   ═══════════════════════════════════════════════════════════ */
.gh-section {
  margin-top: 2rem;
}

.gh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .85rem;
}

.gh-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .12em;
  color: var(--muted);
  font-weight: 600;
  text-transform: uppercase;
}

.gh-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 600;
}

.gh-wrap {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 8px;
  padding: 1rem 1.25rem;
  overflow-x: auto;
}

/* Month labels row */
.gh-months {
  display: flex;
  margin-bottom: .35rem;
}

.gh-row-spacer { width: 28px; flex-shrink: 0; }

.gh-month-labels {
  display: grid;
  /* 26 weeks × 13px + gaps */
  grid-template-columns: repeat(26, 13px);
  gap: 2px;
  flex: 1;
}

.gh-month-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

/* Grid + day labels */
.gh-grid-wrap {
  display: flex;
  gap: 4px;
}

.gh-day-labels {
  display: flex;
  flex-direction: column;
  gap: 2px;
  width: 24px;
  flex-shrink: 0;
}

.gh-day-labels span {
  height: 13px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 13px;
  text-align: right;
  padding-right: 2px;
}

.gh-grid {
  display: flex;
  gap: 2px;
}

.gh-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

/* ─── Cells ─── */
.gh-cell {
  width: 13px; height: 13px;
  border-radius: 2px;
  background: var(--surf);
  border: 1px solid var(--bord);
  transition: transform .15s;
}
.gh-cell:hover { transform: scale(1.4); z-index: 1; position: relative; }

/* ✅ FIX: match class names returned by getCellClass() in TS */
.gh-cell.gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-cell.gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-cell.gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-cell.gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

/* Light mode */
[data-theme="light"] .gh-cell        { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-cell.gh-1   { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-cell.gh-2   { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-cell.gh-3   { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-cell.gh-4   { background: #5b21b6; border-color: #4c1d95; }

/* Legend row */
.gh-legend {
  display: flex;
  align-items: center;
  gap: 3px;
  margin-top: .75rem;
  justify-content: flex-end;
}

.gh-legend-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  margin: 0 2px;
}

/* Legend static cells */
.gh-legend .gh-0 { background: var(--surf); }
.gh-legend .gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-legend .gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-legend .gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-legend .gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

[data-theme="light"] .gh-legend .gh-0 { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-legend .gh-1 { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-legend .gh-2 { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-legend .gh-3 { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-legend .gh-4 { background: #5b21b6; border-color: #4c1d95; }

/* ═══════════════════════════════════════════════════════════
   SKILLS BLOCK
   ═══════════════════════════════════════════════════════════ */
.blk {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 10px;
  padding: 1.75rem;
  margin-bottom: 1.25rem;
}

.blkh {
  font-family: 'JetBrains Mono', monospace;
  font-size: .78rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--white);
  font-weight: 700;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  color: var(--acc);
  background: transparent;
  border: 1px solid rgba(139,92,246,.4);
  border-radius: 4px;
  padding: .25rem .6rem;
  cursor: pointer;
  transition: background .2s, color .2s;
  flex-shrink: 0;
  white-space: nowrap;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.15); }

/* Sphere */
.skill-sphere-wrap {
  width: 100%; height: 210px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.sphere-hint {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .08em;
  pointer-events: none;
}

/* Skill bars */
.skl { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .9rem; }

.sk-item {
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}

.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .3rem;
}

.sk-info span:first-child { font-size: .88rem; color: var(--text); font-weight: 500; }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); }

.sk-bar {
  width: 100%; height: 5px;
  background: var(--bg);
  border-radius: 4px;
  overflow: hidden;
  border: 1px solid var(--bord);
}

.sk-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--acc), rgba(167,139,250,.7));
  border-radius: 4px;
  width: 0;
  animation: barFill 1.2s cubic-bezier(.22,1,.36,1) forwards;
}

@keyframes barFill {
  from { width: 0; }
  to   { width: var(--target-width, 0%); }
}

/* ═══════════════════════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
   ═══════════════════════════════════════════════════════════ */
.cert-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
.cert-counter { font-family: 'JetBrains Mono', monospace; font-size: .72rem; color: var(--muted); letter-spacing: .06em; }
.cert-carousel { position: relative; display: flex; align-items: center; gap: .75rem; }
.cert-nav {
  width: 34px; height: 34px; border: 1px solid var(--bord); border-radius: 50%;
  display: flex; align-items: center; justify-content: center; color: var(--sub);
  font-size: 1.2rem; cursor: pointer; flex-shrink: 0; transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 2px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }
.cert-card-wrap { flex: 1; overflow: hidden; }
.cert-in  { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r{ animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity: 0; transform: translateX(30px);  } to { opacity: 1; transform: none; } }
@keyframes certInR { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: none; } }
.cert-card {
  display: block; border-radius: 8px; overflow: hidden; border: 1px solid var(--bord);
  transition: border-color .25s; text-decoration: none; cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top { height: 52px; display: flex; align-items: center; padding: 0 1.1rem; gap: .7rem; }
.cert-issuer-badge {
  width: 28px; height: 28px; border-radius: 4px; display: flex; align-items: center;
  justify-content: center; font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name { font-family: 'JetBrains Mono', monospace; font-size: .72rem; letter-spacing: .04em; color: var(--text); font-weight: 500; }
.cert-body { background: var(--bg); padding: 1.1rem; }
.cert-name { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1rem; letter-spacing: -.01em; color: var(--white); line-height: 1.25; margin-bottom: .45rem; }
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); letter-spacing: .06em; }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--acc); letter-spacing: .06em; font-weight: 500; }
.cert-dots { display: flex; gap: .4rem; justify-content: center; margin-top: .9rem; }
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s, transform .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); transform: scale(1.4); }

/* ═══════════════════════════════════════════════════════════
   GITHUB HEATMAP LOADING / ERROR STATES
   ═══════════════════════════════════════════════════════════ */
.gh-loading {
  display: flex; align-items: center; gap: .6rem;
  font-family: 'JetBrains Mono', monospace; font-size: .75rem;
  color: var(--muted); padding: 1.5rem 0;
}
.gh-pulse {
  width: 8px; height: 8px; border-radius: 50%; background: var(--acc);
  animation: pulse 1.2s ease-in-out infinite;
}
@keyframes pulse { 0%,100% { opacity: .3; transform: scale(.8); } 50% { opacity: 1; transform: scale(1.2); } }
.gh-error {
  padding: 1.5rem 0;
  font-family: 'JetBrains Mono', monospace; font-size: .75rem;
}
.gh-error-link { color: var(--acc); text-decoration: none; }
.gh-error-link:hover { text-decoration: underline; }

/* ═══════════════════════════════════════════════════════════
   RESUME OVERLAY
   ═══════════════════════════════════════════════════════════ */
.resume-overlay {
  position: fixed;
  inset: 0;
  z-index: 8000;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  animation: slideUp .44s cubic-bezier(.22,1,.36,1) both;
}

.resume-overlay.resume-closing {
  animation: slideDown .44s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideUp   { from { transform: translateY(100%); opacity:0; } to { transform: none; opacity:1; } }
@keyframes slideDown { from { transform: none; opacity:1; } to { transform: translateY(100%); opacity:0; } }

/* Header bar */
.resume-hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.1rem 2rem;
  border-bottom: 1px solid var(--bord);
  background: var(--surf);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

.resume-hd-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--muted);
  letter-spacing: .08em;
  font-weight: 600;
  text-transform: uppercase;
}

.resume-hd-right { display: flex; align-items: center; gap: 1rem; }

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .5rem 1.1rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  background: var(--acc);
  color: #fff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s, box-shadow .2s;
}
.resume-actions:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139,92,246,.35); }

.resume-close-btn {
  width: 36px; height: 36px;
  border: 1px solid var(--bord);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  color: var(--muted);
  font-size: 1rem;
  background: transparent;
  cursor: pointer;
  transition: border-color .2s, color .2s;
}
.resume-close-btn:hover { border-color: var(--acc); color: var(--acc); }

/* Scrollable body */
.resume-body {
  flex: 1;
  overflow-y: auto;
  display: flex;
  justify-content: center;
  padding: 3rem 2rem 4rem;
  background: var(--bg);
}

/* White paper resume card */
.resume-page {
  width: 100%;
  max-width: 860px;
  background: #fff;
  color: #1a1a2e;
  border-radius: 4px;
  padding: 3rem 3.5rem;
  box-shadow: 0 4px 60px rgba(0,0,0,.25);
  font-family: 'Plus Jakarta Sans', sans-serif;
}

/* Resume internals */
.res-name-block { margin-bottom: 2rem; border-bottom: 2px solid #1a1a2e; padding-bottom: 1.25rem; }
.res-name { font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; font-weight: 700; color: #1a1a2e; margin-bottom: .25rem; letter-spacing: -.02em; }
.res-role { font-size: 1.05rem; color: #5c5c7a; font-weight: 500; margin-bottom: .5rem; }
.res-contact { display: flex; flex-wrap: wrap; gap: 1rem; font-size: .8rem; color: #5c5c7a; }
.res-contact a { color: #5b21b6; text-decoration: none; }
.res-contact a:hover { text-decoration: underline; }

.res-cols { display: grid; grid-template-columns: 1.4fr .9fr; gap: 3rem; }

.resume-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: #5b21b6;
  margin-bottom: .65rem;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: .35rem;
}

.res-section { margin-bottom: 1.75rem; }
.res-text { font-size: .9rem; color: #4b5563; line-height: 1.7; font-weight: 400; }

.res-exp-item { margin-bottom: 1.25rem; }
.res-exp-header { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .2rem; }
.res-exp-title { font-weight: 700; font-size: .92rem; color: #1a1a2e; }
.res-exp-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: #9ca3af; flex-shrink: 0; }
.res-exp-org { font-size: .8rem; color: #6b7280; margin-bottom: .5rem; }
.res-exp-list { padding-left: 1rem; margin: 0; display: flex; flex-direction: column; gap: .3rem; }
.res-exp-list li { font-size: .85rem; color: #4b5563; line-height: 1.6; }

.res-proj-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px dashed #e5e7eb; }
.res-proj-item:last-child { border-bottom: none; }
.res-proj-header { display: flex; justify-content: space-between; align-items: center; gap: .5rem; margin-bottom: .25rem; }
.res-proj-name { font-weight: 700; font-size: .9rem; color: #1a1a2e; }
.res-proj-tags { display: flex; gap: .3rem; flex-wrap: wrap; }
.res-tag { font-family: 'JetBrains Mono', monospace; font-size: .6rem; padding: .15rem .4rem; border-radius: 3px; background: #ede9fe; color: #5b21b6; }
.res-proj-desc { font-size: .82rem; color: #6b7280; line-height: 1.55; }

.res-skill-chips { display: flex; flex-wrap: wrap; gap: .4rem; }
.res-chip { font-family: 'JetBrains Mono', monospace; font-size: .68rem; padding: .25rem .55rem; background: #f3f4f6; border: 1px solid #e5e7eb; border-radius: 4px; color: #374151; }

.res-cert-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
.res-cert-item { display: flex; align-items: center; gap: .55rem; font-size: .82rem; }
.res-cert-issuer { font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700; padding: .15rem .4rem; border-radius: 3px; flex-shrink: 0; }
.res-cert-name { flex: 1; color: #1a1a2e; font-weight: 500; font-size: .82rem; }
.res-cert-date { font-family: 'JetBrains Mono', monospace; font-size: .62rem; color: #9ca3af; flex-shrink: 0; }
.res-cert-more { font-style: italic; color: #9ca3af; font-size: .8rem; }
.res-cert-count { font-size: .68rem; background: #5b21b6; color: #fff; padding: .1rem .4rem; border-radius: 8px; }

/* Print */
@media print {
  .resume-hd, app-root > * { display: none !important; }
  .resume-overlay { position: static !important; animation: none !important; }
  .resume-body { padding: 0; overflow: visible; }
  .resume-page { box-shadow: none; border-radius: 0; padding: 2rem; }
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-about { padding: 5.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 2.5rem; }
  .cert-grid { grid-template-columns: 1fr 1fr; max-height: none; }
  .res-cols { grid-template-columns: 1fr; }
  .resume-page { padding: 2rem 1.5rem; }
  .resume-hd { padding: 1rem 1.25rem; }
  .skill-toggle-btn { cursor: pointer !important; }
}

@media (max-width: 480px) {
  .cert-grid { grid-template-columns: 1fr; }
}


--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid — real data fetched from API -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="gh-count">{{ heatmapTotal() }} CONTRIBUTIONS</span>
            }
          </div>

          @if (heatmapLoading()) {
            <div class="gh-loading">
              <span class="gh-pulse"></span> Loading contributions...
            </div>
          } @else if (heatmapError()) {
            <div class="gh-error">
              <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="gh-error-link">
                View on GitHub ↗
              </a>
            </div>
          } @else {
            <div class="gh-wrap">
              <!-- Month labels -->
              <div class="gh-months">
                <div class="gh-row-spacer"></div>
                <div class="gh-month-labels">
                  @for (m of monthLabels(); track m.col) {
                    <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                  }
                </div>
              </div>

              <!-- Day labels + grid -->
              <div class="gh-grid-wrap">
                <div class="gh-day-labels">
                  <span></span>
                  <span>Mon</span>
                  <span></span>
                  <span>Wed</span>
                  <span></span>
                  <span>Fri</span>
                  <span></span>
                </div>
                <div class="gh-grid">
                  @for (week of heatmapWeeks(); track $index) {
                    <div class="gh-col">
                      @for (day of week; track day.date) {
                        <div class="gh-cell" [class]="getCellClass(day.level)"
                             [title]="day.count > 0 ? day.count + ' contribution' + (day.count > 1 ? 's' : '') + ' on ' + day.date : 'No contributions on ' + day.date">
                        </div>
                      }
                    </div>
                  }
                </div>
              </div>

              <!-- Legend -->
              <div class="gh-legend">
                <span class="gh-legend-lbl">Less</span>
                <div class="gh-cell gh-0"></div>
                <div class="gh-cell gh-1"></div>
                <div class="gh-cell gh-2"></div>
                <div class="gh-cell gh-3"></div>
                <div class="gh-cell gh-4"></div>
                <span class="gh-legend-lbl">More</span>
              </div>
            </div>
          }
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — carousel -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNavTo($index)"
                [attr.aria-label]="'Go to cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: src\app\components\about\about.component.ts ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid — real data fetched from API -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="gh-count">{{ heatmapTotal() }} CONTRIBUTIONS</span>
            }
          </div>

          @if (heatmapLoading()) {
            <div class="gh-loading">
              <span class="gh-pulse"></span> Loading contributions...
            </div>
          } @else if (heatmapError()) {
            <div class="gh-error">
              <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="gh-error-link">
                View on GitHub ↗
              </a>
            </div>
          } @else {
            <div class="gh-wrap">
              <!-- Month labels -->
              <div class="gh-months">
                <div class="gh-row-spacer"></div>
                <div class="gh-month-labels">
                  @for (m of monthLabels(); track m.col) {
                    <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                  }
                </div>
              </div>

              <!-- Day labels + grid -->
              <div class="gh-grid-wrap">
                <div class="gh-day-labels">
                  <span></span>
                  <span>Mon</span>
                  <span></span>
                  <span>Wed</span>
                  <span></span>
                  <span>Fri</span>
                  <span></span>
                </div>
                <div class="gh-grid">
                  @for (week of heatmapWeeks(); track $index) {
                    <div class="gh-col">
                      @for (day of week; track day.date) {
                        <div class="gh-cell" [class]="getCellClass(day.level)"
                             [title]="day.count > 0 ? day.count + ' contribution' + (day.count > 1 ? 's' : '') + ' on ' + day.date : 'No contributions on ' + day.date">
                        </div>
                      }
                    </div>
                  }
                </div>
              </div>

              <!-- Legend -->
              <div class="gh-legend">
                <span class="gh-legend-lbl">Less</span>
                <div class="gh-cell gh-0"></div>
                <div class="gh-cell gh-1"></div>
                <div class="gh-cell gh-2"></div>
                <div class="gh-cell gh-3"></div>
                <div class="gh-cell gh-4"></div>
                <span class="gh-legend-lbl">More</span>
              </div>
            </div>
          }
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — carousel -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNavTo($index)"
                [attr.aria-label]="'Go to cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  /* Use 100% instead of min-height:100vh — the page-section wrapper enforces height */
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.h-photo {
  width: 220px;
  height: 220px;
  flex-shrink: 0;
}

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px dashed var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
}
.h-photo-placeholder:hover { border-color: var(--acc); border-style: solid; }
/* Hide fallback icon once real image loads */
.h-photo-placeholder img + .h-photo-icon { display: none; }
.h-photo-placeholder img:not([style*="display: none"]) + .h-photo-icon { display: none; }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb {
  height: 120px;
  overflow: hidden;
  position: relative;
}

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  /* FIX: was padding 7rem 1.5rem 4rem — the big bottom padding caused extra space.
     Reduce to a more modest amount. Use padding-bottom: 2rem. */
  .page-home { padding: 7rem 1.5rem 2rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: flex-start; gap: 2.5rem; margin-bottom: 3rem; }
  .h-photo { width: 160px; height: 160px; }
  .feat-grid { grid-template-columns: 1fr; }
  .btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="btn btn-ghost" download="Jaren_Kendrick_Resume.pdf">Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <img src="pictures/profile.jpg" alt="Jaren Kendrick"
               style="width:100%;height:100%;object-fit:cover;border-radius:50%;position:absolute;inset:0;"
               (error)="$any($event.target).style.display='none'" />
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]">
              <img [src]="'pictures/' + item.project.name.toLowerCase().replace(' ', '-') + '-thumb.jpg'"
                   style="width:100%;height:100%;object-fit:cover;"
                   (error)="$any($event.target).style.display='none'" />
            </div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
/* ══════════════════════════════════════════
   HOST — fills the page section exactly
══════════════════════════════════════════ */
:host {
  display: block;
  width: 100%;
  height: 100%;
}

/* ══════════════════════════════════════════
   PAGE SHELL — no overflow, fills viewport
   Desktop: fixed height so page-slider
   handles scroll. No internal scrolling.
══════════════════════════════════════════ */
.page-resume {
  width: 100%;
  height: 100%;
  padding: 2.5rem 3rem 2rem 3rem;
  background: var(--bg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* ══════════════════════════════════════════
   TWO-COLUMN LAYOUT
══════════════════════════════════════════ */
.res-layout {
  display: grid;
  grid-template-columns: 1.35fr 1fr;
  gap: 3rem;
  height: 100%;
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
  overflow: hidden;
}

.res-left,
.res-right {
  display: flex;
  flex-direction: column;
  gap: 0;
  overflow: hidden;
  min-height: 0;
}

/* ══════════════════════════════════════════
   RESUME HEADER
══════════════════════════════════════════ */
.res-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 2px solid var(--acc);
  margin-bottom: 1.25rem;
  flex-shrink: 0;
}

.res-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.5rem, 2.2vw, 2rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.025em;
  line-height: 1.1;
  margin-bottom: .2rem;
}
.res-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .15rem;
  font-weight: 500;
}
.res-location {
  font-size: .8rem;
  color: var(--muted);
  font-weight: 300;
}

.res-header-contacts {
  display: flex;
  flex-direction: column;
  gap: .35rem;
  align-items: flex-end;
  flex-shrink: 0;
}
.res-contact-link {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-size: .72rem;
  font-family: 'JetBrains Mono', monospace;
  color: var(--muted);
  text-decoration: none;
  letter-spacing: .02em;
  transition: color .2s;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg {
  width: 11px; height: 11px;
  fill: currentColor;
  flex-shrink: 0;
}
.res-contact-link svg[stroke] { fill: none; stroke: currentColor; stroke-width: 2; }

/* Download button — styled differently */
.res-dl-btn {
  display: inline-flex;
  align-items: center;
  gap: .4rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .06em;
  color: #fff;
  background: var(--acc);
  border: none;
  border-radius: 4px;
  padding: .35rem .75rem;
  text-decoration: none;
  margin-top: .25rem;
  cursor: pointer;
  transition: opacity .2s, transform .15s;
}
.res-dl-btn:hover { opacity: .88; transform: translateY(-1px); }
.res-dl-btn svg { width: 11px; height: 11px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

/* ══════════════════════════════════════════
   SECTION LABELS
══════════════════════════════════════════ */
.res-section {
  flex-shrink: 0;
  margin-bottom: 1rem;
}
.res-section:last-child { margin-bottom: 0; }

.res-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .4rem;
}
.res-divider {
  height: 1px;
  background: var(--bord);
  margin-bottom: .75rem;
}

/* ══════════════════════════════════════════
   EXPERIENCE / EDUCATION ENTRIES
══════════════════════════════════════════ */
.res-entry {
  display: grid;
  grid-template-columns: 110px 1fr;
  gap: 1rem;
  margin-bottom: .75rem;
}
.res-entry:last-child { margin-bottom: 0; }

.res-entry-side {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  padding-top: .1rem;
}
.res-entry-period {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--acc);
  letter-spacing: .04em;
  line-height: 1.3;
  font-weight: 600;
}
.res-entry-type {
  font-size: .65rem;
  color: var(--muted);
  font-weight: 300;
  line-height: 1.3;
}

.res-entry-header {
  display: flex;
  align-items: baseline;
  gap: .6rem;
  flex-wrap: wrap;
  margin-bottom: .4rem;
}
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 700;
  font-size: .95rem;
  color: var(--white);
  letter-spacing: -.01em;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  color: var(--acc);
  letter-spacing: .05em;
  font-weight: 500;
}

.res-bullets {
  list-style: none;
  padding: 0;
  margin: 0 0 .5rem;
  display: flex;
  flex-direction: column;
  gap: .25rem;
}
.res-bullets li {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.55;
  font-weight: 300;
  padding-left: .9rem;
  position: relative;
}
.res-bullets li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .62rem;
  top: .15rem;
}
.res-bullets li strong { color: var(--white); font-weight: 600; }

.res-edu-detail {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.6;
  font-weight: 300;
}

.res-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
  margin-top: .35rem;
}
.res-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  padding: .15rem .45rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--muted);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
}

/* ══════════════════════════════════════════
   SKILLS BARS
══════════════════════════════════════════ */
.sk-row { margin-bottom: .45rem; }
.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .2rem;
}
.sk-name {
  font-size: .78rem;
  color: var(--text);
  font-weight: 500;
}
.sk-meta {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
}
.sk-bar {
  height: 4px;
  background: var(--surf);
  border-radius: 3px;
  border: 1px solid var(--bord);
  overflow: hidden;
}
.sk-fill {
  height: 100%;
  background: var(--acc);
  border-radius: 3px;
  transition: width 1s cubic-bezier(.22,1,.36,1);
}

/* Tools chips */
.res-tools-grid {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
}
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
  transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
══════════════════════════════════════════ */
.cert-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .4rem;
}
.cert-counter {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  color: var(--muted);
  letter-spacing: .05em;
}
.cert-carousel {
  position: relative;
  display: flex;
  align-items: center;
  gap: .5rem;
  margin-bottom: .6rem;
}
.cert-nav {
  width: 30px; height: 30px;
  border: 1px solid var(--bord);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--sub); font-size: 1rem; cursor: pointer; flex-shrink: 0;
  transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 1px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap {
  flex: 1;
  overflow: hidden;
}
.cert-in   { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r { animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity:0; transform: translateX(20px);  } to { opacity:1; transform: none; } }
@keyframes certInR { from { opacity:0; transform: translateX(-20px); } to { opacity:1; transform: none; } }

.cert-card {
  display: block;
  border-radius: 7px;
  overflow: hidden;
  border: 1px solid var(--bord);
  transition: border-color .25s;
  text-decoration: none;
  cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top {
  height: 42px;
  display: flex; align-items: center;
  padding: 0 1rem; gap: .6rem;
}
.cert-issuer-badge {
  width: 26px; height: 26px;
  border-radius: 3px; display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700;
  color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  letter-spacing: .04em; color: var(--text); font-weight: 500;
}
.cert-body { background: var(--bg); padding: .85rem 1rem; }
.cert-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: .9rem; color: var(--white); line-height: 1.2; margin-bottom: .35rem;
}
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--muted); }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--acc); font-weight: 500; }

.cert-dots {
  display: flex; gap: .35rem; justify-content: center;
}
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* ══════════════════════════════════════════
   ANIMATIONS
══════════════════════════════════════════ */
@keyframes fadeUp  { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:none; } }

/* ══════════════════════════════════════════
   RESPONSIVE — on mobile, allow normal scroll
══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume {
    overflow: visible;
    height: auto;
    min-height: 100vh;
    padding: 6.5rem 1.5rem 4rem;
  }
  .res-layout {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }
  .res-left, .res-right { overflow: visible; }
  .res-header { flex-direction: column; gap: 1rem; }
  .res-header-contacts { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: .5rem; }
  .cert-nav, .cert-card, .cert-dot, .res-tool-chip, .res-dl-btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="res-layout">

    <!-- ═══════════ LEFT COLUMN ═══════════ -->
    <div class="res-left">

      <!-- HEADER -->
      <div class="res-header au d0">
        <div>
          <h2 class="res-name">Jaren Kendrick Yambao</h2>
          <p class="res-title">Full Stack Developer · Game/App Developer</p>
          <p class="res-location">Pampanga, Philippines</p>
        </div>
        <div class="res-header-contacts">
          <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
            jarenkendrickyambao&#64;gmail.com
          </a>
          <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
            <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
            github.com/jarenkendrick14
          </a>
          <!-- Download: no target="_blank" so browser downloads directly -->
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="res-dl-btn" download="Jaren_Kendrick_Resume.pdf">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Download PDF
          </a>
        </div>
      </div>

      <!-- EXPERIENCE -->
      <div class="res-section au d1">
        <h3 class="res-section-title">Experience</h3>
        <div class="res-divider"></div>
        @for (job of experience; track job.company) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ job.period }}</span>
              <span class="res-entry-type">{{ job.type }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ job.title }}</h4>
                <span class="res-entry-company">{{ job.company }}</span>
              </div>
              <ul class="res-bullets">
                @for (bullet of job.bullets; track $index) {
                  <li [innerHTML]="bullet"></li>
                }
              </ul>
              <div class="res-tags">
                @for (tag of job.tags; track tag) {
                  <span class="res-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>

      <!-- EDUCATION -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Education</h3>
        <div class="res-divider"></div>
        @for (edu of education; track edu.school) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ edu.period }}</span>
              <span class="res-entry-type">{{ edu.location }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ edu.degree }}</h4>
                <span class="res-entry-company">{{ edu.school }}</span>
              </div>
              <p class="res-edu-detail">{{ edu.detail }}</p>
            </div>
          </div>
        }
      </div>

    </div><!-- /res-left -->

    <!-- ═══════════ RIGHT COLUMN ═══════════ -->
    <div class="res-right">

      <!-- SKILLS -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Technical Skills</h3>
        <div class="res-divider"></div>
        @for (sk of skills; track sk.name; let i = $index) {
          <div class="sk-row">
            <div class="sk-info">
              <span class="sk-name">{{ sk.name }}</span>
              <span class="sk-meta">{{ sk.pct }}%</span>
            </div>
            <div class="sk-bar">
              <div class="sk-fill"
                [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                [style.transition-delay]="(i * 0.07) + 's'">
              </div>
            </div>
          </div>
        }

        <!-- Tools row -->
        <div class="res-tools-grid" style="margin-top:.85rem;">
          @for (tool of toolsList; track tool) {
            <span class="res-tool-chip">{{ tool }}</span>
          }
        </div>
      </div>

      <!-- CERTIFICATIONS — CAROUSEL -->
      <div class="res-section au d3">
        <div class="cert-header">
          <h3 class="res-section-title" style="margin-bottom:0;">Certifications</h3>
          <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
        </div>
        <div class="res-divider"></div>
        <div class="cert-carousel">
          <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous cert">‹</button>
          <div class="cert-card-wrap" [ngClass]="certAnimClass()">
            <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
              <div class="cert-top" [style.background]="currentCert.color + '22'">
                <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
              </div>
              <div class="cert-body">
                <div class="cert-name">{{ currentCert.name }}</div>
                <div class="cert-meta">
                  <div class="cert-date">{{ currentCert.date }}</div>
                  <div class="cert-view">View ↗</div>
                </div>
              </div>
            </a>
          </div>
          <button class="cert-nav" (click)="certNav(1)" aria-label="Next cert">›</button>
        </div>
        <!-- Dot indicators -->
        <div class="cert-dots">
          @for (cert of data.certificates; track $index) {
            <button class="cert-dot" [class.on]="$index === certIndex()"
              (click)="certNav($index > certIndex() ? 1 : -1)"
              [attr.aria-label]="'Cert ' + ($index + 1)">
            </button>
          }
        </div>
      </div>

    </div><!-- /res-right -->

  </div><!-- /res-layout -->
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  /* ── Cert Carousel ── */
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  get currentCert() { return this.data.certificates[this.certIndex()]; }
  get certTotal()   { return this.data.certificates.length; }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong> VR app to Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular + Ionic + Node.js + PostgreSQL) with WebSocket + streaming xAI Grok.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Managing own sprint tasks and code reviews across multiple time zones.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking, and deployment workflows. Maintaining strong academic standing while working part-time.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}



--- DIRECTORY: src\app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* Resume should fill exact viewport with no internal scroll */
:host ::ng-deep .page-resume {
  position: static !important;
  height: 100% !important;
  min-height: unset !important;
  overflow: hidden !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }

/* FIX: was [data-theme="light"] .tknob — Angular scoping breaks global selectors.
   :host-context() pierces through to the document root correctly. */
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px;
  }

  /* Resume back to scrollable on mobile */
  :host ::ng-deep .page-resume {
    height: auto !important;
    overflow: visible !important;
    min-height: 100vh !important;
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: src\app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <!-- Loop through first 5 projects for preview -->
   @for (proj of data.projects; track proj.num; let i = $index) {
     <div class="psc" [class.act]="previewActive() === i">
       <div class="psc-img-wrap">
         <!-- Shows the first image of the project -->
         <img [src]="proj.imgs[0]" [alt]="proj.name" style="width:100%; height:100%; object-fit:cover;">
         <div class="psc-overlay"></div>
       </div>
       <div class="psc-meta">
         <span class="psc-name">{{ proj.name }}</span>
         <span class="psc-tag">{{ proj.tags[0] }}</span>
       </div>
     </div>
   }
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: src\app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: src\app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
  overflow-x: hidden;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* ══════════ ANIMATIONS ══════════ */
@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }

/* Base state for animations */
.au, .al, .ar { opacity: 0; }

/* Active states */
.active-page .slabel     { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.active-page .stats-strip{ animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .08s ease both; }
.active-page .apull      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s ease both; }
.active-page .abio       { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d2  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s ease both; }
.active-page .blk.ar.d3  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s ease both; }
.active-page .blk.ar.d4  { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .34s ease both; }

/* ══════════ SECTION LABEL ══════════ */
.slabel {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 2rem;
}

.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* ══════════ COUNTER STATS ══════════ */
.stats-strip {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
  margin-bottom: 3.5rem;
  padding: 2rem 0;
  border-top: 1px solid var(--bord);
  border-bottom: 1px solid var(--bord);
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .35rem;
  text-align: center;
}

.stat-num {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4rem);
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--acc);
}

.stat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  font-weight: 500;
}

/* ══════════ MAIN GRID LAYOUT ══════════ */
.ag { display: grid; grid-template-columns: 1.1fr .9fr; gap: 5rem; }

/* ══════════ BIO TEXT ══════════ */
.bio-img-wrap {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  overflow: hidden;
  border: 2px solid var(--bord);
  margin-bottom: 1.5rem;
}
.bio-img-wrap img { width: 100%; height: 100%; object-fit: cover; }

.apull {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2.2rem, 3.5vw, 3.2rem);
  line-height: 1.2;
  letter-spacing: -.025em;
  color: var(--white);
  margin-bottom: 2rem;
}
.apull em { color: var(--acc); font-style: normal; }

.abio { font-size: 1.1rem; color: var(--text); line-height: 1.8; font-weight: 300; }
.abio p + p { margin-top: 1.25rem; }
.abio strong { color: var(--white); font-weight: 600; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: none; transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none; border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

/* ══════════ RIGHT COLUMN BLOCKS ══════════ */
.blk { margin-bottom: 2.5rem; }
.blkh {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem; letter-spacing: .12em;
  color: var(--muted); margin-bottom: 1.5rem; text-transform: uppercase;
  display: flex; align-items: center; justify-content: space-between; font-weight: 600;
}

/* ══════════ SKILLS & SPHERE ══════════ */
.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .08em;
  color: var(--acc); border: 1px solid rgba(139,92,246,.3); padding: .3rem .65rem;
  border-radius: 4px; cursor: none; background: none; transition: background .2s, border-color .2s;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.1); }

.skill-sphere-wrap {
  position: relative; min-height: 268px; display: flex;
  align-items: center; justify-content: center;
  border: 1px solid var(--bord); border-radius: 8px; background: var(--bg);
  padding: 1.5rem; overflow: hidden;
}
.sphere-hint {
  position: absolute; bottom: .6rem; left: 50%; transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--muted);
  letter-spacing: .08em; opacity: .5; pointer-events: none; width: max-content;
}

/* SPHERE TAGS */
.sphere-tag {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: 0.8rem;
  transition: color 0.2s, z-index 0.2s;
  will-change: transform, opacity;
}

/* Proficiency List */
.skl { list-style: none; display: flex; flex-direction: column; gap: 1.1rem; padding: 0; }
.sk-item { display: flex; flex-direction: column; gap: .4rem; opacity: 0; animation: fadeUp 0.55s cubic-bezier(.22,1,.36,1) forwards; }
.sk-info { display: flex; justify-content: space-between; align-items: center; font-size: .92rem; font-weight: 500; color: var(--text); }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .72rem; color: var(--muted); }
.sk-bar { width: 100%; height: 5px; background: var(--bg); border-radius: 4px; overflow: hidden; border: 1px solid var(--bord); }

@keyframes fillUp { from { width: 0; } to { width: var(--target-width); } }
.sk-fill {
  height: 100%; background: var(--acc); border-radius: 4px;
  width: 0; animation: fillUp 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards;
}

/* ══════════ GITHUB HEATMAP ══════════ */
.heatmap-total {
  font-size: .7rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 500;
}

.heatmap-wrap {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 1.5rem 0; /* Vertical padding only, content manages horiz */
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center; /* Center children vertically */
}

.heatmap-loading {
  display: flex; align-items: center; gap: .75rem;
  font-family: 'JetBrains Mono', monospace; font-size: .75rem;
  color: var(--muted); padding: .5rem 0;
}
.heatmap-pulse {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--acc); animation: pulse 1.2s ease infinite;
}
@keyframes pulse { 0%, 100% { opacity: .4; transform: scale(1); } 50% { opacity: 1; transform: scale(1.3); } }

.heatmap-fallback { text-align: center; padding: 1rem 0; }
.heatmap-link {
  font-family: 'JetBrains Mono', monospace; font-size: .8rem;
  color: var(--acc); text-decoration: none; letter-spacing: .06em;
}
.heatmap-link:hover { text-decoration: underline; }

/* ── Heatmap Container & Centering ── */
.heatmap-container {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin: 0 auto 1rem auto; /* CENTER THIS CONTAINER */
  width: max-content;
  max-width: 100%;
  overflow-x: auto;
}

.heatmap-months {
  display: flex;
  height: 14px;
  width: 100%;
}

.hm-spacer { width: 26px; flex-shrink: 0; }

.hm-months-grid {
  display: grid;
  grid-template-columns: repeat(20, 14px);
  gap: 0; 
  flex: 1;
}

.hm-month {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

.heatmap-body {
  display: flex;
  gap: 4px;
}

.heatmap-days {
  display: flex;
  flex-direction: column;
  gap: 3px;
  width: 26px;
  flex-shrink: 0;
  padding-top: 0;
}

.heatmap-days span {
  height: 11px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 11px;
  text-align: right;
  padding-right: 4px;
}

/* Actual Grid */
.heatmap-grid {
  display: flex;
  gap: 3px;
}
.heatmap-col {
  display: flex;
  flex-direction: column;
  gap: 3px;
  flex-shrink: 0;
}
.heatmap-cell {
  width: 11px;
  height: 11px;
  border-radius: 2px;
  border: 1px solid rgba(139,92,246,.08);
}

/* Legend */
.heatmap-legend {
  display: flex; align-items: center; gap: 4px;
  width: 100%;
  justify-content: flex-end; /* Align right relative to container, or center if desired */
  max-width: 320px; /* Limit width to match approximate grid width for visual center */
  margin-top: .25rem;
}
.heatmap-legend-label {
  font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  color: var(--muted); letter-spacing: .04em; margin: 0 .25rem;
}
.heatmap-legend .heatmap-cell { width: 10px; height: 10px; }

/* ══════════ RECENT COMMITS ══════════ */
.commits-list { 
  border-top: 1px solid var(--bord); 
  padding: .75rem 1.5rem 0; 
  width: 100%;
}
.commits-label {
  font-family: 'JetBrains Mono', monospace; font-size: .65rem; letter-spacing: .1em;
  text-transform: uppercase; color: var(--muted); margin-bottom: .65rem; font-weight: 600;
}
.commit-row {
  display: grid; grid-template-columns: auto 1fr auto; gap: .65rem; align-items: center;
  padding: .45rem .5rem; text-decoration: none; transition: background .15s;
  border-bottom: 1px solid rgba(139,92,246,.06); border-radius: 4px;
}
.commit-row:last-child { border-bottom: none; }
.commit-row:hover { background: rgba(139,92,246,.06); }
.commit-sha { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--acc); letter-spacing: .03em; font-weight: 600; flex-shrink: 0; }
.commit-msg { font-family: 'Plus Jakarta Sans', sans-serif; font-size: .8rem; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.commit-meta { display: flex; align-items: center; gap: .5rem; flex-shrink: 0; }
.commit-repo { font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--acc); background: rgba(139,92,246,.1); padding: .15rem .4rem; border-radius: 3px; letter-spacing: .03em; white-space: nowrap; }
.commit-time { font-family: 'JetBrains Mono', monospace; font-size: .6rem; color: var(--muted); letter-spacing: .03em; white-space: nowrap; }

/* ══════════ CERTIFICATION CAROUSEL ══════════ */
.cert-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
.cert-counter { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .06em; }
.cert-carousel { position: relative; display: flex; align-items: center; gap: .75rem; }
.cert-nav {
  width: 36px; height: 36px; border: 1px solid var(--bord); border-radius: 50%;
  display: flex; align-items: center; justify-content: center; color: var(--sub);
  font-size: 1.2rem; cursor: none; flex-shrink: 0; transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 2px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap { flex: 1; overflow: hidden; }
.cert-in  { animation: certIn  .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
.cert-in-r{ animation: certInR .3s cubic-bezier(0.22, 1, 0.36, 1) both; }
@keyframes certIn  { from { opacity: 0; transform: translateX(30px);  } to { opacity: 1; transform: translateX(0); } }
@keyframes certInR { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: translateX(0); } }

.cert-card {
  display: block; border-radius: 8px; overflow: hidden; border: 1px solid var(--bord);
  transition: border-color .25s; text-decoration: none; cursor: none;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top { height: 55px; display: flex; align-items: center; padding: 0 1.25rem; gap: .75rem; }
.cert-issuer-badge {
  width: 30px; height: 30px; border-radius: 4px; display: flex; align-items: center;
  justify-content: center; font-family: 'JetBrains Mono', monospace; font-size: .65rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name { font-family: 'JetBrains Mono', monospace; font-size: .75rem; letter-spacing: .05em; color: var(--text); font-weight: 500; }
.cert-body { background: var(--bg); padding: 1.25rem; }
.cert-name { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.1rem; letter-spacing: -.01em; color: var(--white); line-height: 1.2; margin-bottom: .5rem; }
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--muted); letter-spacing: .06em; }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: var(--acc); letter-spacing: .06em; font-weight: 500; }
.cert-dots { display: flex; gap: .4rem; justify-content: center; margin-top: 1rem; }
.cert-dot {
  width: 6px; height: 6px; border-radius: 50%; background: var(--bord);
  border: none; cursor: none; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* ══════════ RESPONSIVE ══════════ */
@media (max-width: 960px) {
  .page-about { padding: 6.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 3rem; }
  .stats-strip { grid-template-columns: repeat(3, 1fr); gap: 1rem; padding: 1.5rem 0; }
  .stat-num { font-size: clamp(2.5rem, 10vw, 3.5rem); }
  .btn, .cert-nav, .cert-card, .cert-dot, .skill-toggle-btn { cursor: pointer !important; }
  .sphere-hint { display: none; }
}

:host ::ng-deep .sphere-tag {
  font-family: 'JetBrains Mono', monospace;
  font-weight: 600;
  font-size: 0.8rem;
  transition: color 0.2s, z-index 0.2s, opacity 0.2s;
  will-change: transform, opacity;
  /* Ensure it has a color or it will be invisible */
  color: var(--text); 
}

--- DIRECTORY: src\app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">
    
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- COUNTER STATS -->
    <div class="stats-strip au d1">
      @for (c of counters; track c.label) {
        <div class="stat-item">
          <span class="stat-num">{{ c.current() }}{{ c.suffix }}</span>
          <span class="stat-label">{{ c.label }}</span>
        </div>
      }
    </div>

    <div class="ag">
      
      <!-- LEFT COLUMN: Bio -->
<div>
  <!-- Profile Image -->
  <div class="bio-img-wrap al d1">
    <!-- Updated with a reliable placeholder -->
    <img src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?fit=crop&w=300&h=300" 
         alt="Profile" 
         onerror="this.style.display='none'">
  </div>

  <h2 class="apull al d1">Full Stack Architecture. <br><em>Immersive</em> Interactive Experiences.</h2>
        <div class="abio au d2">
          <p>I am a <strong>20-year-old student</strong> at Holy Angel University (Pampanga, PH), currently pursuing a Bachelor's in Information Technology with a specialization in Web Development.</p>
          <p>While balancing my studies, I work as a Game/App Developer at VirtuIntelligence. I have built VR experiences in Unreal Engine and full-stack web applications using Angular, Node.js, and PostgreSQL. My academic background gives me a strong foundation in systems architecture, while my professional work teaches me how to ship real products.</p>
          <p>I am actively looking for a <strong>Full Stack Internship</strong> where I can apply my skills in a professional team environment.</p>
          
          <div style="margin-top:2rem;">
            <a href="assets/Jaren_Kendrick_Resume.pdf" target="_blank" class="btn btn-fill" download>
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" y1="15" x2="12" y2="3"/>
              </svg>
              Download Resume
            </a>
          </div>
        </div>
      </div>

      <!-- RIGHT COLUMN: Blocks -->
      <div>
        
        <!-- SKILLS -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position: absolute; top: 50%; left: 50%; width: 0; height: 0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          @if (skillsExpanded()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item au" [style.animation-delay]="(i * 0.1) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill" [style.--target-width]="sk.pct + '%'" [style.animation-delay]="(i * 0.1 + 0.2) + 's'"></div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERTIFICATIONS -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View Certificate ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNav($index > certIndex() ? 1 : -1)"
                [attr.aria-label]="'Cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

        <!-- HEATMAP -->
        <div class="blk ar d4">
          <div class="blkh">
            GitHub Activity
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="heatmap-total">{{ heatmapTotal() }} contributions</span>
            }
          </div>
          
          <div class="heatmap-wrap">
            @if (heatmapLoading()) {
              <div class="heatmap-loading"><span class="heatmap-pulse"></span> Loading...</div>
            } @else if (heatmapError()) {
              <div class="heatmap-fallback">
                <a href="https://github.com/jarenkendrick14" target="_blank" class="heatmap-link">View on GitHub ↗</a>
              </div>
            } @else {
              
              <div class="heatmap-container">
                <div class="heatmap-months">
                  <div class="hm-spacer"></div>
                  <div class="hm-months-grid">
                    @for (m of heatmapMonths(); track m.col) {
                      <span class="hm-month" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                    }
                  </div>
                </div>

                <div class="heatmap-body">
                  <div class="heatmap-days">
                    <span></span>
                    <span>Mon</span>
                    <span></span>
                    <span>Wed</span>
                    <span></span>
                    <span>Fri</span>
                    <span></span>
                  </div>

                  <div class="heatmap-grid">
                    @for (week of heatmapWeeks(); track $index) {
                      <div class="heatmap-col">
                        @for (day of week; track day.date) {
                          <div class="heatmap-cell"
                            [style.background]="getHeatColor(day.level)"
                            [title]="day.count > 0 ? day.count + ' contributions on ' + day.date : day.date">
                          </div>
                        }
                      </div>
                    }
                  </div>
                </div>
              </div>

              <div class="heatmap-legend">
                <span class="heatmap-legend-label">Less</span>
                <div class="heatmap-cell" [style.background]="getHeatColor(0)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(1)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(2)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(3)"></div>
                <div class="heatmap-cell" [style.background]="getHeatColor(4)"></div>
                <span class="heatmap-legend-label">More</span>
              </div>
            }
          </div>

          <!-- COMMITS -->
          @if (recentCommits().length > 0) {
            <div class="commits-list">
              <div class="commits-label">Recent Commits</div>
              @for (commit of recentCommits(); track commit.sha) {
                <a class="commit-row" [href]="commit.url" target="_blank" rel="noopener">
                  <span class="commit-sha">{{ commit.sha }}</span>
                  <span class="commit-msg">{{ commit.message }}</span>
                  <span class="commit-meta">
                    <span class="commit-repo">{{ commit.repo }}</span>
                    <span class="commit-time">{{ commit.date }}</span>
                  </span>
                </a>
              }
            </div>
          } @else if (commitsLoading()) {
            <div class="commits-list">
              <div class="heatmap-loading" style="padding: .75rem 0;"><span class="heatmap-pulse"></span> Loading commits...</div>
            </div>
          }
        </div>

      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\about\about.component.ts ---
import { Component, inject, signal, ElementRef, ViewChild, AfterViewInit, OnDestroy, Input, OnChanges, SimpleChanges } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { Certificate } from '../../models/portfolio.models';
import { CommonModule } from '@angular/common';

interface HeatmapDay { date: string; count: number; level: number; }
interface Commit { sha: string; message: string; repo: string; date: string; url: string; }

@Component({
  selector: 'app-about',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './about.component.html',
  styleUrl: './about.component.css',
})
export class AboutComponent implements AfterViewInit, OnDestroy, OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  @ViewChild('sphereContainer') sphereContainer!: ElementRef<HTMLDivElement>;
  @ViewChild('sphereWrap') sphereWrap!: ElementRef<HTMLDivElement>;

  readonly skillsExpanded = signal(false);
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  /* ── Counters ── */
  readonly counters = [
    { target: 5,  label: 'Projects Shipped', suffix: '', current: signal(0) },
    { target: 13, label: 'Certifications',   suffix: '', current: signal(0) },
    { target: 2,  label: 'Years Coding',     suffix: '+', current: signal(0) },
  ];

  /* ── GitHub Heatmap ── */
  readonly heatmapWeeks = signal<HeatmapDay[][]>([]);
  readonly heatmapMonths = signal<{ col: number, label: string }[]>([]);
  readonly heatmapTotal = signal(0);
  readonly heatmapLoading = signal(true);
  readonly heatmapError = signal(false);

  /* ── Recent Commits ── */
  readonly recentCommits = signal<Commit[]>([]);
  readonly commitsLoading = signal(true);

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly sphereTags = [
    { text: 'Angular', color: '#dd0031' }, { text: 'Vue.js 3', color: '#42b883' },
    { text: 'Node.js', color: '#339933' }, { text: 'MongoDB', color: '#47a248' },
    { text: 'TypeScript', color: '#3178c6' }, { text: 'C#', color: '#9b59b6' },
    { text: 'Unity', color: '#a78bfa' }, { text: 'Express.js', color: '#7a7a9c' },
    { text: 'REST APIs', color: '#60a5fa' }, { text: 'Git', color: '#f05030' },
    { text: 'PostgreSQL', color: '#336791' }, { text: 'Prisma', color: '#7c8cf8' },
    { text: 'Ionic', color: '#3880ff' }, { text: 'WebSockets', color: '#f59e0b' },
  ];

  get currentCert(): Certificate { return this.data.certificates[this.certIndex()]; }
  get certTotal(): number { return this.data.certificates.length; }

  private animId = 0;
  private rotX = 0.4; private rotY = 0;
  private isDrag = false; private lx = 0; private ly = 0;
  private velX = 0; private velY = 0.004;

  toggleSkills() { this.skillsExpanded.update(v => !v); }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active) {
      // RESET AND ANIMATE EVERY TIME
      this.counters.forEach(c => c.current.set(0));
      setTimeout(() => this.animateCounters(), 350);
    }
  }

  ngAfterViewInit() {
    this.initSphere();
    this.fetchHeatmap();
    this.fetchCommits();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  private animateCounters() {
    this.counters.forEach(c => {
      const duration = 1400;
      const steps = 30;
      const stepTime = duration / steps;
      let step = 0;
      const iv = setInterval(() => {
        step++;
        const t = step / steps;
        const eased = 1 - Math.pow(1 - t, 3);
        c.current.set(Math.round(eased * c.target));
        if (step >= steps) { c.current.set(c.target); clearInterval(iv); }
      }, stepTime);
    });
  }

  /* ── 100% ACCURATE DIRECT GITHUB HTML SCRAPER ── */
  private async fetchHeatmap() {
    try {
      const url = 'https://corsproxy.io/?https://github.com/users/jarenkendrick14/contributions';
      const res = await fetch(url);
      if (!res.ok) throw new Error('Proxy fetch failed');
      const html = await res.text();

      const parser = new DOMParser();
      const doc = parser.parseFromString(html, 'text/html');

      const h2 = doc.querySelector('h2.f4');
      if (h2) {
        const match = h2.textContent?.match(/([\d,]+)\s+contributions/);
        if (match) this.heatmapTotal.set(parseInt(match[1].replace(/,/g, ''), 10));
      }

      const dayMap = new Map<string, { count: number, level: number }>();
      const cells = doc.querySelectorAll('td.ContributionCalendar-day');
      
      cells.forEach(cell => {
        const date = cell.getAttribute('data-date');
        const level = parseInt(cell.getAttribute('data-level') || '0', 10);
        if (date) dayMap.set(date, { count: 0, level });
      });

      if (dayMap.size === 0) throw new Error('No cells found');

      this.generateGrid(dayMap);
      this.heatmapLoading.set(false);
    } catch (err) {
      this.heatmapError.set(true);
      this.heatmapLoading.set(false);
    }
  }

  private generateGrid(dayMap: Map<string, { count: number, level: number }>) {
    const WEEKS_TO_SHOW = 20; 
    const weeks: HeatmapDay[][] = [];
    const months: { col: number, label: string }[] = [];
    
    const today = new Date();
    const endOffset = 6 - today.getDay(); 
    const endDate = new Date(today);
    endDate.setDate(today.getDate() + endOffset);

    const startDate = new Date(endDate);
    startDate.setDate(endDate.getDate() - (WEEKS_TO_SHOW * 7) + 1);

    let lastMonth = -1;

    for (let w = 0; w < WEEKS_TO_SHOW; w++) {
      const week: HeatmapDay[] = [];
      
      const weekStart = new Date(startDate);
      weekStart.setDate(startDate.getDate() + (w * 7));
      const m = weekStart.getMonth();
      
      if (m !== lastMonth) {
        months.push({ col: w, label: weekStart.toLocaleString('default', { month: 'short' }) });
        lastMonth = m;
      }

      for (let d = 0; d < 7; d++) {
        const cellDate = new Date(startDate);
        cellDate.setDate(startDate.getDate() + (w * 7) + d);
        
        const year = cellDate.getFullYear();
        const month = String(cellDate.getMonth() + 1).padStart(2, '0');
        const day = String(cellDate.getDate()).padStart(2, '0');
        const dateStr = `${year}-${month}-${day}`;

        const data = dayMap.get(dateStr);
        week.push({
          date: dateStr,
          count: data ? data.count : 0,
          level: data ? data.level : 0 
        });
      }
      weeks.push(week);
    }
    
    this.heatmapMonths.set(months);
    this.heatmapWeeks.set(weeks);
  }

  /* ── Recent Commits ── */
  private async fetchCommits() {
    try {
      const res = await fetch('https://api.github.com/users/jarenkendrick14/events/public?per_page=30');
      if (!res.ok) throw new Error('API error');
      const events: any[] = await res.json();
      const commits: Commit[] = [];
      for (const ev of events) {
        if (ev.type === 'PushEvent' && ev.payload?.commits?.length) {
          const repoName = ev.repo?.name?.split('/')[1] || ev.repo?.name || 'unknown';
          for (const c of ev.payload.commits) {
            if (commits.length >= 5) break;
            commits.push({
              sha: c.sha?.substring(0, 7) || '', message: c.message?.split('\n')[0] || '',
              repo: repoName, date: this.timeAgo(new Date(ev.created_at)),
              url: `https://github.com/${ev.repo?.name}/commit/${c.sha}`,
            });
          }
          if (commits.length >= 5) break;
        }
      }
      this.recentCommits.set(commits);
      this.commitsLoading.set(false);
    } catch { this.commitsLoading.set(false); }
  }

  private timeAgo(date: Date): string {
    const seconds = Math.floor((Date.now() - date.getTime()) / 1000);
    if (seconds < 60) return 'just now';
    const mins = Math.floor(seconds / 60);
    if (mins < 60) return `${mins}m ago`;
    const hours = Math.floor(mins / 60);
    if (hours < 24) return `${hours}h ago`;
    const days = Math.floor(hours / 24);
    if (days < 30) return `${days}d ago`;
    return `${Math.floor(days / 30)}mo ago`;
  }

  getHeatColor(level: number): string {
    const colors = [
      'var(--bg)',                  
      'rgba(139,92,246,0.2)',      
      'rgba(139,92,246,0.45)',     
      'rgba(139,92,246,0.7)',      
      'rgba(139,92,246,1)',        
    ];
    return colors[Math.min(level, 4)];
  }

  /* ── Custom Vanilla JS Sphere ── */
  private initSphere() {
    const RADIUS = 95;
    const items = this.sphereTags.map((tag, i) => {
      const phi = Math.acos(1 - 2 * (i + 0.5) / this.sphereTags.length);
      const theta = Math.PI * (1 + Math.sqrt(5)) * i;
      const el = document.createElement('span');
      el.className = 'sphere-tag';
      el.textContent = tag.text;
      el.style.color = tag.color;
      el.style.position = 'absolute';
      el.style.background = 'var(--surf)';
      el.style.padding = '4px 8px';
      el.style.borderRadius = '4px';
      el.style.border = '1px solid var(--bord)';
      el.style.cursor = 'grab';
      el.style.userSelect = 'none';
      el.style.fontFamily = "'JetBrains Mono', monospace";
      el.style.fontSize = "12px";
      el.style.whiteSpace = "nowrap";
      if (this.sphereContainer?.nativeElement) {
        this.sphereContainer.nativeElement.appendChild(el);
      }
      return { el, phi, theta };
    });

    const render = () => {
      const cosRX = Math.cos(this.rotX), sinRX = Math.sin(this.rotX);
      const cosRY = Math.cos(this.rotY), sinRY = Math.sin(this.rotY);
      items.forEach((item) => {
        let x = Math.sin(item.phi) * Math.cos(item.theta);
        let y = Math.cos(item.phi);
        let z = Math.sin(item.phi) * Math.sin(item.theta);
        const x2 = x * cosRY - z * sinRY;
        const z2 = x * sinRY + z * cosRY;
        const y3 = y * cosRX - z2 * sinRX;
        const z3 = y * sinRX + z2 * cosRX;
        const scale = (z3 + 2.2) / 3.2;
        const px = x2 * RADIUS;
        const py = y3 * RADIUS;
        item.el.style.transform = `translate(-50%,-50%) translate(${px}px,${py}px)`;
        item.el.style.opacity = Math.max(0.1, scale).toString();
        item.el.style.zIndex = Math.round(scale * 20).toString();
        item.el.style.borderColor = z3 > 0 ? 'rgba(167,139,250, 0.6)' : 'var(--bord)';
      });
    };

    const animate = () => {
      if (!this.isDrag) { this.rotY += this.velY; this.rotX += this.velX; this.velX *= 0.97; }
      render();
      this.animId = requestAnimationFrame(animate);
    };

    const wrap = this.sphereWrap?.nativeElement;
    if (wrap) {
      wrap.addEventListener('mousedown', e => { this.isDrag = true; this.lx = e.clientX; this.ly = e.clientY; });
      document.addEventListener('mousemove', e => {
        if (!this.isDrag) return;
        this.velY = (e.clientX - this.lx) * 0.006;
        this.velX = (e.clientY - this.ly) * 0.006;
        this.rotY += this.velY; this.rotX += this.velX;
        this.lx = e.clientX; this.ly = e.clientY;
      });
      document.addEventListener('mouseup', () => this.isDrag = false);
    }
    animate();
  }
}

--- DIRECTORY: src\app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">{{ data.blogPosts.length }} posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          
          <!-- UPDATED IMAGE CONTAINER -->
          <div class="bc-img">
            <img [src]="post.img" [alt]="post.title" 
                 style="width:100%; height:100%; object-fit:cover;"
                 onerror="this.style.opacity='0.1'"> 
          </div>

          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: src\app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: src\app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: src\app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: src\app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: src\app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  /* Use 100% instead of min-height:100vh — the page-section wrapper enforces height */
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.h-photo {
  width: 220px;
  height: 220px;
  flex-shrink: 0;
}

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px dashed var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
}
.h-photo-placeholder:hover { border-color: var(--acc); border-style: solid; }
/* Hide fallback icon once real image loads */
.h-photo-placeholder img + .h-photo-icon { display: none; }
.h-photo-placeholder img:not([style*="display: none"]) + .h-photo-icon { display: none; }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb {
  height: 120px;
  overflow: hidden;
  position: relative;
}

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  /* FIX: was padding 7rem 1.5rem 4rem — the big bottom padding caused extra space.
     Reduce to a more modest amount. Use padding-bottom: 2rem. */
  .page-home { padding: 7rem 1.5rem 2rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: flex-start; gap: 2.5rem; margin-bottom: 3rem; }
  .h-photo { width: 160px; height: 160px; }
  .feat-grid { grid-template-columns: 1fr; }
  .btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="btn btn-ghost" download="Jaren_Kendrick_Resume.pdf">Download Resume</a>
        </div>
      </div>
      
      <!-- PROFILE PHOTO -->
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <!-- UPDATED PATH -->
          <img src="assets/images/profile.jpg" alt="Jaren Kendrick"
               style="width:100%;height:100%;object-fit:cover;border-radius:50%;position:absolute;inset:0;"
               onerror="this.style.display='none'; this.nextElementSibling.style.display='block'" />
          <!-- Fallback Icon -->
          <svg style="display:none;" width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <!-- FEATURED PROJECTS -->
    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            
            <!-- UPDATED THUMBNAIL LOGIC -->
            <div class="feat-thumb">
              <img [src]="item.project.imgs[0]" [alt]="item.project.name"
                   style="width:100%;height:100%;object-fit:cover;"
                   onerror="this.style.display='none'" />
            </div>

            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: src\app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: src\app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: src\app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: src\app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: src\app\components\resume\resume.component.css ---
/* ══════════════════════════════════════════
   HOST — fills the page section exactly
══════════════════════════════════════════ */
:host {
  display: block;
  width: 100%;
  height: 100%;
}

/* ══════════════════════════════════════════
   PAGE SHELL — no overflow, fills viewport
   Desktop: fixed height so page-slider
   handles scroll. No internal scrolling.
══════════════════════════════════════════ */
.page-resume {
  width: 100%;
  height: 100%;
  padding: 2.5rem 3rem 2rem 3rem;
  background: var(--bg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* ══════════════════════════════════════════
   TWO-COLUMN LAYOUT
══════════════════════════════════════════ */
.res-layout {
  display: grid;
  grid-template-columns: 1.35fr 1fr;
  gap: 3rem;
  height: 100%;
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
  overflow: hidden;
}

.res-left,
.res-right {
  display: flex;
  flex-direction: column;
  gap: 0;
  overflow: hidden;
  min-height: 0;
}

/* ══════════════════════════════════════════
   RESUME HEADER
══════════════════════════════════════════ */
.res-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 2px solid var(--acc);
  margin-bottom: 1.25rem;
  flex-shrink: 0;
}

.res-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.5rem, 2.2vw, 2rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.025em;
  line-height: 1.1;
  margin-bottom: .2rem;
}
.res-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .15rem;
  font-weight: 500;
}
.res-location {
  font-size: .8rem;
  color: var(--muted);
  font-weight: 300;
}

.res-header-contacts {
  display: flex;
  flex-direction: column;
  gap: .35rem;
  align-items: flex-end;
  flex-shrink: 0;
}
.res-contact-link {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-size: .72rem;
  font-family: 'JetBrains Mono', monospace;
  color: var(--muted);
  text-decoration: none;
  letter-spacing: .02em;
  transition: color .2s;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg {
  width: 11px; height: 11px;
  fill: currentColor;
  flex-shrink: 0;
}
.res-contact-link svg[stroke] { fill: none; stroke: currentColor; stroke-width: 2; }

/* Download button — styled differently */
.res-dl-btn {
  display: inline-flex;
  align-items: center;
  gap: .4rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .06em;
  color: #fff;
  background: var(--acc);
  border: none;
  border-radius: 4px;
  padding: .35rem .75rem;
  text-decoration: none;
  margin-top: .25rem;
  cursor: pointer;
  transition: opacity .2s, transform .15s;
}
.res-dl-btn:hover { opacity: .88; transform: translateY(-1px); }
.res-dl-btn svg { width: 11px; height: 11px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

/* ══════════════════════════════════════════
   SECTION LABELS
══════════════════════════════════════════ */
.res-section {
  flex-shrink: 0;
  margin-bottom: 1rem;
}
.res-section:last-child { margin-bottom: 0; }

.res-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .4rem;
}
.res-divider {
  height: 1px;
  background: var(--bord);
  margin-bottom: .75rem;
}

/* ══════════════════════════════════════════
   EXPERIENCE / EDUCATION ENTRIES
══════════════════════════════════════════ */
.res-entry {
  display: grid;
  grid-template-columns: 110px 1fr;
  gap: 1rem;
  margin-bottom: .75rem;
}
.res-entry:last-child { margin-bottom: 0; }

.res-entry-side {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  padding-top: .1rem;
}
.res-entry-period {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--acc);
  letter-spacing: .04em;
  line-height: 1.3;
  font-weight: 600;
}
.res-entry-type {
  font-size: .65rem;
  color: var(--muted);
  font-weight: 300;
  line-height: 1.3;
}

.res-entry-header {
  display: flex;
  align-items: baseline;
  gap: .6rem;
  flex-wrap: wrap;
  margin-bottom: .4rem;
}
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 700;
  font-size: .95rem;
  color: var(--white);
  letter-spacing: -.01em;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  color: var(--acc);
  letter-spacing: .05em;
  font-weight: 500;
}

.res-bullets {
  list-style: none;
  padding: 0;
  margin: 0 0 .5rem;
  display: flex;
  flex-direction: column;
  gap: .25rem;
}
.res-bullets li {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.55;
  font-weight: 300;
  padding-left: .9rem;
  position: relative;
}
.res-bullets li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .62rem;
  top: .15rem;
}
.res-bullets li strong { color: var(--white); font-weight: 600; }

.res-edu-detail {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.6;
  font-weight: 300;
}

.res-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
  margin-top: .35rem;
}
.res-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  padding: .15rem .45rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--muted);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
}

/* ══════════════════════════════════════════
   SKILLS BARS
══════════════════════════════════════════ */
.sk-row { margin-bottom: .45rem; }
.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .2rem;
}
.sk-name {
  font-size: .78rem;
  color: var(--text);
  font-weight: 500;
}
.sk-meta {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
}
.sk-bar {
  height: 4px;
  background: var(--surf);
  border-radius: 3px;
  border: 1px solid var(--bord);
  overflow: hidden;
}
.sk-fill {
  height: 100%;
  background: var(--acc);
  border-radius: 3px;
  transition: width 1s cubic-bezier(.22,1,.36,1);
}

/* Tools chips */
.res-tools-grid {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
}
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
  transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
══════════════════════════════════════════ */
.cert-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .4rem;
}
.cert-counter {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  color: var(--muted);
  letter-spacing: .05em;
}
.cert-carousel {
  position: relative;
  display: flex;
  align-items: center;
  gap: .5rem;
  margin-bottom: .6rem;
}
.cert-nav {
  width: 30px; height: 30px;
  border: 1px solid var(--bord);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--sub); font-size: 1rem; cursor: pointer; flex-shrink: 0;
  transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 1px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap {
  flex: 1;
  overflow: hidden;
}
.cert-in   { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r { animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity:0; transform: translateX(20px);  } to { opacity:1; transform: none; } }
@keyframes certInR { from { opacity:0; transform: translateX(-20px); } to { opacity:1; transform: none; } }

.cert-card {
  display: block;
  border-radius: 7px;
  overflow: hidden;
  border: 1px solid var(--bord);
  transition: border-color .25s;
  text-decoration: none;
  cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top {
  height: 42px;
  display: flex; align-items: center;
  padding: 0 1rem; gap: .6rem;
}
.cert-issuer-badge {
  width: 26px; height: 26px;
  border-radius: 3px; display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700;
  color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  letter-spacing: .04em; color: var(--text); font-weight: 500;
}
.cert-body { background: var(--bg); padding: .85rem 1rem; }
.cert-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: .9rem; color: var(--white); line-height: 1.2; margin-bottom: .35rem;
}
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--muted); }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--acc); font-weight: 500; }

.cert-dots {
  display: flex; gap: .35rem; justify-content: center;
}
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* ══════════════════════════════════════════
   ANIMATIONS
══════════════════════════════════════════ */
@keyframes fadeUp  { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:none; } }

/* ══════════════════════════════════════════
   RESPONSIVE — on mobile, allow normal scroll
══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume {
    overflow: visible;
    height: auto;
    min-height: 100vh;
    padding: 6.5rem 1.5rem 4rem;
  }
  .res-layout {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }
  .res-left, .res-right { overflow: visible; }
  .res-header { flex-direction: column; gap: 1rem; }
  .res-header-contacts { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: .5rem; }
  .cert-nav, .cert-card, .cert-dot, .res-tool-chip, .res-dl-btn { cursor: pointer !important; }
}

--- DIRECTORY: src\app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="res-layout">

    <!-- ═══════════ LEFT COLUMN ═══════════ -->
    <div class="res-left">

      <!-- HEADER -->
      <div class="res-header au d0">
        <div>
          <h2 class="res-name">Jaren Kendrick Yambao</h2>
          <p class="res-title">Full Stack Developer · Game/App Developer</p>
          <p class="res-location">Pampanga, Philippines</p>
        </div>
        <div class="res-header-contacts">
          <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
            jarenkendrickyambao&#64;gmail.com
          </a>
          <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
            <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
            github.com/jarenkendrick14
          </a>
          <!-- Download: no target="_blank" so browser downloads directly -->
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="res-dl-btn" download="Jaren_Kendrick_Resume.pdf">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Download PDF
          </a>
        </div>
      </div>

      <!-- EXPERIENCE -->
      <div class="res-section au d1">
        <h3 class="res-section-title">Experience</h3>
        <div class="res-divider"></div>
        @for (job of experience; track job.company) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ job.period }}</span>
              <span class="res-entry-type">{{ job.type }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ job.title }}</h4>
                <span class="res-entry-company">{{ job.company }}</span>
              </div>
              <ul class="res-bullets">
                @for (bullet of job.bullets; track $index) {
                  <li [innerHTML]="bullet"></li>
                }
              </ul>
              <div class="res-tags">
                @for (tag of job.tags; track tag) {
                  <span class="res-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>

      <!-- EDUCATION -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Education</h3>
        <div class="res-divider"></div>
        @for (edu of education; track edu.school) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ edu.period }}</span>
              <span class="res-entry-type">{{ edu.location }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ edu.degree }}</h4>
                <span class="res-entry-company">{{ edu.school }}</span>
              </div>
              <p class="res-edu-detail">{{ edu.detail }}</p>
            </div>
          </div>
        }
      </div>

    </div><!-- /res-left -->

    <!-- ═══════════ RIGHT COLUMN ═══════════ -->
    <div class="res-right">

      <!-- SKILLS -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Technical Skills</h3>
        <div class="res-divider"></div>
        @for (sk of skills; track sk.name; let i = $index) {
          <div class="sk-row">
            <div class="sk-info">
              <span class="sk-name">{{ sk.name }}</span>
              <span class="sk-meta">{{ sk.pct }}%</span>
            </div>
            <div class="sk-bar">
              <div class="sk-fill"
                [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                [style.transition-delay]="(i * 0.07) + 's'">
              </div>
            </div>
          </div>
        }

        <!-- Tools row -->
        <div class="res-tools-grid" style="margin-top:.85rem;">
          @for (tool of toolsList; track tool) {
            <span class="res-tool-chip">{{ tool }}</span>
          }
        </div>
      </div>

      <!-- CERTIFICATIONS — CAROUSEL -->
      <div class="res-section au d3">
        <div class="cert-header">
          <h3 class="res-section-title" style="margin-bottom:0;">Certifications</h3>
          <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
        </div>
        <div class="res-divider"></div>
        <div class="cert-carousel">
          <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous cert">‹</button>
          <div class="cert-card-wrap" [ngClass]="certAnimClass()">
            <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
              <div class="cert-top" [style.background]="currentCert.color + '22'">
                <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
              </div>
              <div class="cert-body">
                <div class="cert-name">{{ currentCert.name }}</div>
                <div class="cert-meta">
                  <div class="cert-date">{{ currentCert.date }}</div>
                  <div class="cert-view">View ↗</div>
                </div>
              </div>
            </a>
          </div>
          <button class="cert-nav" (click)="certNav(1)" aria-label="Next cert">›</button>
        </div>
        <!-- Dot indicators -->
        <div class="cert-dots">
          @for (cert of data.certificates; track $index) {
            <button class="cert-dot" [class.on]="$index === certIndex()"
              (click)="certNav($index > certIndex() ? 1 : -1)"
              [attr.aria-label]="'Cert ' + ($index + 1)">
            </button>
          }
        </div>
      </div>

    </div><!-- /res-right -->

  </div><!-- /res-layout -->
</section>

--- DIRECTORY: src\app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  /* ── Cert Carousel ── */
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  get currentCert() { return this.data.certificates[this.certIndex()]; }
  get certTotal()   { return this.data.certificates.length; }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong> VR app to Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular + Ionic + Node.js + PostgreSQL) with WebSocket + streaming xAI Grok.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Managing own sprint tasks and code reviews across multiple time zones.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking, and deployment workflows. Maintaining strong academic standing while working part-time.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: src\app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: src\app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: src\app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: src\app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: src\app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: src\app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            
            <!-- UPDATED AVATAR LOGIC -->
            <div class="tmav" style="overflow:hidden;">
              <img [src]="tm.img" [alt]="tm.name" 
                   style="width:100%; height:100%; object-fit:cover;"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
              
              <!-- Fallback if image fails -->
              <div style="display:none; width:100%; height:100%; align-items:center; justify-content:center;">
                {{ tm.initial }}
              </div>
            </div>

            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) { <span class="placeholder-note"></span> }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: src\app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: src\app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: src\app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', 
      name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }],
      github: null,
      imgs: [
        'https://images.unsplash.com/photo-1622979135225-d2ba269fb1bd?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1617802690992-15d93263d3a9?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1592478411213-61535fdd861d?q=80&w=1000&auto=format&fit=crop'
      ]
    },
    {
      num: '02', 
      name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI Grok. Features WebSocket-based messaging, streaming AI responses, and a full @mention system.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols and full-stack TypeScript.',
      features: ['Real-time WebSocket messaging', 'Streaming AI responses via xAI Grok', 'Turn management system', '@mention filtered dropdown', 'Cross-platform via Ionic'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: [
        'https://images.unsplash.com/photo-1611162617474-5b21e879e113?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1587560699334-cc4da63c24b9?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1551288049-bebda4e38f71?q=80&w=1000&auto=format&fit=crop'
      ]
    },
    {
      num: '03', 
      name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: [
        'https://images.unsplash.com/photo-1469854523086-cc02fe5d8800?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1501785888041-af3ef285b470?q=80&w=1000&auto=format&fit=crop'
      ]
    },
    {
      num: '04', 
      name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, and admin dashboard.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 frontend.',
      features: ['Persistent cart state', 'Full CRUD admin dashboard', 'JWT authentication', 'Product search and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: [
        'https://images.unsplash.com/photo-1523381210434-271e8be1f52b?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1556742049-0cfed4f7a07d?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1555529771-835f59fc5deb?q=80&w=1000&auto=format&fit=crop'
      ]
    },
    {
      num: '05', 
      name: "Einstein's Art",
      subtitle: 'Dynamic company website · Angular + TypeScript',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: [
        'https://images.unsplash.com/photo-1544816155-12df9643f363?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1513542789411-b6a5d4f31634?q=80&w=1000&auto=format&fit=crop', 
        'https://images.unsplash.com/photo-1460925895917-afdab827c52f?q=80&w=1000&auto=format&fit=crop'
      ]
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me.',
      content: `<p>Shipping PathFinder taught me that users don't read instructions; they react to environments. This realization changed how I approach web UI—designing for intuition over instruction.</p><h3>State management is universal</h3><p>Unity's event systems prepared me for the reactivity of Angular and Vue. The syntax changed, but the architectural logic remained the same.</p>`,
      img: 'https://images.unsplash.com/photo-1593508512255-86ab42a8e620?q=80&w=1000&auto=format&fit=crop'
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here is the honest breakdown.',
      content: `<p>For a portfolio site, vanilla JS is king. But for a booking platform with JWT auth and complex routing, a framework like Vue earns its keep by handling the boilerplate so I can focus on logic.</p>`,
      img: 'https://images.unsplash.com/photo-1555099962-4199c345e5dd?q=80&w=1000&auto=format&fit=crop'
    }
  ];

  readonly testimonials: Testimonial[] = [
    { 
      quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', 
      name: 'Louiery Sincioco', 
      role: 'Chief Technology Officer · VirtuIntelligence', 
      initial: 'C',
      img: 'https://randomuser.me/api/portraits/men/32.jpg'
    },
    { 
      quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', 
      name: 'Samantha T.', 
      role: 'Classmate & Collaborator · HAU', 
      initial: 'M',
      img: 'https://randomuser.me/api/portraits/women/44.jpg'
    },
    { 
      quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', 
      name: 'Prof. [Name]', 
      role: 'IT Instructor · Holy Angel University', 
      initial: 'P',
      img: 'https://randomuser.me/api/portraits/men/85.jpg'
    },
  ];
}

--- DIRECTORY: src\app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: src\app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
  img?: string; // Optional image path
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
  img?: string; // Optional image path
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}

--- DIRECTORY: src\index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: src\main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { provideHttpClient } from '@angular/common/http'; 
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
    provideHttpClient(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: src\project_bundle.txt ---

--- DIRECTORY: app\app.component.css ---
.app-root {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  display: flex;
  background: var(--bg);
}

.main-content {
  position: fixed;
  left: 200px;
  top: 0;
  right: 0;
  bottom: 0;
  overflow: hidden;
}

/* ══════════ PAGE SLIDER LOGIC ══════════ */
.page-slider {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

.page-section {
  display: block;
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  will-change: transform;
  transition: transform 0.6s cubic-bezier(0.76, 0, 0.24, 1);
  transform: translateY(100%);
  z-index: 10;
}

.page-section.active { transform: translateY(0); }
.page-section.prev   { transform: translateY(-100%); }
.page-section.next   { transform: translateY(100%); }

/* ══════════ FIX INNER COMPONENT SCROLLING ══════════ */
:host ::ng-deep .page-home,
:host ::ng-deep .page-about,
:host ::ng-deep .page-projects,
:host ::ng-deep .page-experience,
:host ::ng-deep .page-blog,
:host ::ng-deep .page-testimonials,
:host ::ng-deep .page-contact {
  position: static !important;
  height: auto !important;
  min-height: 100% !important;
  overflow: visible !important;
  width: 100% !important;
}

/* Resume should fill exact viewport with no internal scroll */
:host ::ng-deep .page-resume {
  position: static !important;
  height: 100% !important;
  min-height: unset !important;
  overflow: hidden !important;
  width: 100% !important;
}

/* ══════════ SCROLLBAR ══════════ */
.page-section::-webkit-scrollbar { width: 6px; }
.page-section::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* ══════════ MOBILE NAV ══════════ */
.mobnav {
  display: none;
  position: fixed; top: 0; left: 0; right: 0; height: 70px;
  background: var(--sb); border-bottom: 1px solid var(--sbbrd);
  z-index: 500; align-items: center; justify-content: space-between; padding: 0 1.5rem;
}
.mlogo { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-style: italic; font-size: 1.8rem; color: var(--white); text-decoration: none; }
.mlogo span { color: var(--acc); font-style: normal; }
.mnr { display: flex; align-items: center; gap: 1.5rem; }
.thm { width: 46px; height: 24px; border-radius: 100px; background: var(--surf); border: 1px solid var(--bord); position: relative; cursor: none; }
.tknob { position: absolute; top: 2px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: var(--acc); display: flex; align-items: center; justify-content: center; transition: transform .3s ease; color: #fff; }

/* FIX: was [data-theme="light"] .tknob — Angular scoping breaks global selectors.
   :host-context() pierces through to the document root correctly. */
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

.mbg { display: flex; flex-direction: column; gap: 6px; background: none; border: none; padding: 4px; cursor: none; }
.mbg span { display: block; width: 26px; height: 2px; background: var(--white); transition: all .25s; }
.mbg.open span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
.mbg.open span:nth-child(2) { opacity: 0; }
.mbg.open span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

.mobmenu {
  display: flex; position: fixed; top: 70px; left: 0; right: 0; bottom: 0;
  background: var(--bg); padding: 2rem; z-index: 499; flex-direction: column; gap: 1.5rem;
  animation: fadeUp .3s ease both;
}
.mobmenu a {
  font-family: 'JetBrains Mono', monospace; font-size: 1.1rem; letter-spacing: .05em;
  color: var(--text); text-transform: uppercase; font-weight: 500;
  border-bottom: 1px solid var(--bord); padding-bottom: 1rem; cursor: pointer; text-decoration: none;
}
.mobmenu a.on { color: var(--acc); border-color: var(--acc); }

/* ══════════ CURSOR & PREVIEW ══════════ */
#cdot{position:fixed;width:7px;height:7px;background:var(--acc);border-radius:50%;pointer-events:none;z-index:10000;transform:translate(-50%,-50%);}
#cring{position:fixed;width:32px;height:32px;border:1.5px solid var(--acc);border-radius:50%;pointer-events:none;z-index:9999;transform:translate(-50%,-50%);opacity:.5;transition:width .25s,height .25s,opacity .25s !important;}
#cring.big{width:55px;height:55px;opacity:.22;}
.cl{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:.12em;color:var(--acc);opacity:0;transition:opacity .2s !important;user-select:none;}
#cring.big .cl{opacity:1;}

#prvw{position:fixed;width:290px;height:185px;border-radius:8px;overflow:hidden;pointer-events:none;z-index:9990;opacity:0;transform:translate(-50%,-65%) scale(.88);transition:opacity .28s ease,transform .28s ease !important;border:1px solid var(--bord);box-shadow:0 18px 55px rgba(0,0,0,.55);}
#prvw.on{opacity:1;transform:translate(-50%,-65%) scale(1);}
.psc{position:absolute;inset:0;opacity:0;transition:opacity .2s !important;}
.psc.act{opacity:1;}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

/* ══════════ MOBILE STACKING ══════════ */
@media (max-width: 960px) {
  .main-content {
    position: relative;
    left: 0;
    top: 0;
    bottom: auto;
    right: 0;
    overflow: visible;
    padding-top: 70px;
  }

  /* Resume back to scrollable on mobile */
  :host ::ng-deep .page-resume {
    height: auto !important;
    overflow: visible !important;
    min-height: 100vh !important;
  }

  .mobnav { display: flex; }

  .app-root {
    overflow: auto;
    height: auto;
    display: block;
  }

  .page-slider {
    height: auto;
    overflow: visible;
    display: flex;
    flex-direction: column;
  }

  .page-section {
    position: relative !important;
    height: auto !important;
    transform: none !important;
    transition: none !important;
    opacity: 1 !important;
    min-height: 100vh !important;
    overflow: visible !important;
    display: block !important;
  }

  #cdot, #cring, #prvw { display: none !important; }
}

--- DIRECTORY: app\app.component.html ---
<div id="cdot" #cdot aria-hidden="true"></div>
<div id="cring" #cring [class.big]="cursorHover()" aria-hidden="true"><span class="cl">VIEW</span></div>

<div id="prvw" #prvw [class.on]="previewActive() !== null" aria-hidden="true">
   <div class="psc" [class.act]="previewActive() === 0"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#0a0a14"/><defs><radialGradient id="rg0" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><circle cx="145" cy="92" r="82" fill="url(#rg0)"/><circle cx="145" cy="92" r="78" fill="none" stroke="#a78bfa" stroke-width=".7" stroke-dasharray="5 9" opacity=".4"/><circle cx="145" cy="92" r="48" fill="none" stroke="#a78bfa" stroke-width=".5" opacity=".25"/><text x="145" y="97" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".5">🥽</text><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">PATHFINDER · VR</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 1"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#070d14"/><defs><radialGradient id="rg1s" cx="40%" cy="50%"><stop offset="0%" stop-color="#38bdf8" stop-opacity=".2"/><stop offset="100%" stop-color="#38bdf8" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg1s)"/><rect x="60" y="50" width="120" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".35"/><rect x="110" y="76" width="140" height="18" rx="9" fill="#38bdf8" opacity=".08"/><rect x="60" y="102" width="100" height="18" rx="9" fill="none" stroke="#38bdf8" stroke-width=".8" opacity=".25"/><circle cx="45" cy="59" r="8" fill="#38bdf8" opacity=".15"/><circle cx="255" cy="85" r="8" fill="#38bdf8" opacity=".15"/><text x="145" y="170" text-anchor="middle" fill="#38bdf8" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">SYMPOSIUM · REAL-TIME CHAT</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 2"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#09101a"/><defs><radialGradient id="rg2t" cx="35%" cy="60%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".22"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg2t)"/><rect x="45" y="32" width="200" height="120" rx="5" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/><rect x="45" y="32" width="200" height="22" rx="5" fill="#60a5fa" opacity=".08"/><circle cx="59" cy="43" r="4" fill="#60a5fa" opacity=".25"/><rect x="56" y="63" width="115" height="7" rx="3" fill="#60a5fa" opacity=".2"/><text x="145" y="170" text-anchor="middle" fill="#60a5fa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">TRAVEL ATELIER · FULL STACK</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 3"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#080f0b"/><defs><radialGradient id="rg3d" cx="65%" cy="40%"><stop offset="0%" stop-color="#34d399" stop-opacity=".18"/><stop offset="100%" stop-color="#34d399" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg3d)"/><rect x="30" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".35"/><rect x="108" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".25"/><rect x="186" y="28" width="68" height="88" rx="4" fill="none" stroke="#34d399" stroke-width=".7" opacity=".18"/><text x="145" y="170" text-anchor="middle" fill="#34d399" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">DROPIFY · E-COMMERCE</text></svg></div>
   <div class="psc" [class.act]="previewActive() === 4"><svg width="290" height="185" viewBox="0 0 290 185" xmlns="http://www.w3.org/2000/svg"><rect width="290" height="185" fill="#100d16"/><defs><radialGradient id="rg4e" cx="50%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs><rect width="290" height="185" fill="url(#rg4e)"/><rect x="55" y="30" width="180" height="115" rx="6" fill="none" stroke="#a78bfa" stroke-width=".7" opacity=".35"/><text x="145" y="170" text-anchor="middle" fill="#a78bfa" font-family="monospace" font-size="9" letter-spacing="2" opacity=".55">EINSTEIN'S ART · ANGULAR</text></svg></div>
</div>

<div class="app-root" [attr.data-page]="nav.currentPage()">
  <app-sidebar />

  <header class="mobnav" role="banner">
    <a href="#" class="mlogo" (click)="goTo(0); $event.preventDefault()">jk<span>.</span></a>
    <div class="mnr">
      <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle theme">
        <div class="tknob">
          @if (nav.isDark()) { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg> } 
          @else { <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/></svg> }
        </div>
      </button>
      <button class="mbg" [class.open]="mobileMenuOpen()" (click)="mobileMenuOpen.set(!mobileMenuOpen())" aria-label="Toggle menu" [attr.aria-expanded]="mobileMenuOpen()">
        <span></span><span></span><span></span>
      </button>
    </div>
  </header>

  @if (mobileMenuOpen()) {
    <nav class="mobmenu" role="navigation">
      @for (item of data.navItems; track item.index) {
        <a (click)="goTo(item.index)" [class.on]="nav.currentPage() === item.index">0{{ item.index + 1 }} {{ item.label }}</a>
      }
    </nav>
  }

  <main class="main-content">
    <div class="page-slider">
      <!-- Logic: Component receives [active]="true" only when slide finishes. 
           Slide handles transition, directive handles the glitch effect. -->
      
      <app-home class="page-section" 
        [class.active]="nav.currentPage() === 0" 
        [class.prev]="nav.currentPage() > 0"
        [active]="nav.currentPage() === 0"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-home>

      <app-about class="page-section" 
        [class.active]="nav.currentPage() === 1" 
        [class.prev]="nav.currentPage() > 1"
        [class.next]="nav.currentPage() < 1"
        [active]="nav.currentPage() === 1">
      </app-about>

      <app-projects class="page-section" 
        [class.active]="nav.currentPage() === 2" 
        [class.prev]="nav.currentPage() > 2"
        [class.next]="nav.currentPage() < 2"
        [active]="nav.currentPage() === 2"
        (projectOpened)="openProject($event)" 
        (previewHover)="setPreview($event)">
      </app-projects>

      <app-experience class="page-section" 
        [class.active]="nav.currentPage() === 3" 
        [class.prev]="nav.currentPage() > 3"
        [class.next]="nav.currentPage() < 3"
        [active]="nav.currentPage() === 3">
      </app-experience>

      <app-blog class="page-section" 
        [class.active]="nav.currentPage() === 4" 
        [class.prev]="nav.currentPage() > 4"
        [class.next]="nav.currentPage() < 4"
        [active]="nav.currentPage() === 4"
        (postOpened)="openBlog($event)">
      </app-blog>

      <app-testimonials class="page-section" 
        [class.active]="nav.currentPage() === 5" 
        [class.prev]="nav.currentPage() > 5"
        [class.next]="nav.currentPage() < 5"
        [active]="nav.currentPage() === 5">
      </app-testimonials>

      <app-contact class="page-section" 
        [class.active]="nav.currentPage() === 6" 
        [class.prev]="nav.currentPage() > 6"
        [class.next]="nav.currentPage() < 6"
        [active]="nav.currentPage() === 6">
      </app-contact>
    </div>
  </main>

  @if (activeProject()) { <app-project-overlay [project]="activeProject()" (closed)="closeProject()" /> }
  @if (activeBlogPost()) { <app-blog-overlay [post]="activeBlogPost()" (closed)="closeBlog()" /> }
  
  <app-terminal #terminal />
</div>

--- DIRECTORY: app\app.component.ts ---
import {
  Component, inject, signal, HostListener, ViewChild, ElementRef, AfterViewInit, OnDestroy
} from '@angular/core';
import { NavigationService } from './data/navigation.service';
import { PortfolioDataService } from './data/portfolio.data';
import { Project, BlogPost } from './models/portfolio.models';

import { SidebarComponent } from './components/sidebar/sidebar.component';
import { HomeComponent } from './components/home/home.component';
import { AboutComponent } from './components/about/about.component';
import { ProjectsComponent } from './components/projects/projects.component';
import { ExperienceComponent } from './components/experience/experience.component';
import { BlogComponent } from './components/blog/blog.component';
import { TestimonialsComponent } from './components/testimonials/testimonials.component';
import { ContactComponent } from './components/contact/contact.component';
import { ProjectOverlayComponent } from './components/project-overlay/project-overlay.component';
import { BlogOverlayComponent } from './components/blog-overlay/blog-overlay.component';
import { TerminalComponent } from './components/terminal/terminal.component';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [
    SidebarComponent, HomeComponent, AboutComponent, ProjectsComponent,
    ExperienceComponent, BlogComponent, TestimonialsComponent, ContactComponent,
    ProjectOverlayComponent, BlogOverlayComponent, TerminalComponent,
  ],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css',
})
export class AppComponent implements AfterViewInit, OnDestroy {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  @ViewChild(TerminalComponent) terminal!: TerminalComponent;
  @ViewChild('cdot') cdot!: ElementRef<HTMLDivElement>;
  @ViewChild('cring') cring!: ElementRef<HTMLDivElement>;
  @ViewChild('prvw') prvw!: ElementRef<HTMLDivElement>;

  readonly activeProject = signal<Project | null>(null);
  readonly activeBlogPost = signal<BlogPost | null>(null);
  readonly mobileMenuOpen = signal(false);
  readonly totalPages = this.data.navItems.length;

  readonly cursorHover = signal(false);
  readonly previewActive = signal<number | null>(null);

  private mx = 0; private my = 0;
  private rx = 0; private ry = 0;
  private prx = 0; private pry = 0;
  private animId = 0;

  openProject(index: number) {
    if (index === -1) { this.goTo(2); return; }
    this.activeProject.set(this.data.projects[index]);
  }
  closeProject() { this.activeProject.set(null); }
  openBlog(post: BlogPost) { this.activeBlogPost.set(post); }
  closeBlog() { this.activeBlogPost.set(null); }

  setPreview(index: number | null) {
    this.previewActive.set(index);
    if (index !== null) { this.prx = this.mx; this.pry = this.my; }
  }

  goTo(index: number) {
    if (window.innerWidth < 960) {
      // Mobile: scroll to section using window.scrollTo with 70px nav offset
      const sections = document.querySelectorAll('.page-section');
      const el = sections[index] as HTMLElement;
      if (el) {
        const top = el.getBoundingClientRect().top + window.scrollY - 70;
        window.scrollTo({ top: Math.max(0, top), behavior: 'smooth' });
      }
      this.nav.currentPage.set(index);
    } else {
      this.nav.goTo(index, this.totalPages);
    }
    this.mobileMenuOpen.set(false);
  }

  ngAfterViewInit() {
    const lerp = () => {
      this.rx += (this.mx - this.rx) * 0.11;
      this.ry += (this.my - this.ry) * 0.11;
      if (this.cring) {
        this.cring.nativeElement.style.left = `${this.rx}px`;
        this.cring.nativeElement.style.top  = `${this.ry}px`;
      }
      if (this.previewActive() !== null && this.prvw) {
        this.prx += (this.mx - this.prx) * 0.1;
        this.pry += (this.my - this.pry) * 0.1;
        this.prvw.nativeElement.style.left = `${this.prx}px`;
        this.prvw.nativeElement.style.top  = `${this.pry}px`;
      }
      this.animId = requestAnimationFrame(lerp);
    };
    lerp();
  }

  ngOnDestroy() { cancelAnimationFrame(this.animId); }

  @HostListener('document:mousemove', ['$event'])
  onMouseMove(e: MouseEvent) {
    this.mx = e.clientX;
    this.my = e.clientY;
    if (this.cdot) {
      this.cdot.nativeElement.style.left = `${this.mx}px`;
      this.cdot.nativeElement.style.top  = `${this.my}px`;
    }
    const target = e.target as HTMLElement;
    const isHoverable = target.closest('a, button, .btn, .sdw, .pi, .bc, .tmc, .feat-card, .term-trigger, .skill-toggle-btn, .cert-nav, .cert-card');
    this.cursorHover.set(!!isHoverable);
  }

  @HostListener('document:keydown', ['$event'])
  onKeyDown(e: KeyboardEvent) {
    if (this.activeProject() || this.activeBlogPost()) {
      if (e.key === 'Escape') {
        this.activeProject.set(null);
        this.activeBlogPost.set(null);
      }
      return;
    }
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    if (e.key === 'ArrowDown' || e.key === 'ArrowRight') this.goTo(Math.min(this.nav.currentPage() + 1, this.totalPages - 1));
    if (e.key === 'ArrowUp'   || e.key === 'ArrowLeft')  this.goTo(Math.max(this.nav.currentPage() - 1, 0));
    if (e.key === 'Escape' && this.nav.terminalOpen()) this.nav.terminalOpen.set(false);
  }

  private wheelTimer: any = null;
  @HostListener('wheel', ['$event'])
  onWheel(e: WheelEvent) {
    if (window.innerWidth < 960) return;
    if (this.activeProject() || this.activeBlogPost() || this.wheelTimer) return;
    if (this.mobileMenuOpen()) return;

    const activePage = document.querySelector('.page-section.active') as HTMLElement;
    if (!activePage) return;

    const atTop    = activePage.scrollTop <= 5;
    const atBottom = Math.ceil(activePage.scrollTop + activePage.clientHeight) >= activePage.scrollHeight - 5;

    if (e.deltaY > 20 && atBottom) {
      if (this.nav.currentPage() < this.totalPages - 1) {
        this.goTo(this.nav.currentPage() + 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    } else if (e.deltaY < -20 && atTop) {
      if (this.nav.currentPage() > 0) {
        this.goTo(this.nav.currentPage() - 1);
        this.wheelTimer = setTimeout(() => { this.wheelTimer = null; }, 800);
      }
    }
  }
}

--- DIRECTORY: app\components\about\about.component.css ---
/* ═══════════════════════════════════════════════════════════
   ABOUT — HOST & PAGE SHELL
   ═══════════════════════════════════════════════════════════ */
:host { display: block; min-height: 100vh; width: 100%; }

.page-about {
  width: 100%;
  min-height: 100vh;
  padding: 4rem 5rem 5rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

/* Section label */
.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 2.5rem; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

/* Animations */
.active-page .au.d0 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .04s both; }
.active-page .au.d1 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .au.d2 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .au.d3 { animation: fadeUp   .55s cubic-bezier(.22,1,.36,1) .26s both; }
.active-page .al.d1 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .10s both; }
.active-page .al.d2 { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d2 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .18s both; }
.active-page .ar.d3 { animation: fadeRight .55s cubic-bezier(.22,1,.36,1) .26s both; }

@keyframes fadeUp    { from { opacity:0; transform:translateY(36px); } to { opacity:1; transform:none; } }
@keyframes fadeLeft  { from { opacity:0; transform:translateX(-36px); } to { opacity:1; transform:none; } }
@keyframes fadeRight { from { opacity:0; transform:translateX(36px); } to { opacity:1; transform:none; } }

/* ═══════════════════════════════════════════════════════════
   MAIN TWO-COLUMN GRID
   ═══════════════════════════════════════════════════════════ */
.ag {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

/* ═══════════════════════════════════════════════════════════
   BIO — PHOTO + BADGES
   ═══════════════════════════════════════════════════════════ */
.bio-header { margin-bottom: 1.75rem; }

.bio-photo-wrap {
  display: flex;
  align-items: flex-end;
  gap: 1rem;
  margin-bottom: 1rem;
}

.bio-photo {
  width: 72px; height: 72px;
  border-radius: 50%;
  border: 2px solid var(--bord);
  overflow: hidden;
  flex-shrink: 0;
  transition: border-color .3s;
  position: relative;
}
.bio-photo:hover { border-color: var(--acc); }
.bio-photo img { width: 100%; height: 100%; object-fit: cover; }
.bio-photo-fallback {
  display: none;
  width: 100%; height: 100%;
  align-items: center; justify-content: center;
  background: var(--surf);
  color: var(--muted);
}

.bio-badges { display: flex; flex-wrap: wrap; gap: .4rem; }

.bio-badge {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--muted);
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 4px;
  padding: .3rem .65rem;
  letter-spacing: .03em;
}
.bio-badge.accent { color: var(--acc); border-color: rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   BIO TEXT
   ═══════════════════════════════════════════════════════════ */
.apull {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.6rem, 2.4vw, 2.2rem);
  font-weight: 600;
  line-height: 1.25;
  color: var(--white);
  letter-spacing: -.02em;
  margin-bottom: 1.25rem;
}
.apull em { color: var(--acc); font-style: italic; }

.abio { display: flex; flex-direction: column; gap: .9rem; margin-bottom: 1.5rem; }
.abio p { font-size: 1rem; color: var(--text); line-height: 1.75; font-weight: 300; }
.abio strong { color: var(--white); font-weight: 600; }

.bio-actions { display: flex; gap: 1rem; flex-wrap: wrap; margin-top: .25rem; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .75rem 1.5rem;
  font-family: 'Plus Jakarta Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border-radius: 6px; border: none; cursor: pointer;
  text-decoration: none; transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 24px rgba(139,92,246,.35); }

/* ═══════════════════════════════════════════════════════════
   GITHUB ACTIVITY GRID
   FIX: cell classes are gh-0..gh-4 (match getCellClass() in TS)
   ═══════════════════════════════════════════════════════════ */
.gh-section {
  margin-top: 2rem;
}

.gh-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .85rem;
}

.gh-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .12em;
  color: var(--muted);
  font-weight: 600;
  text-transform: uppercase;
}

.gh-count {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .06em;
  font-weight: 600;
}

.gh-wrap {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 8px;
  padding: 1rem 1.25rem;
  overflow-x: auto;
}

/* Month labels row */
.gh-months {
  display: flex;
  margin-bottom: .35rem;
}

.gh-row-spacer { width: 28px; flex-shrink: 0; }

.gh-month-labels {
  display: grid;
  /* 26 weeks × 13px + gaps */
  grid-template-columns: repeat(26, 13px);
  gap: 2px;
  flex: 1;
}

.gh-month-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
  grid-row: 1;
  white-space: nowrap;
}

/* Grid + day labels */
.gh-grid-wrap {
  display: flex;
  gap: 4px;
}

.gh-day-labels {
  display: flex;
  flex-direction: column;
  gap: 2px;
  width: 24px;
  flex-shrink: 0;
}

.gh-day-labels span {
  height: 13px;
  font-family: 'JetBrains Mono', monospace;
  font-size: .55rem;
  color: var(--muted);
  line-height: 13px;
  text-align: right;
  padding-right: 2px;
}

.gh-grid {
  display: flex;
  gap: 2px;
}

.gh-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

/* ─── Cells ─── */
.gh-cell {
  width: 13px; height: 13px;
  border-radius: 2px;
  background: var(--surf);
  border: 1px solid var(--bord);
  transition: transform .15s;
}
.gh-cell:hover { transform: scale(1.4); z-index: 1; position: relative; }

/* ✅ FIX: match class names returned by getCellClass() in TS */
.gh-cell.gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-cell.gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-cell.gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-cell.gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

/* Light mode */
[data-theme="light"] .gh-cell        { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-cell.gh-1   { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-cell.gh-2   { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-cell.gh-3   { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-cell.gh-4   { background: #5b21b6; border-color: #4c1d95; }

/* Legend row */
.gh-legend {
  display: flex;
  align-items: center;
  gap: 3px;
  margin-top: .75rem;
  justify-content: flex-end;
}

.gh-legend-lbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  margin: 0 2px;
}

/* Legend static cells */
.gh-legend .gh-0 { background: var(--surf); }
.gh-legend .gh-1 { background: rgba(139,92,246,.25); border-color: rgba(139,92,246,.35); }
.gh-legend .gh-2 { background: rgba(139,92,246,.48); border-color: rgba(139,92,246,.55); }
.gh-legend .gh-3 { background: rgba(139,92,246,.72); border-color: rgba(139,92,246,.8); }
.gh-legend .gh-4 { background: rgba(139,92,246,.92); border-color: rgba(139,92,246,1); }

[data-theme="light"] .gh-legend .gh-0 { background: #eaedf0; border-color: #d1d5db; }
[data-theme="light"] .gh-legend .gh-1 { background: #d1c4e9; border-color: #b39ddb; }
[data-theme="light"] .gh-legend .gh-2 { background: #9575cd; border-color: #7e57c2; }
[data-theme="light"] .gh-legend .gh-3 { background: #7c3aed; border-color: #6d28d9; }
[data-theme="light"] .gh-legend .gh-4 { background: #5b21b6; border-color: #4c1d95; }

/* ═══════════════════════════════════════════════════════════
   SKILLS BLOCK
   ═══════════════════════════════════════════════════════════ */
.blk {
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 10px;
  padding: 1.75rem;
  margin-bottom: 1.25rem;
}

.blkh {
  font-family: 'JetBrains Mono', monospace;
  font-size: .78rem;
  letter-spacing: .1em;
  text-transform: uppercase;
  color: var(--white);
  font-weight: 700;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
}

.skill-toggle-btn {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  color: var(--acc);
  background: transparent;
  border: 1px solid rgba(139,92,246,.4);
  border-radius: 4px;
  padding: .25rem .6rem;
  cursor: pointer;
  transition: background .2s, color .2s;
  flex-shrink: 0;
  white-space: nowrap;
}
.skill-toggle-btn:hover { background: rgba(139,92,246,.15); }

/* Sphere */
.skill-sphere-wrap {
  width: 100%; height: 210px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.sphere-hint {
  position: absolute;
  bottom: 6px;
  left: 50%;
  transform: translateX(-50%);
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  color: var(--muted);
  letter-spacing: .08em;
  pointer-events: none;
}

/* Skill bars */
.skl { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .9rem; }

.sk-item {
  animation: fadeUp .4s cubic-bezier(.22,1,.36,1) both;
}

.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .3rem;
}

.sk-info span:first-child { font-size: .88rem; color: var(--text); font-weight: 500; }
.sk-time { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); }

.sk-bar {
  width: 100%; height: 5px;
  background: var(--bg);
  border-radius: 4px;
  overflow: hidden;
  border: 1px solid var(--bord);
}

.sk-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--acc), rgba(167,139,250,.7));
  border-radius: 4px;
  width: 0;
  animation: barFill 1.2s cubic-bezier(.22,1,.36,1) forwards;
}

@keyframes barFill {
  from { width: 0; }
  to   { width: var(--target-width, 0%); }
}

/* ═══════════════════════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
   ═══════════════════════════════════════════════════════════ */
.cert-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
.cert-counter { font-family: 'JetBrains Mono', monospace; font-size: .72rem; color: var(--muted); letter-spacing: .06em; }
.cert-carousel { position: relative; display: flex; align-items: center; gap: .75rem; }
.cert-nav {
  width: 34px; height: 34px; border: 1px solid var(--bord); border-radius: 50%;
  display: flex; align-items: center; justify-content: center; color: var(--sub);
  font-size: 1.2rem; cursor: pointer; flex-shrink: 0; transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 2px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }
.cert-card-wrap { flex: 1; overflow: hidden; }
.cert-in  { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r{ animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity: 0; transform: translateX(30px);  } to { opacity: 1; transform: none; } }
@keyframes certInR { from { opacity: 0; transform: translateX(-30px); } to { opacity: 1; transform: none; } }
.cert-card {
  display: block; border-radius: 8px; overflow: hidden; border: 1px solid var(--bord);
  transition: border-color .25s; text-decoration: none; cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top { height: 52px; display: flex; align-items: center; padding: 0 1.1rem; gap: .7rem; }
.cert-issuer-badge {
  width: 28px; height: 28px; border-radius: 4px; display: flex; align-items: center;
  justify-content: center; font-family: 'JetBrains Mono', monospace; font-size: .6rem;
  font-weight: 700; color: #fff; flex-shrink: 0;
}
.cert-issuer-name { font-family: 'JetBrains Mono', monospace; font-size: .72rem; letter-spacing: .04em; color: var(--text); font-weight: 500; }
.cert-body { background: var(--bg); padding: 1.1rem; }
.cert-name { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1rem; letter-spacing: -.01em; color: var(--white); line-height: 1.25; margin-bottom: .45rem; }
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--muted); letter-spacing: .06em; }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .68rem; color: var(--acc); letter-spacing: .06em; font-weight: 500; }
.cert-dots { display: flex; gap: .4rem; justify-content: center; margin-top: .9rem; }
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s, transform .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); transform: scale(1.4); }

/* ═══════════════════════════════════════════════════════════
   GITHUB HEATMAP LOADING / ERROR STATES
   ═══════════════════════════════════════════════════════════ */
.gh-loading {
  display: flex; align-items: center; gap: .6rem;
  font-family: 'JetBrains Mono', monospace; font-size: .75rem;
  color: var(--muted); padding: 1.5rem 0;
}
.gh-pulse {
  width: 8px; height: 8px; border-radius: 50%; background: var(--acc);
  animation: pulse 1.2s ease-in-out infinite;
}
@keyframes pulse { 0%,100% { opacity: .3; transform: scale(.8); } 50% { opacity: 1; transform: scale(1.2); } }
.gh-error {
  padding: 1.5rem 0;
  font-family: 'JetBrains Mono', monospace; font-size: .75rem;
}
.gh-error-link { color: var(--acc); text-decoration: none; }
.gh-error-link:hover { text-decoration: underline; }

/* ═══════════════════════════════════════════════════════════
   RESUME OVERLAY
   ═══════════════════════════════════════════════════════════ */
.resume-overlay {
  position: fixed;
  inset: 0;
  z-index: 8000;
  display: flex;
  flex-direction: column;
  background: var(--bg);
  animation: slideUp .44s cubic-bezier(.22,1,.36,1) both;
}

.resume-overlay.resume-closing {
  animation: slideDown .44s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideUp   { from { transform: translateY(100%); opacity:0; } to { transform: none; opacity:1; } }
@keyframes slideDown { from { transform: none; opacity:1; } to { transform: translateY(100%); opacity:0; } }

/* Header bar */
.resume-hd {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.1rem 2rem;
  border-bottom: 1px solid var(--bord);
  background: var(--surf);
  flex-shrink: 0;
  position: sticky;
  top: 0;
  z-index: 2;
}

.resume-hd-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--muted);
  letter-spacing: .08em;
  font-weight: 600;
  text-transform: uppercase;
}

.resume-hd-right { display: flex; align-items: center; gap: 1rem; }

.resume-actions {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  padding: .5rem 1.1rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .85rem;
  font-weight: 600;
  background: var(--acc);
  color: #fff;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: transform .2s, box-shadow .2s;
}
.resume-actions:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(139,92,246,.35); }

.resume-close-btn {
  width: 36px; height: 36px;
  border: 1px solid var(--bord);
  border-radius: 6px;
  display: flex; align-items: center; justify-content: center;
  color: var(--muted);
  font-size: 1rem;
  background: transparent;
  cursor: pointer;
  transition: border-color .2s, color .2s;
}
.resume-close-btn:hover { border-color: var(--acc); color: var(--acc); }

/* Scrollable body */
.resume-body {
  flex: 1;
  overflow-y: auto;
  display: flex;
  justify-content: center;
  padding: 3rem 2rem 4rem;
  background: var(--bg);
}

/* White paper resume card */
.resume-page {
  width: 100%;
  max-width: 860px;
  background: #fff;
  color: #1a1a2e;
  border-radius: 4px;
  padding: 3rem 3.5rem;
  box-shadow: 0 4px 60px rgba(0,0,0,.25);
  font-family: 'Plus Jakarta Sans', sans-serif;
}

/* Resume internals */
.res-name-block { margin-bottom: 2rem; border-bottom: 2px solid #1a1a2e; padding-bottom: 1.25rem; }
.res-name { font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; font-weight: 700; color: #1a1a2e; margin-bottom: .25rem; letter-spacing: -.02em; }
.res-role { font-size: 1.05rem; color: #5c5c7a; font-weight: 500; margin-bottom: .5rem; }
.res-contact { display: flex; flex-wrap: wrap; gap: 1rem; font-size: .8rem; color: #5c5c7a; }
.res-contact a { color: #5b21b6; text-decoration: none; }
.res-contact a:hover { text-decoration: underline; }

.res-cols { display: grid; grid-template-columns: 1.4fr .9fr; gap: 3rem; }

.resume-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  font-weight: 700;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: #5b21b6;
  margin-bottom: .65rem;
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: .35rem;
}

.res-section { margin-bottom: 1.75rem; }
.res-text { font-size: .9rem; color: #4b5563; line-height: 1.7; font-weight: 400; }

.res-exp-item { margin-bottom: 1.25rem; }
.res-exp-header { display: flex; justify-content: space-between; align-items: baseline; gap: 1rem; margin-bottom: .2rem; }
.res-exp-title { font-weight: 700; font-size: .92rem; color: #1a1a2e; }
.res-exp-date { font-family: 'JetBrains Mono', monospace; font-size: .7rem; color: #9ca3af; flex-shrink: 0; }
.res-exp-org { font-size: .8rem; color: #6b7280; margin-bottom: .5rem; }
.res-exp-list { padding-left: 1rem; margin: 0; display: flex; flex-direction: column; gap: .3rem; }
.res-exp-list li { font-size: .85rem; color: #4b5563; line-height: 1.6; }

.res-proj-item { margin-bottom: 1rem; padding-bottom: 1rem; border-bottom: 1px dashed #e5e7eb; }
.res-proj-item:last-child { border-bottom: none; }
.res-proj-header { display: flex; justify-content: space-between; align-items: center; gap: .5rem; margin-bottom: .25rem; }
.res-proj-name { font-weight: 700; font-size: .9rem; color: #1a1a2e; }
.res-proj-tags { display: flex; gap: .3rem; flex-wrap: wrap; }
.res-tag { font-family: 'JetBrains Mono', monospace; font-size: .6rem; padding: .15rem .4rem; border-radius: 3px; background: #ede9fe; color: #5b21b6; }
.res-proj-desc { font-size: .82rem; color: #6b7280; line-height: 1.55; }

.res-skill-chips { display: flex; flex-wrap: wrap; gap: .4rem; }
.res-chip { font-family: 'JetBrains Mono', monospace; font-size: .68rem; padding: .25rem .55rem; background: #f3f4f6; border: 1px solid #e5e7eb; border-radius: 4px; color: #374151; }

.res-cert-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: .45rem; }
.res-cert-item { display: flex; align-items: center; gap: .55rem; font-size: .82rem; }
.res-cert-issuer { font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700; padding: .15rem .4rem; border-radius: 3px; flex-shrink: 0; }
.res-cert-name { flex: 1; color: #1a1a2e; font-weight: 500; font-size: .82rem; }
.res-cert-date { font-family: 'JetBrains Mono', monospace; font-size: .62rem; color: #9ca3af; flex-shrink: 0; }
.res-cert-more { font-style: italic; color: #9ca3af; font-size: .8rem; }
.res-cert-count { font-size: .68rem; background: #5b21b6; color: #fff; padding: .1rem .4rem; border-radius: 8px; }

/* Print */
@media print {
  .resume-hd, app-root > * { display: none !important; }
  .resume-overlay { position: static !important; animation: none !important; }
  .resume-body { padding: 0; overflow: visible; }
  .resume-page { box-shadow: none; border-radius: 0; padding: 2rem; }
}

/* ═══════════════════════════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-about { padding: 5.5rem 1.5rem 4rem; }
  .ag { grid-template-columns: 1fr; gap: 2.5rem; }
  .cert-grid { grid-template-columns: 1fr 1fr; max-height: none; }
  .res-cols { grid-template-columns: 1fr; }
  .resume-page { padding: 2rem 1.5rem; }
  .resume-hd { padding: 1rem 1.25rem; }
  .skill-toggle-btn { cursor: pointer !important; }
}

@media (max-width: 480px) {
  .cert-grid { grid-template-columns: 1fr; }
}


--- DIRECTORY: app\components\about\about.component.html ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid — real data fetched from API -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="gh-count">{{ heatmapTotal() }} CONTRIBUTIONS</span>
            }
          </div>

          @if (heatmapLoading()) {
            <div class="gh-loading">
              <span class="gh-pulse"></span> Loading contributions...
            </div>
          } @else if (heatmapError()) {
            <div class="gh-error">
              <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="gh-error-link">
                View on GitHub ↗
              </a>
            </div>
          } @else {
            <div class="gh-wrap">
              <!-- Month labels -->
              <div class="gh-months">
                <div class="gh-row-spacer"></div>
                <div class="gh-month-labels">
                  @for (m of monthLabels(); track m.col) {
                    <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                  }
                </div>
              </div>

              <!-- Day labels + grid -->
              <div class="gh-grid-wrap">
                <div class="gh-day-labels">
                  <span></span>
                  <span>Mon</span>
                  <span></span>
                  <span>Wed</span>
                  <span></span>
                  <span>Fri</span>
                  <span></span>
                </div>
                <div class="gh-grid">
                  @for (week of heatmapWeeks(); track $index) {
                    <div class="gh-col">
                      @for (day of week; track day.date) {
                        <div class="gh-cell" [class]="getCellClass(day.level)"
                             [title]="day.count > 0 ? day.count + ' contribution' + (day.count > 1 ? 's' : '') + ' on ' + day.date : 'No contributions on ' + day.date">
                        </div>
                      }
                    </div>
                  }
                </div>
              </div>

              <!-- Legend -->
              <div class="gh-legend">
                <span class="gh-legend-lbl">Less</span>
                <div class="gh-cell gh-0"></div>
                <div class="gh-cell gh-1"></div>
                <div class="gh-cell gh-2"></div>
                <div class="gh-cell gh-3"></div>
                <div class="gh-cell gh-4"></div>
                <span class="gh-legend-lbl">More</span>
              </div>
            </div>
          }
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — carousel -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNavTo($index)"
                [attr.aria-label]="'Go to cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: app\components\about\about.component.ts ---
<section class="page-about" [class.active-page]="active">
  <div class="pin">

    <!-- ══ Section label ═══════════════════════════════════════════════════ -->
    <div class="slabel au d0">
      <span class="sn">02</span><span class="st">/ about me</span>
      <div class="shr"></div>
    </div>

    <!-- ══ Main two-column grid ═════════════════════════════════════════════ -->
    <div class="ag">

      <!-- LEFT: bio + GitHub activity -->
      <div>

        <!-- Bio header with photo -->
        <div class="bio-header al d1">
          <div class="bio-photo-wrap">
            <div class="bio-photo">
              <!-- Replace src with your actual photo path: src/assets/profile.jpg -->
              <img src="assets/profile.jpg" alt="Jaren Kendrick"
                   onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" />
              <div class="bio-photo-fallback">
                <svg width="48" height="48" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                </svg>
              </div>
            </div>
            <div class="bio-badges">
              <span class="bio-badge">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                Pampanga, PH
              </span>
              <span class="bio-badge">🎓 IT Student · HAU</span>
              <span class="bio-badge accent">20 yrs old</span>
            </div>
          </div>
        </div>

        <h2 class="apull al d1">From <em>idea</em> to shipped product —<br>full stack, no shortcuts.</h2>

        <div class="abio au d2">
          <p>I'm a <strong>20-year-old IT student</strong> at Holy Angel University in Pampanga, Philippines, specializing in Web Development. I build complete systems — from secure database schemas and REST APIs to clean, intuitive frontends.</p>
          <p>While I currently work part-time as a Game Developer at VirtuIntelligence, programming complex systems in <strong>C# (Unity)</strong> has deeply shaped my backend thinking, OOP design, and architectural instincts.</p>
          <p>I don't just write code — I ship things. Looking for a full-stack internship where I can contribute to real-world codebases and keep learning fast.</p>

          <div class="bio-actions">
            <button class="btn btn-fill" (click)="openResume()">
              <svg style="width:16px;height:16px;fill:none;stroke:currentColor;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;" viewBox="0 0 24 24">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
                <polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/>
                <line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
              </svg>
              View Resume
            </button>
          </div>
        </div>

        <!-- GitHub Activity Grid — real data fetched from API -->
        <div class="gh-section au d3">
          <div class="gh-header">
            <span class="gh-title">GITHUB ACTIVITY</span>
            @if (!heatmapLoading() && !heatmapError()) {
              <span class="gh-count">{{ heatmapTotal() }} CONTRIBUTIONS</span>
            }
          </div>

          @if (heatmapLoading()) {
            <div class="gh-loading">
              <span class="gh-pulse"></span> Loading contributions...
            </div>
          } @else if (heatmapError()) {
            <div class="gh-error">
              <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="gh-error-link">
                View on GitHub ↗
              </a>
            </div>
          } @else {
            <div class="gh-wrap">
              <!-- Month labels -->
              <div class="gh-months">
                <div class="gh-row-spacer"></div>
                <div class="gh-month-labels">
                  @for (m of monthLabels(); track m.col) {
                    <span class="gh-month-lbl" [style.grid-column]="m.col + 1">{{ m.label }}</span>
                  }
                </div>
              </div>

              <!-- Day labels + grid -->
              <div class="gh-grid-wrap">
                <div class="gh-day-labels">
                  <span></span>
                  <span>Mon</span>
                  <span></span>
                  <span>Wed</span>
                  <span></span>
                  <span>Fri</span>
                  <span></span>
                </div>
                <div class="gh-grid">
                  @for (week of heatmapWeeks(); track $index) {
                    <div class="gh-col">
                      @for (day of week; track day.date) {
                        <div class="gh-cell" [class]="getCellClass(day.level)"
                             [title]="day.count > 0 ? day.count + ' contribution' + (day.count > 1 ? 's' : '') + ' on ' + day.date : 'No contributions on ' + day.date">
                        </div>
                      }
                    </div>
                  }
                </div>
              </div>

              <!-- Legend -->
              <div class="gh-legend">
                <span class="gh-legend-lbl">Less</span>
                <div class="gh-cell gh-0"></div>
                <div class="gh-cell gh-1"></div>
                <div class="gh-cell gh-2"></div>
                <div class="gh-cell gh-3"></div>
                <div class="gh-cell gh-4"></div>
                <span class="gh-legend-lbl">More</span>
              </div>
            </div>
          }
        </div>

      </div>

      <!-- RIGHT: skills + certs -->
      <div>

        <!-- SKILLS SECTION -->
        <div class="blk ar d2">
          <div class="blkh">
            Technical Skills
            <button class="skill-toggle-btn" (click)="toggleSkills()">
              @if (skillsExpanded()) { ← Show Sphere } @else { Show Proficiency → }
            </button>
          </div>

          <!-- 3D Sphere -->
          <div class="skill-sphere-wrap" #sphereWrap [style.display]="skillsExpanded() ? 'none' : 'flex'">
            <div #sphereContainer style="position:absolute;top:50%;left:50%;width:0;height:0;"></div>
            <div class="sphere-hint">drag to spin</div>
          </div>

          <!-- Skill bars -->
          @if (skillsExpanded() && barsReady()) {
            <ul class="skl" role="list">
              @for (sk of skills; track sk.name; let i = $index) {
                <li class="sk-item" [style.animation-delay]="(i * 0.08) + 's'">
                  <div class="sk-info">
                    <span>{{ sk.name }}</span>
                    <span class="sk-time">{{ sk.time }}</span>
                  </div>
                  <div class="sk-bar">
                    <div class="sk-fill"
                         [style.--target-width]="sk.pct + '%'"
                         [style.animation-delay]="(i * 0.08 + 0.15) + 's'">
                    </div>
                  </div>
                </li>
              }
            </ul>
          }
        </div>

        <!-- CERT SECTION — carousel -->
        <div class="blk ar d3">
          <div class="cert-header">
            <div class="blkh" style="margin-bottom:0;">Certifications</div>
            <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
          </div>
          <div class="cert-carousel">
            <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous">‹</button>
            <div class="cert-card-wrap" [ngClass]="certAnimClass()">
              <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
                <div class="cert-top" [style.background]="currentCert.color + '22'">
                  <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                  <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
                </div>
                <div class="cert-body">
                  <div class="cert-name">{{ currentCert.name }}</div>
                  <div class="cert-meta">
                    <div class="cert-date">{{ currentCert.date }}</div>
                    <div class="cert-view">View ↗</div>
                  </div>
                </div>
              </a>
            </div>
            <button class="cert-nav" (click)="certNav(1)" aria-label="Next">›</button>
          </div>
          <div class="cert-dots">
            @for (cert of data.certificates; track $index) {
              <button class="cert-dot" [class.on]="$index === certIndex()"
                (click)="certNavTo($index)"
                [attr.aria-label]="'Go to cert ' + ($index + 1)">
              </button>
            }
          </div>
        </div>

      </div>
    </div>
  </div>
</section>


<!-- ══ RESUME OVERLAY ════════════════════════════════════════════════════════ -->
@if (showResume()) {
  <div class="resume-overlay" [class.resume-closing]="resumeClosing()">

    <!-- Header bar -->
    <div class="resume-hd">
      <div class="resume-hd-left">
        <span class="resume-hd-label">Resume — Jaren Kendrick</span>
      </div>
      <div class="resume-hd-right">
        <button class="resume-actions" (click)="printResume()">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <polyline points="6 9 6 2 18 2 18 9"/><polyline points="6 18 4 18 4 9 20 9 20 18 18 18"/>
            <rect x="6" y="14" width="12" height="8"/>
          </svg>
          Download PDF
        </button>
        <button class="resume-close-btn" (click)="closeResume()" aria-label="Close resume">✕</button>
      </div>
    </div>

    <!-- Resume body -->
    <div class="resume-body">
      <div class="resume-page">

        <!-- Name + contact -->
        <div class="res-name-block">
          <h1 class="res-name">Jaren Kendrick Yambao</h1>
          <p class="res-role">Full Stack Developer &amp; IT Student</p>
          <div class="res-contact">
            <span>📍 Pampanga, Philippines</span>
            <span>🎓 Holy Angel University</span>
            <span>
              <a href="https://github.com/jarenkendrick14" target="_blank">github.com/jarenkendrick14</a>
            </span>
          </div>
        </div>

        <div class="res-cols">

          <!-- LEFT col -->
          <div class="res-left">

            <!-- Summary -->
            <div class="res-section">
              <h2 class="resume-section-title">Profile</h2>
              <p class="res-text">20-year-old IT student specializing in Web Development at Holy Angel University. Part-time Game Developer at VirtuIntelligence. Builds end-to-end web applications using Angular, Node.js, and MongoDB, with strong C# and Unity backgrounds informing backend and architectural decisions.</p>
            </div>

            <!-- Experience -->
            <div class="res-section">
              <h2 class="resume-section-title">Experience</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">Game Developer (Part-Time)</span>
                  <span class="res-exp-date">Sep 2024 — Present</span>
                </div>
                <div class="res-exp-org">VirtuIntelligence · Remote, PH</div>
                <ul class="res-exp-list">
                  <li>Shipped PathFinder VR app to Meta Quest store using Unreal Engine 5.5 Blueprints</li>
                  <li>Built companion Android app in Unity (C#) for cross-platform deployment</li>
                  <li>Integrated Wit.ai voice-to-text and local data persistence for privacy-first design</li>
                </ul>
              </div>
            </div>

            <!-- Projects -->
            <div class="res-section">
              <h2 class="resume-section-title">Key Projects</h2>
              @for (proj of data.projects.slice(0, 4); track proj.num) {
                <div class="res-proj-item">
                  <div class="res-proj-header">
                    <span class="res-proj-name">{{ proj.name }}</span>
                    <div class="res-proj-tags">
                      @for (tag of proj.tags.slice(0,3); track tag) {
                        <span class="res-tag">{{ tag }}</span>
                      }
                    </div>
                  </div>
                  <p class="res-proj-desc">{{ proj.purpose }}</p>
                </div>
              }
            </div>

          </div>

          <!-- RIGHT col -->
          <div class="res-right">

            <!-- Education -->
            <div class="res-section">
              <h2 class="resume-section-title">Education</h2>
              <div class="res-exp-item">
                <div class="res-exp-header">
                  <span class="res-exp-title">BS Information Technology</span>
                  <span class="res-exp-date">2023 — 2027</span>
                </div>
                <div class="res-exp-org">Holy Angel University · Pampanga, PH</div>
                <p class="res-text" style="margin-top:.4rem;">Specialization: Web Development. Coursework in full-stack architecture, databases, software engineering.</p>
              </div>
            </div>

            <!-- Skills -->
            <div class="res-section">
              <h2 class="resume-section-title">Technical Skills</h2>
              <div class="res-skill-chips">
                @for (sk of skills; track sk.name) {
                  <span class="res-chip">{{ sk.name }}</span>
                }
                <span class="res-chip">Unity</span>
                <span class="res-chip">Unreal Engine 5</span>
                <span class="res-chip">REST API Design</span>
                <span class="res-chip">Git / GitHub</span>
                <span class="res-chip">Netlify</span>
              </div>
            </div>

            <!-- Certifications -->
            <div class="res-section">
              <h2 class="resume-section-title">Certifications <span class="res-cert-count">{{ data.certificates.length }}</span></h2>
              <ul class="res-cert-list">
                @for (cert of data.certificates; track cert.name; let i = $index) {
                  @if (i < 8) {
                    <li class="res-cert-item">
                      <span class="res-cert-issuer" [style.background]="cert.color + '22'" [style.color]="cert.color">{{ cert.abbr }}</span>
                      <span class="res-cert-name">{{ cert.name }}</span>
                      <span class="res-cert-date">{{ cert.date }}</span>
                    </li>
                  }
                }
                @if (data.certificates.length > 8) {
                  <li class="res-cert-more">+{{ data.certificates.length - 8 }} more certifications</li>
                }
              </ul>
            </div>

          </div>
        </div>

      </div>
    </div>

  </div>
}

--- DIRECTORY: app\components\blog\blog.component.css ---
.page-blog {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader { display: flex; align-items: baseline; gap: 2rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.ph2 { font-family: 'Cormorant Garamond', serif; font-weight: 600; font-size: clamp(3rem, 5vw, 4.5rem); letter-spacing: -.04em; color: var(--white); line-height: 1; }
.pcnt { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .1em; }
.phr { flex: 1; height: 1px; background: var(--bord); }

.blog-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2.5rem; }

.bc {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  transition: transform .25s, border-color .25s;
  cursor: pointer;
  position: relative;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
}
.bc:first-child  { animation-delay: .10s; }
.bc:nth-child(2) { animation-delay: .18s; }
.bc:hover { transform: translateY(-5px); border-color: var(--acc); }

.bc-img { height: 200px; background: var(--bg); border-bottom: 1px solid var(--bord); overflow: hidden; }

.bc-body {
  padding: 2rem;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.bc-date    { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--acc); letter-spacing: .08em; margin-bottom: 1rem; }
.bc-title   { font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600; font-size: 1.5rem; letter-spacing: -.01em; color: var(--white); line-height: 1.3; margin-bottom: 1rem; }
.bc-excerpt { font-size: 1.05rem; color: var(--text); line-height: 1.75; font-weight: 300; margin-bottom: 1.5rem; flex: 1; }
.bc-read    { font-size: .95rem; font-weight: 600; color: var(--acc); display: inline-flex; align-items: center; gap: .4rem; transition: gap .2s; }
.bc:hover .bc-read { gap: .75rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-blog { padding: 6.5rem 1.5rem 4rem; }
  .blog-grid { grid-template-columns: 1fr; }
}

--- DIRECTORY: app\components\blog\blog.component.html ---
<section class="page-blog" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Blog.</h2>
      <span class="pcnt">02 posts</span>
      <div class="phr"></div>
    </div>
    <div class="blog-grid">
      @for (post of data.blogPosts; track post.title; let i = $index) {
        <article class="bc as" [ngClass]="i === 0 ? 'd1' : 'd2'"
                 (click)="postOpened.emit(post)" role="button" tabindex="0"
                 (keydown.enter)="postOpened.emit(post)">
          <div class="bc-img">
            @if (i === 0) {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b1g" cx="30%" cy="50%"><stop offset="0%" stop-color="#a78bfa" stop-opacity=".2"/><stop offset="100%" stop-color="#a78bfa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b1g)"/>
                <circle cx="90" cy="95" r="52" fill="none" stroke="#a78bfa" stroke-width=".8" stroke-dasharray="5 9" opacity=".4"/>
                <text x="90" y="101" text-anchor="middle" fill="#a78bfa" font-size="22" opacity=".55">🥽</text>
                <text x="220" y="82" fill="#a78bfa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">GAME DEV → WEB DEV</text>
              </svg>
            } @else {
              <svg width="100%" height="190" viewBox="0 0 400 190" xmlns="http://www.w3.org/2000/svg">
                <rect width="400" height="190" fill="var(--bg)"/>
                <defs><radialGradient id="b2g" cx="70%" cy="50%"><stop offset="0%" stop-color="#60a5fa" stop-opacity=".18"/><stop offset="100%" stop-color="#60a5fa" stop-opacity="0"/></radialGradient></defs>
                <rect width="400" height="190" fill="url(#b2g)"/>
                <rect x="225" y="28" width="130" height="90" rx="6" fill="none" stroke="#60a5fa" stroke-width=".8" opacity=".4"/>
                <text x="52" y="80" fill="#60a5fa" font-family="monospace" font-size="10" letter-spacing="1.5" opacity=".4">VUE.JS vs VANILLA</text>
              </svg>
            }
          </div>
          <div class="bc-body">
            <p class="bc-date">{{ post.date }}</p>
            <h3 class="bc-title">{{ post.title }}</h3>
            <p class="bc-excerpt">{{ post.excerpt }}</p>
            <span class="bc-read">Read Post →</span>
          </div>
        </article>
      }
    </div>
  </div>
</section>

--- DIRECTORY: app\components\blog\blog.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './blog.component.html',
  styleUrl: './blog.component.css',
})
export class BlogComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  postOpened = output<BlogPost>();
}

--- DIRECTORY: app\components\blog-overlay\blog-overlay.component.css ---
.blog-overlay {
  position: fixed;
  inset: 0;
  z-index: 9100;
  background: var(--bg);
  border-top: 2px solid var(--acc);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  animation: slideUp .5s cubic-bezier(.76,0,.24,1) both;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.blog-overlay::-webkit-scrollbar { width: 6px; }
.blog-overlay::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

.blog-overlay.closing {
  animation: slideDown .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}

.bp-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 5rem;
  border-bottom: 1px solid var(--bord);
  position: sticky;
  top: 0;
  background: var(--bg);
  z-index: 1;
}

.bp-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--muted);
  letter-spacing: .1em;
  font-weight: 600;
}

.bp-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--sub);
  font-size: 1.1rem;
  cursor: pointer;
  background: none;
  transition: border-color .2s, color .2s;
}
.bp-close:hover { border-color: var(--acc); color: var(--acc); }

.bp-body {
  padding: 3.5rem 5rem;
  max-width: 900px;
  margin: 0 auto;
  width: 100%;
}

.bp-hero-img {
  width: 100%;
  height: 320px;
  border-radius: 8px;
  border: 1px solid var(--bord);
  background: var(--surf);
  margin-bottom: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
}

.bp-date {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: 1rem;
}

.bp-title {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(2rem, 4vw, 3.2rem);
  letter-spacing: -.03em;
  color: var(--white);
  line-height: 1.08;
  margin-bottom: 2.5rem;
}

.bp-content {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.9;
  font-weight: 300;
}

:host ::ng-deep .bp-content p { margin-bottom: 1.25rem; }
:host ::ng-deep .bp-content h3 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.025em;
  color: var(--white);
  margin: 2rem 0 .75rem;
}
:host ::ng-deep .bp-content strong { color: var(--white); font-weight: 600; }
:host ::ng-deep .bp-content em { color: var(--acc); font-style: italic; }
:host ::ng-deep .bp-content code {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9em;
  color: var(--acc);
  background: var(--surf);
  padding: .1rem .35rem;
  border-radius: 3px;
}

@media (max-width: 960px) {
  .bp-top { padding: 1.5rem; }
  .bp-body { padding: 2rem 1.5rem; }
}


--- DIRECTORY: app\components\blog-overlay\blog-overlay.component.html ---
 @if (post()) {
  <div class="blog-overlay" [class.closing]="isClosing()" role="dialog" aria-modal="true" aria-label="Blog post">
  ...
    <div class="bp-top">
      <div class="bp-label">FROM THE DESK</div>
      <button class="bp-close" (click)="close()" aria-label="Close post">✕</button>
    </div>
    <div class="bp-body">
      <div class="bp-hero-img">
        <svg viewBox="0 0 24 24" width="48" height="48" fill="currentColor" opacity="0.5">
          <path d="M21 19V5c0-1.1-.9-2-2-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2zM8.5 13.5l2.5 3.01L14.5 12l4.5 6H5l3.5-4.5z"/>
        </svg>
      </div>
      <p class="bp-date">{{ post()!.date }}</p>
      <h2 class="bp-title">{{ post()!.title }}</h2>
      <div class="bp-content" [innerHTML]="post()!.content"></div>
    </div>
  </div>
}

--- DIRECTORY: app\components\blog-overlay\blog-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { BlogPost } from '../../models/portfolio.models';

@Component({
  selector: 'app-blog-overlay',
  standalone: true,
  templateUrl: './blog-overlay.component.html',
  styleUrl: './blog-overlay.component.css',
})
export class BlogOverlayComponent {
  post = input<BlogPost | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500);
  }
}

--- DIRECTORY: app\components\contact\contact.component.css ---
.page-contact {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.cho {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(4rem, 9vw, 6rem);
  line-height: 1;
  letter-spacing: -.02em;
  color: var(--white);
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both;
}

.cgg { display: grid; grid-template-columns: 1fr 1.5fr; gap: 6rem; width: 100%; }

.cp {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 2.5rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.socl { list-style: none; display: flex; flex-direction: column; gap: 1.25rem; padding: 0; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both; }

.solink {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  font-size: 1.05rem;
  color: var(--white);
  font-weight: 500;
  transition: color .2s;
  text-decoration: none;
}
.solink:hover { color: var(--acc); }

.soico {
  width: 48px; height: 48px;
  border-radius: 6px;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: border-color .2s;
}
.solink:hover .soico { border-color: var(--acc); }

/* Filled icons (LinkedIn, GitHub) */
.soico svg { width: 22px; height: 22px; fill: currentColor; }

/* Stroke-based envelope icon override */
.soico-mail svg {
  fill: none;
  stroke: currentColor;
  stroke-width: 2;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.cform {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  background: var(--surf);
  padding: 3rem;
  border: 1px solid var(--bord);
  border-radius: 8px;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .18s ease both;
}

.frow { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
.fg   { display: flex; flex-direction: column; gap: .6rem; }

label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  font-weight: 600;
  color: var(--white);
  text-transform: uppercase;
  letter-spacing: .05em;
}

input, textarea {
  background: var(--bg);
  border: 1px solid var(--bord);
  border-radius: 6px;
  padding: 1.1rem 1.25rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1.05rem;
  font-weight: 300;
  color: var(--white);
  outline: none;
  width: 100%;
  resize: vertical;
  transition: border-color .2s;
}
input::placeholder, textarea::placeholder { color: var(--muted); }
input:focus, textarea:focus { border-color: var(--acc); }
textarea { min-height: 150px; }

.btn {
  display: inline-flex; align-items: center; gap: .6rem; justify-content: center;
  padding: 1rem 2rem; font-family: 'Plus Jakarta Sans', sans-serif; font-size: 1rem;
  font-weight: 600; border-radius: 6px; cursor: pointer; transition: transform .2s, box-shadow .2s;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }

.fsok {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  padding: 1rem 1.25rem;
  border: 1px solid rgba(139,92,246,.3);
  border-radius: 6px;
  animation: fadeUp .3s ease both;
}

@keyframes fadeUp { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 960px) {
  .page-contact { padding: 6.5rem 1.5rem 4rem; }
  .cgg { grid-template-columns: 1fr; gap: 3.5rem; }
  .frow { grid-template-columns: 1fr; }
  .cform { padding: 1.5rem; }
  .cho { font-size: clamp(3.5rem, 12vw, 5.5rem); }
}

--- DIRECTORY: app\components\contact\contact.component.html ---
<section class="page-contact" [class.active-page]="active">
  <div class="pin">
    <h2 class="cho au d0">Hire Me.</h2>
    <div class="cgg">
      <div class="au d1">
        <p class="cp">I'm actively looking for a full-stack internship. If you're building something and need someone who understands both the database and the interface — reach out. I prioritize clean code and prompt communication.</p>
        <ul class="socl" role="list">
          <li>
            <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
              </span>
              linkedin.com/in/jarenkendrick
            </a>
          </li>
          <li>
            <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="solink">
              <span class="soico">
                <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
              </span>
              github.com/jarenkendrick14
            </a>
          </li>
          <li>
            <!-- Envelope mail icon -->
            <a href="mailto:jarenkendrickyambao@gmail.com" class="solink">
              <span class="soico soico-mail">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <rect x="2" y="4" width="20" height="16" rx="2"/>
                  <path d="M22 4L12 13L2 4"/>
                </svg>
              </span>
              jarenkendrickyambao@gmail.com
            </a>
          </li>
        </ul>
      </div>

      <div class="cform au d2">
        <div class="frow">
          <div class="fg">
            <label for="fn">Name</label>
            <input type="text" id="fn" placeholder="Your name" [value]="formData().name" (input)="updateField('name', $any($event.target).value)" autocomplete="name"/>
          </div>
          <div class="fg">
            <label for="fe">Email</label>
            <input type="email" id="fe" placeholder="you@company.com" [value]="formData().email" (input)="updateField('email', $any($event.target).value)" autocomplete="email"/>
          </div>
        </div>
        <div class="fg">
          <label for="fs">Subject</label>
          <input type="text" id="fs" placeholder="Internship inquiry, collaboration..." [value]="formData().subject" (input)="updateField('subject', $any($event.target).value)"/>
        </div>
        <div class="fg">
          <label for="fm">Message</label>
          <textarea id="fm" placeholder="Tell me about what you're building..." [value]="formData().message" (input)="updateField('message', $any($event.target).value)"></textarea>
        </div>
        <button type="button" class="btn btn-fill" (click)="submit()" style="width:100%;">Send Message</button>
        @if (submitted()) {
          <div class="fsok" role="status" aria-live="polite">✓ Sent — I'll get back to you soon.</div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: app\components\contact\contact.component.ts ---
import { Component, signal, Input } from '@angular/core';
import { FormsModule } from '@angular/forms';

@Component({
  selector: 'app-contact',
  standalone: true,
  imports: [FormsModule],
  templateUrl: './contact.component.html',
  styleUrl: './contact.component.css',
})
export class ContactComponent {
  @Input() active = false;
  readonly submitted = signal(false);

  readonly formData = signal({
    name: '',
    email: '',
    subject: '',
    message: '',
  });

  updateField(field: string, value: string) {
    this.formData.update(d => ({ ...d, [field]: value }));
  }

  submit() {
    const d = this.formData();
    if (!d.name || !d.email || !d.message) return;
    const body = encodeURIComponent(`Hi Jaren,\n\n${d.message}\n\nFrom: ${d.name}\nEmail: ${d.email}`);
    const subject = encodeURIComponent(d.subject || 'Portfolio Inquiry');
    window.location.href = `mailto:jarenkendrickyambao@gmail.com?subject=${subject}&body=${body}`;
    this.submitted.set(true);
    setTimeout(() => {
      this.submitted.set(false);
      this.formData.set({ name: '', email: '', subject: '', message: '' });
    }, 4000);
  }
}

--- DIRECTORY: app\components\experience\experience.component.css ---
/* ════ experience.component.css ════ */
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-experience {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tl { display: flex; flex-direction: column; }

.tlr {
  display: grid;
  grid-template-columns: 220px 1fr;
  gap: 4rem;
  padding: 3rem 0;
  border-bottom: 1px solid var(--bord);
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}
.tlr:last-child { border-bottom: none; padding-bottom: 0; }
.tlr:first-child { padding-top: 0; animation-delay: .18s; }
.tlr:nth-child(2) { animation-delay: .26s; }

.tlpd {
  font-family: 'JetBrains Mono', monospace;
  font-size: .9rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .5rem;
  line-height: 1.4;
  font-weight: 500;
}

.tlorg {
  font-size: 1rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.tltl {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.6rem;
  letter-spacing: -.01em;
  color: var(--white);
  margin-bottom: 1rem;
}

.tldc {
  font-size: 1.1rem;
  color: var(--text);
  line-height: 1.8;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.tlch { display: flex; flex-wrap: wrap; gap: .5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

@media (max-width: 960px) {
  .page-experience { padding: 6.5rem 1.5rem 4rem; }
  .tlr { grid-template-columns: 1fr; gap: 1rem; }
}

--- DIRECTORY: app\components\experience\experience.component.html ---
<section class="page-experience" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">04</span><span class="st">/ experience &amp; education</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Where I've been.</h2>
    <div class="tl">
      @for (entry of entries; track entry.title; let i = $index) {
        <div class="tlr" [ngClass]="i === 0 ? 'al d2' : 'ar d3'">
          <div>
            <p class="tlpd">{{ entry.period }}</p>
            <p class="tlorg">{{ entry.org }}</p>
          </div>
          <div>
            <h3 class="tltl">{{ entry.title }}</h3>
            <p class="tldc" [innerHTML]="entry.desc"></p>
            @if (entry.tags.length > 0) {
              <div class="tlch">
                @for (tag of entry.tags; track tag) {
                  <span class="ptag">{{ tag }}</span>
                }
              </div>
            }
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: app\components\experience\experience.component.ts ---
import { Component, Input } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-experience',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './experience.component.html',
  styleUrl: './experience.component.css',
})
export class ExperienceComponent {
  @Input() active = false;
  readonly entries = [
    {
      period: 'Apr 2025 — Present',
      org: 'VirtuIntelligence · Part-time · Remote',
      title: 'Game/App Developer',
      desc: 'Shipped PathFinder, a VR self-discovery app, to the Meta Quest store using Unreal Engine 5.5 Blueprints and Unity. Currently developing <em>Robots vs Aliens</em>, an action VR title in Unity (C#). Built Symposium, a real-time AI-mediated chat platform using Angular, Ionic, Node.js, and PostgreSQL. Working across diverse tech stacks has strengthened my full-stack architecture and problem-solving skills.',
      tags: ['Unreal 5.5', 'Blueprints', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL'],
    },
    {
      period: 'Jun 2023 — 2027',
      org: 'Holy Angel University · Pampanga, PH',
      title: 'BS Information Technology',
      desc: 'Specialization in Web Development. Coursework spanning full-stack architecture, database systems, software engineering principles, and deployment workflows.',
      tags: [],
    },
  ];
}

--- DIRECTORY: app\components\home\home.component.css ---
.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.page-home {
  position: relative;
  width: 100%;
  /* Use 100% instead of min-height:100vh — the page-section wrapper enforces height */
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  overflow-y: auto;
}

#bg-canvas {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  pointer-events: all;
}

.h-top { position: relative; z-index: 1; padding-top: 1rem; }

.h-top-grid {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 4rem;
  margin-bottom: 5rem;
}

.h-text { flex: 1; max-width: 650px; }

.hname {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  line-height: 1;
  letter-spacing: -.03em;
  color: var(--white);
  font-size: clamp(4rem, 8vw, 5.5rem);
  margin-bottom: .8rem;
}
.accent-name { margin-bottom: 1.2rem; }

.hrole {
  font-family: 'JetBrains Mono', monospace;
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--acc);
  margin-bottom: 1.5rem;
  display: block;
}

.hdesc {
  font-size: 1.15rem;
  color: var(--text);
  line-height: 1.75;
  margin-bottom: 2rem;
  font-weight: 300;
}
.hdesc strong { color: var(--white); font-weight: 600; }

.h-meta {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.hloc {
  display: inline-flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--white);
  padding: .65rem 1.1rem;
  background: var(--surf);
  border: 1px solid var(--bord);
  border-radius: 6px;
}

.hbtns {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.h-photo {
  width: 220px;
  height: 220px;
  flex-shrink: 0;
}

.h-photo-placeholder {
  width: 100%;
  height: 100%;
  border: 2px dashed var(--bord);
  border-radius: 50%;
  background: var(--surf);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  overflow: hidden;
  transition: border-color .3s;
  position: relative;
}
.h-photo-placeholder:hover { border-color: var(--acc); border-style: solid; }
/* Hide fallback icon once real image loads */
.h-photo-placeholder img + .h-photo-icon { display: none; }
.h-photo-placeholder img:not([style*="display: none"]) + .h-photo-icon { display: none; }

.feat-section { position: relative; z-index: 1; }

.feat-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .14em;
  color: var(--muted);
  text-transform: uppercase;
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: .75rem;
  font-weight: 500;
}
.feat-label::after { content: ''; flex: 1; height: 1px; background: var(--bord); }

.feat-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.feat-card {
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--surf);
  overflow: hidden;
  cursor: none;
  transition: border-color .25s, transform .25s;
  position: relative;
}
.feat-card:hover { border-color: var(--acc); transform: translateY(-4px); }

.feat-thumb {
  height: 120px;
  overflow: hidden;
  position: relative;
}

.feat-body {
  padding: 1.25rem;
  position: relative;
  z-index: 1;
}

.feat-name {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -.01em;
  color: var(--white);
  line-height: 1;
  margin-bottom: .5rem;
}

.feat-desc {
  font-size: .85rem;
  color: var(--text);
  line-height: 1.55;
  margin-bottom: 1rem;
  font-weight: 300;
}

.feat-tags { display: flex; gap: .4rem; flex-wrap: wrap; }

.feat-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  padding: .2rem .5rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--acc);
  border: 1px solid var(--bord);
}

.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  justify-content: center;
  padding: 1rem 2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: none;
  transition: transform .2s ease, box-shadow .2s ease;
  text-decoration: none;
  border: none;
}
.btn:hover { transform: translateY(-3px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(139,92,246,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }
@keyframes fadeRight { from { opacity: 0; transform: translateX(44px); }  to { opacity: 1; transform: translateX(0); } }
@keyframes scaleIn   { from { opacity: 0; transform: scale(.88); }        to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  /* FIX: was padding 7rem 1.5rem 4rem — the big bottom padding caused extra space.
     Reduce to a more modest amount. Use padding-bottom: 2rem. */
  .page-home { padding: 7rem 1.5rem 2rem; }
  .h-top-grid { flex-direction: column-reverse; align-items: flex-start; gap: 2.5rem; margin-bottom: 3rem; }
  .h-photo { width: 160px; height: 160px; }
  .feat-grid { grid-template-columns: 1fr; }
  .btn { cursor: pointer !important; }
}

--- DIRECTORY: app\components\home\home.component.html ---
<section class="page-home" [class.active-page]="active">
  <canvas id="bg-canvas" aria-hidden="true"></canvas>

  <div class="pin h-top">
    <div class="h-top-grid">
      <div class="h-text">
        <h1 class="hname au d1" [appDecrypt]="'Hello, I\'m'" [active]="active" [delay]="400">Hello, I'm</h1>
        <h1 class="hname accent-name au d1" [appDecrypt]="'Jaren Kendrick.'" [active]="active" [delay]="600">Jaren Kendrick.</h1>
        <span class="hrole au d2">Full Stack Web Developer</span>
        <p class="hdesc au d3">
          I'm a 20 year old college student that builds end-to-end web applications, specializing in <strong>Angular</strong> for the frontend
          and robust, scalable architectures on the backend with <strong>Node.js and Express</strong>.
          I focus on writing clean code and shipping products that solve real problems.
        </p>
        <div class="h-meta au d4">
          <div class="hloc">
            <svg style="width:16px;height:16px;fill:currentColor;" viewBox="0 0 24 24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
            Pampanga, Philippines
          </div>
        </div>
        <div class="hbtns au d5">
          <button class="btn btn-fill" (click)="projectOpened.emit(-1)">View My Projects</button>
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="btn btn-ghost" download="Jaren_Kendrick_Resume.pdf">Download Resume</a>
        </div>
      </div>
      <div class="h-photo au d3">
        <div class="h-photo-placeholder">
          <img src="pictures/profile.jpg" alt="Jaren Kendrick"
               style="width:100%;height:100%;object-fit:cover;border-radius:50%;position:absolute;inset:0;"
               (error)="$any($event.target).style.display='none'" />
          <svg width="60" height="60" fill="currentColor" viewBox="0 0 24 24"><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>
        </div>
      </div>
    </div>

    <div class="feat-section au d5">
      <p class="feat-label">Featured Projects</p>
      <div class="feat-grid">
        @for (item of featuredProjects; track item.index) {
          <div class="feat-card" (click)="projectOpened.emit(item.index)"
               (mouseenter)="previewHover.emit(item.index)"
               (mouseleave)="previewHover.emit(null)"
               role="button" tabindex="0">
            <div class="feat-thumb" [style.background]="item.project.imgs[0]">
              <img [src]="'pictures/' + item.project.name.toLowerCase().replace(' ', '-') + '-thumb.jpg'"
                   style="width:100%;height:100%;object-fit:cover;"
                   (error)="$any($event.target).style.display='none'" />
            </div>
            <div class="feat-body">
              <div class="feat-name">{{ item.project.name }}</div>
              <div class="feat-desc">{{ item.project.desc.substring(0, 80) }}...</div>
              <div class="feat-tags">
                @for (tag of item.project.tags.slice(0,3); track tag) {
                  <span class="feat-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</section>

--- DIRECTORY: app\components\home\home.component.ts ---
import { Component, inject, ElementRef, AfterViewInit, OnDestroy, output, Input, effect } from '@angular/core';
import { PortfolioDataService } from '../../data/portfolio.data';
import { NavigationService } from '../../data/navigation.service';
import { DecryptDirective } from '../../directives/decrypt.directive';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [DecryptDirective],
  templateUrl: './home.component.html',
  styleUrl: './home.component.css',
})
export class HomeComponent implements AfterViewInit, OnDestroy {
  @Input() active = false;

  data = inject(PortfolioDataService);
  nav  = inject(NavigationService);
  projectOpened = output<number>();
  previewHover  = output<number | null>();

  readonly featuredIndices = [0, 1, 3];

  private canvas!: HTMLCanvasElement;
  private ctx!: CanvasRenderingContext2D;
  private animId = 0;
  private pts: { ox: number; oy: number; x: number; y: number }[] = [];
  private vx: number[] = [];
  private vy: number[] = [];
  private cmx = -9999;
  private cmy = -9999;

  private readonly SP     = 42;
  private readonly REPEL  = 110;
  private readonly FORCE  = 22;
  private readonly RESTORE = 0.07;
  private readonly DAMP   = 0.82;

  private _docMouseMove!: (e: MouseEvent) => void;
  private _docMouseLeave!: () => void;
  private _resizeHandler!: () => void;

  get featuredProjects() {
    return this.featuredIndices.map(i => ({ project: this.data.projects[i], index: i }));
  }

  ngAfterViewInit() {
    this.canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    if (!this.canvas) return;
    this.ctx = this.canvas.getContext('2d')!;

    // Initial resize — slight delay so the DOM has settled and scrollHeight is accurate
    setTimeout(() => this.resize(), 50);

    this._resizeHandler = () => this.resize();
    window.addEventListener('resize', this._resizeHandler);

    this._docMouseMove = (e: MouseEvent) => {
      const rect = this.canvas.getBoundingClientRect();
      this.cmx = e.clientX - rect.left;
      this.cmy = e.clientY - rect.top;
    };
    this._docMouseLeave = () => {
      this.cmx = -9999;
      this.cmy = -9999;
    };

    document.addEventListener('mousemove',  this._docMouseMove);
    document.addEventListener('mouseleave', this._docMouseLeave);

    this.draw();
  }

  ngOnDestroy() {
    cancelAnimationFrame(this.animId);
    document.removeEventListener('mousemove',  this._docMouseMove);
    document.removeEventListener('mouseleave', this._docMouseLeave);
    if (this._resizeHandler) window.removeEventListener('resize', this._resizeHandler);
  }

  private resize() {
    /*
      FIX: Canvas must cover the FULL scrollable content of the page section,
      not just the viewport-sized container. Use the scroll container's
      scrollHeight so the grid extends behind the featured projects section.
    */
    const section = this.canvas.closest('.page-section') as HTMLElement;
    const W = this.canvas.clientWidth || (section ? section.clientWidth : window.innerWidth);
    const H = section
      ? Math.max(section.scrollHeight, section.clientHeight)
      : window.innerHeight;

    // Override CSS height so the canvas physically stretches
    this.canvas.style.height = H + 'px';
    this.canvas.width  = W;
    this.canvas.height = H;

    this.pts = [];
    this.vx  = [];
    this.vy  = [];

    for (let c = 0; c <= Math.ceil(W / this.SP) + 1; c++) {
      for (let r = 0; r <= Math.ceil(H / this.SP) + 1; r++) {
        this.pts.push({ ox: c * this.SP, oy: r * this.SP, x: c * this.SP, y: r * this.SP });
        this.vx.push(0);
        this.vy.push(0);
      }
    }
  }

  private draw() {
    const { ctx, canvas: { width: W, height: H } } = this;
    ctx.clearRect(0, 0, W, H);
    const rows = Math.ceil(H / this.SP) + 2;
    const cols = Math.ceil(W / this.SP) + 2;

    this.pts.forEach((p, i) => {
      const dx   = p.x - this.cmx;
      const dy   = p.y - this.cmy;
      const dist = Math.hypot(dx, dy) || 1;
      if (dist < this.REPEL) {
        const mag = ((this.REPEL - dist) / this.REPEL) * this.FORCE;
        this.vx[i] += (dx / dist) * mag;
        this.vy[i] += (dy / dist) * mag;
      }
      this.vx[i] += (p.ox - p.x) * this.RESTORE;
      this.vy[i] += (p.oy - p.y) * this.RESTORE;
      this.vx[i] *= this.DAMP;
      this.vy[i] *= this.DAMP;
      p.x += this.vx[i];
      p.y += this.vy[i];
    });

    const isDark = this.nav.isDark();
    ctx.strokeStyle = isDark ? 'rgba(42,42,64,0.65)' : 'rgba(200,195,224,0.55)';
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    this.pts.forEach((p, i) => {
      const c = Math.floor(i / rows);
      const r = i % rows;
      if (c < cols - 1) {
        const n = this.pts[(c + 1) * rows + r];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
      if (r < rows - 1) {
        const n = this.pts[c * rows + r + 1];
        if (n) { ctx.moveTo(p.x, p.y); ctx.lineTo(n.x, n.y); }
      }
    });
    ctx.stroke();

    this.pts.forEach(p => {
      const d    = Math.hypot(p.x - this.cmx, p.y - this.cmy);
      const prox = Math.max(0, 1 - d / this.REPEL);
      ctx.beginPath();
      ctx.arc(p.x, p.y, 1.1 + prox * 3, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(139,92,246,${0.14 + prox * 0.65})`;
      ctx.fill();
    });

    this.animId = requestAnimationFrame(() => this.draw());
  }
}

--- DIRECTORY: app\components\project-overlay\project-overlay.component.css ---
.overlay-wrap {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: flex;
  animation: overlayIn .55s cubic-bezier(.76,0,.24,1) both;
}

.overlay-wrap.closing {
  animation: overlayOut .5s cubic-bezier(.76,0,.24,1) forwards;
}

@keyframes overlayOut {
  from { transform: translateX(0); }
  to   { transform: translateX(100%); }
}

@keyframes overlayIn {
  from { transform: translateX(100%); }
  to   { transform: translateX(0); }
}

.ov-backdrop {
  width: 200px;
  flex-shrink: 0;
  background: rgba(0,0,0,.6);
  cursor: pointer;
}

.ov-panel {
  flex: 1;
  background: var(--bg);
  border-left: 1px solid var(--bord);
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}
.ov-panel::-webkit-scrollbar { width: 6px; }
.ov-panel::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

/* FIX: Remove position:sticky — it was creating a static box-shadow that
   didn't move with the slide-in animation, making the shadow look "stuck" */
.ov-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 2rem 3rem;
  border-bottom: 1px solid var(--bord);
  background: var(--bg);
  /* Changed from sticky → uses a bottom border instead; looks correct during animation */
}

.ov-num {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--acc);
  letter-spacing: .1em;
  margin-bottom: .75rem;
}

.ov-section-label {
  font-family: 'JetBrains Mono', monospace;
  font-size: .8rem;
  color: var(--text);
  text-transform: uppercase;
  font-weight: 600;
}

.ov-close {
  width: 40px; height: 40px;
  border: 1px solid var(--bord);
  border-radius: 4px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--muted);
  font-size: 1.2rem;
  cursor: pointer;
  background: transparent;
  transition: border-color .2s, color .2s;
}
.ov-close:hover { border-color: var(--acc); color: var(--acc); }

.ov-body {
  padding: 3rem;
  flex: 1;
}

.ov-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.03em;
  margin-bottom: 2rem;
  line-height: 1.1;
}

.ov-imgs {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: .75rem;
  margin-bottom: 2.5rem;
}

.ov-img {
  aspect-ratio: 16/10;
  border-radius: 6px;
  border: 1px solid var(--bord);
  overflow: hidden;
  transition: border-color .2s;
}
.ov-img:hover { border-color: var(--acc); }

.ov-sections {
  display: grid;
  grid-template-columns: 1.2fr .8fr;
  gap: 3rem;
}

.ov-h {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  letter-spacing: .12em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .85rem;
}

.ov-desc {
  font-size: 1rem;
  color: var(--text);
  line-height: 1.75;
  font-weight: 300;
  margin-bottom: 1.5rem;
}
.ov-desc strong { color: var(--white); font-weight: 600; }

.ov-tags { display: flex; flex-wrap: wrap; gap: .4rem; margin-bottom: 1.5rem; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  letter-spacing: .04em;
  padding: .3rem .65rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.ov-links { display: flex; flex-wrap: wrap; gap: .75rem; }
.links-coming { font-family: 'JetBrains Mono', monospace; font-size: .8rem; color: var(--muted); font-style: italic; }

.ov-features {
  list-style: none;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: .7rem;
}
.ov-features li {
  font-size: .95rem;
  color: var(--text);
  line-height: 1.65;
  font-weight: 300;
  padding-left: 1.25rem;
  position: relative;
}
.ov-features li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .8rem;
  top: .2rem;
}

/* Buttons used inside overlay */
.btn {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  padding: .65rem 1.2rem;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .88rem;
  font-weight: 600;
  border-radius: 6px;
  cursor: pointer;
  text-decoration: none;
  border: none;
  transition: transform .2s, box-shadow .2s;
}
.btn:hover { transform: translateY(-2px); }
.btn-fill { background: var(--acc); color: #fff; }
.btn-fill:hover { box-shadow: 0 8px 28px rgba(167,139,250,.35); }
.btn-ghost { background: var(--surf); color: var(--white); border: 1px solid var(--bord); }
.btn-ghost:hover { border-color: var(--acc); color: var(--acc); }

@media (max-width: 960px) {
  .ov-backdrop { display: none; }
  .ov-imgs { grid-template-columns: 1fr 1fr; }
  .ov-sections { grid-template-columns: 1fr; }
  .ov-body { padding: 2rem 1.5rem; }
  .ov-top { padding: 1.5rem; }
}

--- DIRECTORY: app\components\project-overlay\project-overlay.component.html ---
@if (project()) {
  <div class="overlay-wrap" [class.closing]="isClosing()">
    <div class="ov-backdrop" (click)="close()"></div>
    <div class="ov-panel" role="dialog" aria-modal="true" aria-label="Project details">
      <div class="ov-top">
        <div>
          <div class="ov-num">{{ project()!.num }}</div>
          <div class="ov-section-label">Project Details</div>
        </div>
        <button class="ov-close" (click)="close()" aria-label="Close">✕</button>
      </div>
      <div class="ov-body">
        <h2 class="ov-name">{{ project()!.name }}</h2>
        <div class="ov-imgs">
          @for (img of project()!.imgs; track $index) {
            <div class="ov-img" [style.background]="img">
              <svg width="100%" height="100%" viewBox="0 0 200 125" xmlns="http://www.w3.org/2000/svg">
                <rect width="200" height="125" fill="transparent" rx="4"/>
                <path d="M0 0 L200 125 M200 0 L0 125" stroke="var(--bord)" stroke-width="1" opacity=".25"/>
              </svg>
            </div>
          }
        </div>
        <div class="ov-sections">
          <div>
            <p class="ov-h">ABOUT THIS PROJECT</p>
            <p class="ov-desc">
              <strong>{{ project()!.purpose }}</strong><br/><br/>
              {{ project()!.desc }}
            </p>
            <div class="ov-tags">
              @for (tag of project()!.tags; track tag) {
                <span class="ptag">{{ tag }}</span>
              }
            </div>
            <div class="ov-links">
              @if (project()!.live) {
                @for (link of project()!.live!; track link.url) {
                  <a [href]="link.url" target="_blank" rel="noopener" class="btn btn-fill btn-sm">{{ link.name }}</a>
                }
              }
              @if (project()!.github) {
                <a [href]="project()!.github!" target="_blank" rel="noopener" class="btn btn-ghost btn-sm">GitHub →</a>
              }
              @if (!project()!.live && !project()!.github) {
                <span class="links-coming">Links coming soon</span>
              }
            </div>
          </div>
          <div>
            <p class="ov-h">KEY FEATURES</p>
            <ul class="ov-features">
              @for (feat of project()!.features; track feat) {
                <li>{{ feat }}</li>
              }
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
}

--- DIRECTORY: app\components\project-overlay\project-overlay.component.ts ---
import { Component, input, output, signal } from '@angular/core';
import { Project } from '../../models/portfolio.models';

@Component({
  selector: 'app-project-overlay',
  standalone: true,
  templateUrl: './project-overlay.component.html',
  styleUrl: './project-overlay.component.css',
})
export class ProjectOverlayComponent {
  project = input<Project | null>(null);
  closed = output<void>();
  isClosing = signal(false);

  close() {
    this.isClosing.set(true);
    setTimeout(() => {
      this.isClosing.set(false);
      this.closed.emit();
    }, 500); // Wait for CSS animation to finish
  }
}

--- DIRECTORY: app\components\projects\projects.component.css ---
.page-projects {
  width: 100%;
  min-height: 100%;
  padding: 5rem 5rem 3rem;
  background: var(--bg);
  overflow-y: auto;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.pheader {
  display: flex;
  align-items: baseline;
  gap: 2rem;
  margin-bottom: 3rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both;
}

.ph2 {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  line-height: 1;
}

.pcnt {
  font-family: 'JetBrains Mono', monospace;
  font-size: .85rem;
  color: var(--muted);
  letter-spacing: .1em;
}

.phr { flex: 1; height: 1px; background: var(--bord); }

.plist { list-style: none; width: 100%; padding: 0; }

.pi {
  display: grid;
  grid-template-columns: 3rem 1fr auto 2rem;
  align-items: center;
  gap: 2rem;
  padding: 2.5rem 0;
  border-bottom: 1px solid var(--bord);
  cursor: pointer;
  transition: border-color .2s, padding .2s, background .2s;
  position: relative;
  overflow: hidden;
  animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both;
}

.pi:first-child { border-top: 1px solid var(--bord); }

.pi:hover {
  border-color: transparent;
  background: var(--surf);
  padding-left: 1rem;
  padding-right: 1rem;
  border-radius: 8px;
}
.pi:hover .pnm  { color: var(--acc); }
.pi:hover .parr { color: var(--acc); transform: translate(5px, -5px); }

.pnum {
  font-family: 'JetBrains Mono', monospace;
  font-size: 1rem;
  color: var(--acc);
  font-weight: 500;
  letter-spacing: .06em;
}

.pnm {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 600;
  font-size: clamp(1.8rem, 3vw, 2.4rem);
  letter-spacing: -.02em;
  color: var(--white);
  line-height: 1;
  transition: color .25s;
  margin-bottom: .5rem;
}

.psub {
  font-size: 1.05rem;
  color: var(--text);
  font-weight: 300;
  line-height: 1.5;
}

.ptgs { display: flex; gap: .5rem; flex-wrap: wrap; justify-content: flex-end; }

.ptag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .75rem;
  letter-spacing: .04em;
  padding: .3rem .6rem;
  border-radius: 4px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
}

.parr {
  font-size: 1.5rem;
  color: var(--muted);
  transition: color .2s, transform .2s;
}

.active-page .ph2,
.active-page .pheader { animation: fadeUp .55s cubic-bezier(.22,1,.36,1) ease both; }
.active-page .pi      { animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) ease both; }

@keyframes fadeUp    { from { opacity: 0; transform: translateY(40px); }  to { opacity: 1; transform: translateY(0); } }
@keyframes fadeLeft  { from { opacity: 0; transform: translateX(-44px); } to { opacity: 1; transform: translateX(0); } }

.pi:nth-child(1) { animation-delay: .10s; }
.pi:nth-child(2) { animation-delay: .18s; }
.pi:nth-child(3) { animation-delay: .26s; }
.pi:nth-child(4) { animation-delay: .34s; }
.pi:nth-child(5) { animation-delay: .44s; }

@media (max-width: 960px) {
  .page-projects { padding: 6.5rem 1.5rem 4rem; }
  .pi { grid-template-columns: auto 1fr auto; gap: 1.25rem; }
  .pnum, .ptgs { display: none; }
  .pnm { font-size: clamp(1.6rem, 7vw, 2.2rem); }
}

--- DIRECTORY: app\components\projects\projects.component.html ---
<section class="page-projects" [class.active-page]="active">
  <div class="pin">
    <div class="pheader au d0">
      <h2 class="ph2">Selected Work</h2>
      <span class="pcnt">05 projects</span>
      <div class="phr" aria-hidden="true"></div>
    </div>
    <ul class="plist" role="list">
      @for (proj of data.projects; track proj.num; let i = $index) {
        <li class="pi al" 
            [class.d1]="i === 0" [class.d2]="i === 1" [class.d3]="i === 2" [class.d4]="i === 3" [class.d5]="i === 4"
            (click)="projectOpened.emit(i)" 
            (mouseenter)="previewHover.emit(i)" 
            (mouseleave)="previewHover.emit(null)"
            role="button" tabindex="0" (keydown.enter)="projectOpened.emit(i)">
          <span class="pnum">{{ proj.num }}</span>
          <div>
            <div class="pnm">{{ proj.name }}</div>
            <div class="psub">{{ proj.subtitle }}</div>
          </div>
          <div class="ptgs">
            @for (tag of proj.tags; track tag) {
              <span class="ptag">{{ tag }}</span>
            }
          </div>
          <span class="parr" aria-hidden="true">↗</span>
        </li>
      }
    </ul>
  </div>
</section>

--- DIRECTORY: app\components\projects\projects.component.ts ---
import { Component, inject, output, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './projects.component.html',
  styleUrl: './projects.component.css',
})
export class ProjectsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
  projectOpened = output<number>();
  previewHover = output<number | null>();
}

--- DIRECTORY: app\components\resume\resume.component.css ---
/* ══════════════════════════════════════════
   HOST — fills the page section exactly
══════════════════════════════════════════ */
:host {
  display: block;
  width: 100%;
  height: 100%;
}

/* ══════════════════════════════════════════
   PAGE SHELL — no overflow, fills viewport
   Desktop: fixed height so page-slider
   handles scroll. No internal scrolling.
══════════════════════════════════════════ */
.page-resume {
  width: 100%;
  height: 100%;
  padding: 2.5rem 3rem 2rem 3rem;
  background: var(--bg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

/* ══════════════════════════════════════════
   TWO-COLUMN LAYOUT
══════════════════════════════════════════ */
.res-layout {
  display: grid;
  grid-template-columns: 1.35fr 1fr;
  gap: 3rem;
  height: 100%;
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
  overflow: hidden;
}

.res-left,
.res-right {
  display: flex;
  flex-direction: column;
  gap: 0;
  overflow: hidden;
  min-height: 0;
}

/* ══════════════════════════════════════════
   RESUME HEADER
══════════════════════════════════════════ */
.res-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 1.5rem;
  padding-bottom: 1.25rem;
  border-bottom: 2px solid var(--acc);
  margin-bottom: 1.25rem;
  flex-shrink: 0;
}

.res-name {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(1.5rem, 2.2vw, 2rem);
  font-weight: 600;
  color: var(--white);
  letter-spacing: -.025em;
  line-height: 1.1;
  margin-bottom: .2rem;
}
.res-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .72rem;
  color: var(--acc);
  letter-spacing: .05em;
  margin-bottom: .15rem;
  font-weight: 500;
}
.res-location {
  font-size: .8rem;
  color: var(--muted);
  font-weight: 300;
}

.res-header-contacts {
  display: flex;
  flex-direction: column;
  gap: .35rem;
  align-items: flex-end;
  flex-shrink: 0;
}
.res-contact-link {
  display: inline-flex;
  align-items: center;
  gap: .35rem;
  font-size: .72rem;
  font-family: 'JetBrains Mono', monospace;
  color: var(--muted);
  text-decoration: none;
  letter-spacing: .02em;
  transition: color .2s;
}
.res-contact-link:hover { color: var(--acc); }
.res-contact-link svg {
  width: 11px; height: 11px;
  fill: currentColor;
  flex-shrink: 0;
}
.res-contact-link svg[stroke] { fill: none; stroke: currentColor; stroke-width: 2; }

/* Download button — styled differently */
.res-dl-btn {
  display: inline-flex;
  align-items: center;
  gap: .4rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .06em;
  color: #fff;
  background: var(--acc);
  border: none;
  border-radius: 4px;
  padding: .35rem .75rem;
  text-decoration: none;
  margin-top: .25rem;
  cursor: pointer;
  transition: opacity .2s, transform .15s;
}
.res-dl-btn:hover { opacity: .88; transform: translateY(-1px); }
.res-dl-btn svg { width: 11px; height: 11px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

/* ══════════════════════════════════════════
   SECTION LABELS
══════════════════════════════════════════ */
.res-section {
  flex-shrink: 0;
  margin-bottom: 1rem;
}
.res-section:last-child { margin-bottom: 0; }

.res-section-title {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .14em;
  text-transform: uppercase;
  color: var(--acc);
  font-weight: 700;
  margin-bottom: .4rem;
}
.res-divider {
  height: 1px;
  background: var(--bord);
  margin-bottom: .75rem;
}

/* ══════════════════════════════════════════
   EXPERIENCE / EDUCATION ENTRIES
══════════════════════════════════════════ */
.res-entry {
  display: grid;
  grid-template-columns: 110px 1fr;
  gap: 1rem;
  margin-bottom: .75rem;
}
.res-entry:last-child { margin-bottom: 0; }

.res-entry-side {
  display: flex;
  flex-direction: column;
  gap: .15rem;
  padding-top: .1rem;
}
.res-entry-period {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--acc);
  letter-spacing: .04em;
  line-height: 1.3;
  font-weight: 600;
}
.res-entry-type {
  font-size: .65rem;
  color: var(--muted);
  font-weight: 300;
  line-height: 1.3;
}

.res-entry-header {
  display: flex;
  align-items: baseline;
  gap: .6rem;
  flex-wrap: wrap;
  margin-bottom: .4rem;
}
.res-entry-title {
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-weight: 700;
  font-size: .95rem;
  color: var(--white);
  letter-spacing: -.01em;
}
.res-entry-company {
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  color: var(--acc);
  letter-spacing: .05em;
  font-weight: 500;
}

.res-bullets {
  list-style: none;
  padding: 0;
  margin: 0 0 .5rem;
  display: flex;
  flex-direction: column;
  gap: .25rem;
}
.res-bullets li {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.55;
  font-weight: 300;
  padding-left: .9rem;
  position: relative;
}
.res-bullets li::before {
  content: '▸';
  position: absolute;
  left: 0;
  color: var(--acc);
  font-size: .62rem;
  top: .15rem;
}
.res-bullets li strong { color: var(--white); font-weight: 600; }

.res-edu-detail {
  font-size: .78rem;
  color: var(--text);
  line-height: 1.6;
  font-weight: 300;
}

.res-tags {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
  margin-top: .35rem;
}
.res-tag {
  font-family: 'JetBrains Mono', monospace;
  font-size: .58rem;
  padding: .15rem .45rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--muted);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
}

/* ══════════════════════════════════════════
   SKILLS BARS
══════════════════════════════════════════ */
.sk-row { margin-bottom: .45rem; }
.sk-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: .2rem;
}
.sk-name {
  font-size: .78rem;
  color: var(--text);
  font-weight: 500;
}
.sk-meta {
  font-family: 'JetBrains Mono', monospace;
  font-size: .62rem;
  color: var(--muted);
}
.sk-bar {
  height: 4px;
  background: var(--surf);
  border-radius: 3px;
  border: 1px solid var(--bord);
  overflow: hidden;
}
.sk-fill {
  height: 100%;
  background: var(--acc);
  border-radius: 3px;
  transition: width 1s cubic-bezier(.22,1,.36,1);
}

/* Tools chips */
.res-tools-grid {
  display: flex;
  flex-wrap: wrap;
  gap: .3rem;
}
.res-tool-chip {
  font-family: 'JetBrains Mono', monospace;
  font-size: .6rem;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: var(--surf);
  color: var(--text);
  border: 1px solid var(--bord);
  letter-spacing: .03em;
  transition: border-color .2s, color .2s;
}
.res-tool-chip:hover { border-color: var(--acc); color: var(--acc); }

/* ══════════════════════════════════════════
   CERTIFICATIONS CAROUSEL
══════════════════════════════════════════ */
.cert-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: .4rem;
}
.cert-counter {
  font-family: 'JetBrains Mono', monospace;
  font-size: .7rem;
  color: var(--muted);
  letter-spacing: .05em;
}
.cert-carousel {
  position: relative;
  display: flex;
  align-items: center;
  gap: .5rem;
  margin-bottom: .6rem;
}
.cert-nav {
  width: 30px; height: 30px;
  border: 1px solid var(--bord);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  color: var(--sub); font-size: 1rem; cursor: pointer; flex-shrink: 0;
  transition: border-color .2s, color .2s;
  background: var(--bg); line-height: 0; padding-bottom: 1px;
}
.cert-nav:hover { border-color: var(--acc); color: var(--acc); }

.cert-card-wrap {
  flex: 1;
  overflow: hidden;
}
.cert-in   { animation: certIn  .3s cubic-bezier(.22,1,.36,1) both; }
.cert-in-r { animation: certInR .3s cubic-bezier(.22,1,.36,1) both; }
@keyframes certIn  { from { opacity:0; transform: translateX(20px);  } to { opacity:1; transform: none; } }
@keyframes certInR { from { opacity:0; transform: translateX(-20px); } to { opacity:1; transform: none; } }

.cert-card {
  display: block;
  border-radius: 7px;
  overflow: hidden;
  border: 1px solid var(--bord);
  transition: border-color .25s;
  text-decoration: none;
  cursor: pointer;
}
.cert-card:hover { border-color: var(--acc); }
.cert-top {
  height: 42px;
  display: flex; align-items: center;
  padding: 0 1rem; gap: .6rem;
}
.cert-issuer-badge {
  width: 26px; height: 26px;
  border-radius: 3px; display: flex; align-items: center; justify-content: center;
  font-family: 'JetBrains Mono', monospace; font-size: .6rem; font-weight: 700;
  color: #fff; flex-shrink: 0;
}
.cert-issuer-name {
  font-family: 'JetBrains Mono', monospace; font-size: .7rem;
  letter-spacing: .04em; color: var(--text); font-weight: 500;
}
.cert-body { background: var(--bg); padding: .85rem 1rem; }
.cert-name {
  font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 600;
  font-size: .9rem; color: var(--white); line-height: 1.2; margin-bottom: .35rem;
}
.cert-meta { display: flex; align-items: center; justify-content: space-between; }
.cert-date { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--muted); }
.cert-view { font-family: 'JetBrains Mono', monospace; font-size: .65rem; color: var(--acc); font-weight: 500; }

.cert-dots {
  display: flex; gap: .35rem; justify-content: center;
}
.cert-dot {
  width: 5px; height: 5px; border-radius: 50%; background: var(--bord);
  border: none; cursor: pointer; transition: background .2s; padding: 0;
}
.cert-dot.on { background: var(--acc); }

/* ══════════════════════════════════════════
   ANIMATIONS
══════════════════════════════════════════ */
@keyframes fadeUp  { from { opacity:0; transform:translateY(30px); } to { opacity:1; transform:none; } }

/* ══════════════════════════════════════════
   RESPONSIVE — on mobile, allow normal scroll
══════════════════════════════════════════ */
@media (max-width: 960px) {
  .page-resume {
    overflow: visible;
    height: auto;
    min-height: 100vh;
    padding: 6.5rem 1.5rem 4rem;
  }
  .res-layout {
    grid-template-columns: 1fr;
    height: auto;
    overflow: visible;
  }
  .res-left, .res-right { overflow: visible; }
  .res-header { flex-direction: column; gap: 1rem; }
  .res-header-contacts { align-items: flex-start; }
  .res-entry { grid-template-columns: 1fr; gap: .5rem; }
  .cert-nav, .cert-card, .cert-dot, .res-tool-chip, .res-dl-btn { cursor: pointer !important; }
}

--- DIRECTORY: app\components\resume\resume.component.html ---
<section class="page-resume" [class.active-page]="active">
  <div class="res-layout">

    <!-- ═══════════ LEFT COLUMN ═══════════ -->
    <div class="res-left">

      <!-- HEADER -->
      <div class="res-header au d0">
        <div>
          <h2 class="res-name">Jaren Kendrick Yambao</h2>
          <p class="res-title">Full Stack Developer · Game/App Developer</p>
          <p class="res-location">Pampanga, Philippines</p>
        </div>
        <div class="res-header-contacts">
          <a href="mailto:jarenkendrickyambao@gmail.com" class="res-contact-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 4L12 13L2 4"/></svg>
            jarenkendrickyambao&#64;gmail.com
          </a>
          <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="res-contact-link">
            <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
            github.com/jarenkendrick14
          </a>
          <!-- Download: no target="_blank" so browser downloads directly -->
          <a href="assets/Jaren_Kendrick_Resume.pdf" class="res-dl-btn" download="Jaren_Kendrick_Resume.pdf">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Download PDF
          </a>
        </div>
      </div>

      <!-- EXPERIENCE -->
      <div class="res-section au d1">
        <h3 class="res-section-title">Experience</h3>
        <div class="res-divider"></div>
        @for (job of experience; track job.company) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ job.period }}</span>
              <span class="res-entry-type">{{ job.type }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ job.title }}</h4>
                <span class="res-entry-company">{{ job.company }}</span>
              </div>
              <ul class="res-bullets">
                @for (bullet of job.bullets; track $index) {
                  <li [innerHTML]="bullet"></li>
                }
              </ul>
              <div class="res-tags">
                @for (tag of job.tags; track tag) {
                  <span class="res-tag">{{ tag }}</span>
                }
              </div>
            </div>
          </div>
        }
      </div>

      <!-- EDUCATION -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Education</h3>
        <div class="res-divider"></div>
        @for (edu of education; track edu.school) {
          <div class="res-entry">
            <div class="res-entry-side">
              <span class="res-entry-period">{{ edu.period }}</span>
              <span class="res-entry-type">{{ edu.location }}</span>
            </div>
            <div class="res-entry-main">
              <div class="res-entry-header">
                <h4 class="res-entry-title">{{ edu.degree }}</h4>
                <span class="res-entry-company">{{ edu.school }}</span>
              </div>
              <p class="res-edu-detail">{{ edu.detail }}</p>
            </div>
          </div>
        }
      </div>

    </div><!-- /res-left -->

    <!-- ═══════════ RIGHT COLUMN ═══════════ -->
    <div class="res-right">

      <!-- SKILLS -->
      <div class="res-section au d2">
        <h3 class="res-section-title">Technical Skills</h3>
        <div class="res-divider"></div>
        @for (sk of skills; track sk.name; let i = $index) {
          <div class="sk-row">
            <div class="sk-info">
              <span class="sk-name">{{ sk.name }}</span>
              <span class="sk-meta">{{ sk.pct }}%</span>
            </div>
            <div class="sk-bar">
              <div class="sk-fill"
                [style.width]="barsReady() ? sk.pct + '%' : '0%'"
                [style.transition-delay]="(i * 0.07) + 's'">
              </div>
            </div>
          </div>
        }

        <!-- Tools row -->
        <div class="res-tools-grid" style="margin-top:.85rem;">
          @for (tool of toolsList; track tool) {
            <span class="res-tool-chip">{{ tool }}</span>
          }
        </div>
      </div>

      <!-- CERTIFICATIONS — CAROUSEL -->
      <div class="res-section au d3">
        <div class="cert-header">
          <h3 class="res-section-title" style="margin-bottom:0;">Certifications</h3>
          <div class="cert-counter">{{ certIndex() + 1 }} / {{ certTotal }}</div>
        </div>
        <div class="res-divider"></div>
        <div class="cert-carousel">
          <button class="cert-nav" (click)="certNav(-1)" aria-label="Previous cert">‹</button>
          <div class="cert-card-wrap" [ngClass]="certAnimClass()">
            <a class="cert-card" [href]="currentCert.url" target="_blank" rel="noopener">
              <div class="cert-top" [style.background]="currentCert.color + '22'">
                <div class="cert-issuer-badge" [style.background]="currentCert.color">{{ currentCert.abbr }}</div>
                <span class="cert-issuer-name">{{ currentCert.issuer }}</span>
              </div>
              <div class="cert-body">
                <div class="cert-name">{{ currentCert.name }}</div>
                <div class="cert-meta">
                  <div class="cert-date">{{ currentCert.date }}</div>
                  <div class="cert-view">View ↗</div>
                </div>
              </div>
            </a>
          </div>
          <button class="cert-nav" (click)="certNav(1)" aria-label="Next cert">›</button>
        </div>
        <!-- Dot indicators -->
        <div class="cert-dots">
          @for (cert of data.certificates; track $index) {
            <button class="cert-dot" [class.on]="$index === certIndex()"
              (click)="certNav($index > certIndex() ? 1 : -1)"
              [attr.aria-label]="'Cert ' + ($index + 1)">
            </button>
          }
        </div>
      </div>

    </div><!-- /res-right -->

  </div><!-- /res-layout -->
</section>

--- DIRECTORY: app\components\resume\resume.component.ts ---
import { Component, Input, inject, signal, OnChanges, SimpleChanges } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-resume',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './resume.component.html',
  styleUrl: './resume.component.css',
})
export class ResumeComponent implements OnChanges {
  @Input() active = false;
  data = inject(PortfolioDataService);

  private barsAnimated = false;
  readonly barsReady = signal(false);

  /* ── Cert Carousel ── */
  readonly certIndex = signal(0);
  readonly certAnimClass = signal('');

  get currentCert() { return this.data.certificates[this.certIndex()]; }
  get certTotal()   { return this.data.certificates.length; }

  certNav(dir: number) {
    this.certAnimClass.set('');
    this.certIndex.update(i => (i + dir + this.certTotal) % this.certTotal);
    setTimeout(() => this.certAnimClass.set(dir > 0 ? 'cert-in' : 'cert-in-r'), 10);
  }

  readonly experience = [
    {
      period: 'Apr 2025 — Present',
      company: 'VirtuIntelligence',
      type: 'Part-time · Remote',
      title: 'Game/App Developer',
      bullets: [
        'Shipped <strong>PathFinder</strong> VR app to Meta Quest store using Unreal Engine 5.5 Blueprints; built its Android companion in Unity.',
        'Architected <strong>Symposium</strong>, a full-stack real-time AI-mediated chat platform (Angular + Ionic + Node.js + PostgreSQL) with WebSocket + streaming xAI Grok.',
        'Currently developing <strong>Robots vs Aliens</strong>, a mobile action game in Unity (C#) with procedural level generation.',
        'Managing own sprint tasks and code reviews across multiple time zones.',
      ],
      tags: ['Unreal 5.5', 'Unity', 'C#', 'Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'WebSockets'],
    },
  ];

  readonly education = [
    {
      period: 'Jun 2023 — 2027',
      school: 'Holy Angel University',
      location: 'Pampanga, Philippines',
      degree: 'BS Information Technology',
      detail: 'Specialization in Web Development. Coursework in full-stack architecture, database systems, software engineering, networking, and deployment workflows. Maintaining strong academic standing while working part-time.',
    },
  ];

  readonly skills = [
    { name: 'JavaScript', time: '2 yrs', pct: 85 },
    { name: 'TypeScript', time: '1.5 yrs', pct: 80 },
    { name: 'Angular', time: '1 yr', pct: 75 },
    { name: 'Node.js & Express', time: '1 yr', pct: 75 },
    { name: 'Vue.js 3', time: '1 yr', pct: 70 },
    { name: 'C#', time: '1 yr', pct: 70 },
    { name: 'MongoDB', time: '1 yr', pct: 65 },
    { name: 'PostgreSQL', time: '6 mo', pct: 55 },
  ];

  readonly toolsList = [
    'Git & GitHub', 'Prisma ORM', 'Ionic + Capacitor', 'WebSockets',
    'REST APIs', 'JWT Auth', 'Pinia', 'Wit.ai',
    'Figma', 'Netlify', 'Vercel', 'VS Code',
  ];

  ngOnChanges(changes: SimpleChanges) {
    if (changes['active'] && this.active && !this.barsAnimated) {
      this.barsAnimated = true;
      setTimeout(() => this.barsReady.set(true), 400);
    }
  }
}

--- DIRECTORY: app\components\sidebar\sidebar.component.css ---
.sidebar {
  position: fixed;
  left: 0; top: 0; bottom: 0;
  width: 200px;
  background: var(--sb);
  border-right: 1px solid var(--sbbrd);
  z-index: 100;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 2rem 0;
}

.sb-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  font-size: 0;
  line-height: 0;
}

.sb-logo-img {
  height: 44px;
  width: auto;
  max-width: 140px;
  object-fit: contain;
  display: block;
  transition: opacity .2s ease;
}
.sb-logo:hover .sb-logo-img { opacity: .8; }

.sb-nav {
  margin: auto 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.sb-line {
  width: 1px;
  height: 1.8rem;
  background: var(--bord);
  display: block;
}

.sdw {
  display: flex;
  align-items: center;
  gap: .7rem;
  cursor: pointer;
  padding: .2rem 0;
  justify-content: center;
  width: 100px;
  background: none;
  border: none;
  font-family: inherit;
}

.sdot {
  width: 9px; height: 9px;
  border-radius: 50%;
  border: 1.5px solid var(--muted);
  background: transparent;
  flex-shrink: 0;
  transition: border-color .2s, background .2s;
}
.sdw.on .sdot, .sdw:hover .sdot {
  background: var(--acc);
  border-color: var(--acc);
}

.slbl {
  font-family: 'JetBrains Mono', monospace;
  font-size: .65rem;
  letter-spacing: .1em;
  color: var(--muted);
  text-transform: uppercase;
  transition: color .2s;
  width: 52px;
}
.sdw.on .slbl, .sdw:hover .slbl { color: var(--white); }

.sb-bot {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
}

.sb-socs {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: .65rem;
}

.sbsoc {
  width: 36px; height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1px solid var(--bord);
  color: var(--muted);
  text-decoration: none;
  transition: border-color .2s, color .2s;
}
.sbsoc:hover { border-color: var(--acc); color: var(--acc); background: var(--surf); }
.sbsoc svg { width: 16px; height: 16px; fill: currentColor; }
.sbsoc svg[stroke] { fill: none; }

.thm {
  width: 46px; height: 24px;
  border-radius: 100px;
  background: var(--surf);
  border: 1px solid var(--bord);
  position: relative;
  cursor: pointer;
  transition: border-color .2s;
}
.thm:hover { border-color: var(--acc); }

.tknob {
  position: absolute;
  top: 2px; left: 3px;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--acc);
  font-size: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform .3s ease;
  color: #fff;
}
:host-context([data-theme="light"]) .tknob { transform: translateX(20px); }

/* ══════════ TERMINAL TRIGGER — PROMINENT ══════════ */
.term-trigger {
  display: flex;
  align-items: center;
  gap: .5rem;
  font-family: 'JetBrains Mono', monospace;
  font-size: .68rem;
  letter-spacing: .07em;
  color: #a78bfa;
  border: 1px solid rgba(139, 92, 246, .4);
  border-radius: 6px;
  padding: .45rem .75rem;
  cursor: pointer;
  background: rgba(139, 92, 246, .06);
  transition: color .2s, border-color .2s, background .2s, box-shadow .2s;
  margin-top: .5rem;
  position: relative;
  overflow: hidden;
}

.term-trigger:hover {
  color: #c4b5fd;
  border-color: #a78bfa;
  background: rgba(139, 92, 246, .12);
  box-shadow: 0 0 16px rgba(139, 92, 246, .25), 0 0 4px rgba(139, 92, 246, .15);
}

/* Subtle animated glow sweep */
.term-trigger::after {
  content: '';
  position: absolute;
  top: 0; left: -100%;
  width: 100%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,.12), transparent);
  animation: termSweep 3s ease infinite;
}
@keyframes termSweep {
  0% { left: -100%; }
  50% { left: 100%; }
  100% { left: 100%; }
}

.term-trigger-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #a78bfa;
  opacity: .8;
  animation: termPulse 1.6s ease infinite;
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}

.term-trigger-text {
  position: relative;
  z-index: 1;
}

@keyframes termPulse {
  0%, 100% { opacity: .7; transform: scale(1); box-shadow: 0 0 0 0 rgba(167,139,250,.4); }
  50%      { opacity: 1; transform: scale(1.3); box-shadow: 0 0 8px 2px rgba(167,139,250,.3); }
}

@media (max-width: 960px) {
  .sidebar { display: none; }
}

--- DIRECTORY: app\components\sidebar\sidebar.component.html ---
<aside class="sidebar" role="navigation" aria-label="Main Navigation">

  <!-- Logo: swaps between dark-mode and light-mode PNGs -->
  <!-- Place your files at: src/assets/logo-dark.png  (for dark bg) -->
  <!--                       src/assets/logo-light.png (for light bg) -->
  <a href="#" (click)="goTo(0); $event.preventDefault()" class="sb-logo" aria-label="Home">
    @if (nav.isDark()) {
      <img src="assets/logo-dark.png" alt="Jaren Kendrick" class="sb-logo-img" />
    } @else {
      <img src="assets/logo-light.png" alt="Jaren Kendrick" class="sb-logo-img" />
    }
  </a>

  <nav class="sb-nav">
    @for (item of navItems; track item.index) {
      <div class="sb-line"></div>
      <button
        class="sdw"
        [class.on]="nav.currentPage() === item.index"
        (click)="goTo(item.index)"
        [attr.aria-label]="item.label"
        [attr.aria-current]="nav.currentPage() === item.index ? 'page' : null">
        <div class="sdot"></div>
        <span class="slbl">{{ item.short }}</span>
      </button>
    }
    <div class="sb-line"></div>
  </nav>

  <div class="sb-bot">
    <div class="sb-socs">
      <a href="https://linkedin.com/in/jarenkendrick" target="_blank" rel="noopener" class="sbsoc" aria-label="LinkedIn">
        <svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
      </a>
      <a href="https://github.com/jarenkendrick14" target="_blank" rel="noopener" class="sbsoc" aria-label="GitHub">
        <svg viewBox="0 0 24 24"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
      </a>
      <!-- Envelope mail icon -->
      <a href="mailto:jarenkendrickyambao@gmail.com" class="sbsoc" aria-label="Email">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"/>
          <path d="M22 4L12 13L2 4"/>
        </svg>
      </a>
    </div>

    <button class="thm" (click)="nav.toggleTheme()" aria-label="Toggle dark/light mode">
      <div class="tknob">
        @if (nav.isDark()) {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        } @else {
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
        }
      </div>
    </button>

    <button class="term-trigger" (click)="nav.toggleTerminal()" aria-label="Open developer terminal" title="Developer Mode">
      <span class="term-trigger-dot"></span>[ ]_ dev
    </button>
  </div>
</aside>

--- DIRECTORY: app\components\sidebar\sidebar.component.ts ---
import { Component, inject, effect } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-sidebar',
  standalone: true,
  templateUrl: './sidebar.component.html',
  styleUrl: './sidebar.component.css',
})
export class SidebarComponent {
  nav = inject(NavigationService);
  data = inject(PortfolioDataService);

  readonly navItems = this.data.navItems;

  constructor() {
    effect(() => {
      document.documentElement.dataset['theme'] = this.nav.theme();
    });
  }

  goTo(index: number) {
    this.nav.goTo(index, this.navItems.length);
  }
}


--- DIRECTORY: app\components\terminal\terminal.component.css ---
/*
  Terminal is intentionally theme-agnostic — it always uses the dark-mode
  lavender (#a78bfa) regardless of whether the page is in light or dark mode.
  This means we hardcode the purple instead of using var(--acc).
*/

.terminal {
  position: fixed;
  bottom: 0;
  left: 200px;
  right: 0;
  height: 0;
  background: #06060d;
  /* Hardcoded lavender border — ignores page theme */
  border-top: 2px solid #a78bfa;
  z-index: 9500;
  display: flex;
  flex-direction: column;
  transition: height .45s cubic-bezier(.76,0,.24,1);
  font-family: 'JetBrains Mono', monospace;
  overflow: hidden;
}

.terminal.open { height: 55vh; }

.term-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: .65rem 1.5rem;
  border-bottom: 1px solid rgba(167, 139, 250, .18);
  flex-shrink: 0;
  background: #080810;
}

.term-wm-dots { display: flex; gap: .45rem; align-items: center; }
.term-wm-dots span { width: 11px; height: 11px; border-radius: 50%; }

.term-title {
  font-size: .72rem;
  letter-spacing: .1em;
  /* muted color hardcoded — dark bg always */
  color: #6e6c8a;
}

.term-bar-close {
  font-size: .9rem;
  color: #6e6c8a;
  cursor: pointer;
  padding: .2rem .5rem;
  border-radius: 3px;
  background: none;
  border: none;
  transition: color .2s, background .2s;
}
.term-bar-close:hover {
  color: #a78bfa;
  background: rgba(167, 139, 250, .08);
}

.term-output {
  flex: 1;
  overflow-y: auto;
  padding: 1rem 1.5rem;
  font-size: .82rem;
  line-height: 1.85;
  color: #a0a0c0;
}
.term-output::-webkit-scrollbar { width: 4px; }
.term-output::-webkit-scrollbar-thumb { background: #2a2a40; border-radius: 4px; }

/* All tl-* classes use hardcoded colors — never var(--acc) */
:host ::ng-deep .tl-cmd  { color: #a78bfa; }
:host ::ng-deep .tl-ok   { color: #34d399; }
:host ::ng-deep .tl-err  { color: #f87171; }
:host ::ng-deep .tl-dim  { color: #504e6a; }
:host ::ng-deep .tl-head { color: #ffffff; font-weight: 600; }
:host ::ng-deep .tl-acc  { color: #a78bfa; }
:host ::ng-deep .tl-warn { color: #fbbf24; }

.term-prompt-row {
  display: flex;
  align-items: center;
  gap: .75rem;
  padding: .7rem 1.5rem;
  border-top: 1px solid rgba(167, 139, 250, .12);
  flex-shrink: 0;
  background: #06060d;
}

.term-ps {
  /* Hardcoded lavender */
  color: #a78bfa;
  font-size: .82rem;
  white-space: nowrap;
}

.term-input {
  flex: 1;
  background: none;
  border: none;
  color: #e0def0;
  font-family: 'JetBrains Mono', monospace;
  font-size: .82rem;
  outline: none;
  /* Hardcoded caret */
  caret-color: #a78bfa;
  padding: 0;
}

@media (max-width: 960px) {
  .terminal { left: 0; }
}

--- DIRECTORY: app\components\terminal\terminal.component.html ---
<div class="terminal" [class.open]="isOpen()" role="complementary" aria-label="Developer terminal">
  <div class="term-bar">
    <div class="term-wm-dots">
      <span style="background:#f87171;"></span>
      <span style="background:#fbbf24;"></span>
      <span style="background:#34d399;"></span>
    </div>
    <span class="term-title">jaren&#64;portfolio:~$ — bash</span>
    <button class="term-bar-close" (click)="close()" aria-label="Close terminal">✕</button>
  </div>
  <!-- Stop propagation prevents scrolling the underlying page when hovering terminal -->
  <div class="term-output" #termOutput (wheel)="$event.stopPropagation()" (touchmove)="$event.stopPropagation()">
    @for (line of lines(); track $index) {
      <p [innerHTML]="line.html"></p>
    }
  </div>
  <div class="term-prompt-row">
    <span class="term-ps">jaren&#64;portfolio:~$</span>
    <input
      #termInput
      type="text"
      class="term-input"
      autocomplete="off"
      spellcheck="false"
      placeholder="type 'help' to get started..."
      aria-label="Terminal input"
      [value]="inputValue()"
      (input)="inputValue.set($any($event.target).value)"
      (keydown)="onKeyDown($event)"/>
  </div>
</div>

--- DIRECTORY: app\components\terminal\terminal.component.ts ---
import { Component, signal, ViewChild, ElementRef, effect, inject } from '@angular/core';
import { NavigationService } from '../../data/navigation.service';

interface TermLine { html: string; }

@Component({
  selector: 'app-terminal',
  standalone: true,
  templateUrl: './terminal.component.html',
  styleUrl: './terminal.component.css',
})
export class TerminalComponent {
  @ViewChild('termOutput') termOutput!: ElementRef<HTMLDivElement>;
  @ViewChild('termInput') termInput!: ElementRef<HTMLInputElement>;

  nav = inject(NavigationService);
  isOpen = this.nav.terminalOpen;

  readonly lines = signal<TermLine[]>([]);
  readonly inputValue = signal('');

  private history: string[] = [];
  private histIdx = -1;
  private booted = false;

  private readonly BOOT_MSG = [
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-ok">  ✓ jaren-portfolio v2.5.0 [developer mode]</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `<span class="tl-dim">  System:</span> <span class="tl-val">Full Stack Portfolio • Angular 21</span>`,
    `<span class="tl-dim">  Location:</span> <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
    `<span class="tl-dim">  Status:</span> <span class="tl-ok">● seeking internship</span>`,
    `<span class="tl-dim">──────────────────────────────────────────</span>`,
    `  Type <span class="tl-acc">help</span> to see available commands.`,
  ];

  private readonly COMMANDS: Record<string, () => string[] | null> = {
    help: () => [
      ``,
      `<span class="tl-head">  COMMANDS</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">ls</span>              <span class="tl-dim">list projects</span>`,
      `  <span class="tl-acc">cat resume.txt</span>  <span class="tl-dim">view career summary</span>`,
      `  <span class="tl-acc">skills</span>          <span class="tl-dim">show skill stack</span>`,
      `  <span class="tl-acc">whoami</span>          <span class="tl-dim">who is jaren</span>`,
      `  <span class="tl-acc">contact</span>         <span class="tl-dim">get contact info</span>`,
      `  <span class="tl-acc">github</span>          <span class="tl-dim">open github profile</span>`,
      `  <span class="tl-acc">run pathfinder</span>  <span class="tl-dim">launch vr app</span>`,
      `  <span class="tl-acc">clear</span>           <span class="tl-dim">clear terminal</span>`,
      `  <span class="tl-acc">exit</span>            <span class="tl-dim">close terminal</span>`,
    ],
    ls: () => [
      ``,
      `<span class="tl-head">  projects/</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-dim">01</span>  <span class="tl-head">PathFinder</span>        <span class="tl-dim">·</span> <span class="tl-val">Unreal 5.5 + Unity</span>   <span class="tl-warn">VR</span>`,
      `  <span class="tl-dim">02</span>  <span class="tl-head">Symposium</span>         <span class="tl-dim">·</span> <span class="tl-val">Angular + Node.js</span>    <span class="tl-warn">REAL-TIME</span>`,
      `  <span class="tl-dim">03</span>  <span class="tl-head">Travel Atelier</span>    <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + Node.js</span>   <span class="tl-warn">FULL-STACK</span>`,
      `  <span class="tl-dim">04</span>  <span class="tl-head">Dropify</span>           <span class="tl-dim">·</span> <span class="tl-val">Vue.js 3 + MongoDB</span>   <span class="tl-warn">E-COMMERCE</span>`,
      `  <span class="tl-dim">05</span>  <span class="tl-head">Einstein's Art</span>    <span class="tl-dim">·</span> <span class="tl-val">Angular + TypeScript</span> <span class="tl-warn">DEPLOYED</span>`,
    ],
    'cat resume.txt': () => [
      ``,
      `<span class="tl-head">  ╔══════════════════════════════════╗</span>`,
      `<span class="tl-head">  ║   JAREN KENDRICK YAMBAO          ║</span>`,
      `<span class="tl-head">  ╚══════════════════════════════════╝</span>`,
      `  <span class="tl-dim">Full Stack Developer · Game/App Developer · HAU IT Student</span>`,
      ``,
      `  <span class="tl-warn">▸ EXPERIENCE</span>`,
      `  <span class="tl-acc">  Game/App Developer</span> <span class="tl-dim">@ VirtuIntelligence</span>  <span class="tl-dim">Apr 2025 – Present</span>`,
      `    <span class="tl-val">• Shipped PathFinder to Meta Quest (Unreal 5.5 + Unity)</span>`,
      `    <span class="tl-val">• Built Symposium real-time AI chat (Angular + Node.js)</span>`,
      `    <span class="tl-val">• Building Robots vs Aliens mobile game (Unity + C#)</span>`,
      ``,
      `  <span class="tl-warn">▸ STACK</span>`,
      `    <span class="tl-val">Angular · Vue.js 3 · Node.js · PostgreSQL · MongoDB · C# · TypeScript</span>`,
      ``,
      `  <span class="tl-warn">▸ LOOKING FOR</span>`,
      `    <span class="tl-val">Full-stack internship</span> <span class="tl-acc">→ jarenkendrickyambao@gmail.com</span>`,
    ],
    skills: () => [
      ``,
      `  <span class="tl-head">Technical Stack</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-val">JavaScript</span>  <span class="tl-acc">█████████░</span> <span class="tl-head">85%</span>  <span class="tl-dim">2 yrs</span>`,
      `  <span class="tl-val">TypeScript</span>  <span class="tl-acc">████████░░</span> <span class="tl-head">80%</span>  <span class="tl-dim">1.5 yrs</span>`,
      `  <span class="tl-val">Angular</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Node.js</span>     <span class="tl-acc">████████░░</span> <span class="tl-head">75%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">Vue.js 3</span>    <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">C#</span>          <span class="tl-acc">███████░░░</span> <span class="tl-head">70%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">MongoDB</span>     <span class="tl-acc">███████░░░</span> <span class="tl-head">65%</span>  <span class="tl-dim">1 yr</span>`,
      `  <span class="tl-val">PostgreSQL</span>  <span class="tl-acc">██████░░░░</span> <span class="tl-head">55%</span>  <span class="tl-dim">6 mo</span>`,
    ],
    whoami: () => [
      ``,
      `  <span class="tl-head">jaren kendrick yambao</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Role</span>      <span class="tl-val">Full Stack Developer + Game/App Dev</span>`,
      `  <span class="tl-acc">Age</span>       <span class="tl-val">20 years old</span>`,
      `  <span class="tl-acc">Location</span>  <span class="tl-val">Pampanga, Philippines</span> 🇵🇭`,
      `  <span class="tl-acc">Uni</span>       <span class="tl-val">Holy Angel University (2027)</span>`,
      `  <span class="tl-acc">Status</span>    <span class="tl-ok">● Open to internship</span>`,
      ``,
      `  <span class="tl-quote">"I don't just write code — I ship things."</span>`,
    ],
    contact: () => [
      ``,
      `  <span class="tl-head">Contact</span>`,
      `<span class="tl-dim">  ──────────────────────────────</span>`,
      `  <span class="tl-acc">Email</span>     <span class="tl-val">jarenkendrickyambao@gmail.com</span>`,
      `  <span class="tl-acc">LinkedIn</span>  <span class="tl-val">linkedin.com/in/jarenkendrick</span>`,
      `  <span class="tl-acc">GitHub</span>    <span class="tl-val">github.com/jarenkendrick14</span>`,
    ],
    github: () => {
      window.open('https://github.com/jarenkendrick14', '_blank');
      return [`<span class="tl-ok">✓ Opening github.com/jarenkendrick14...</span>`];
    },
    'run pathfinder': () => {
      const outputLines = [
        `<span class="tl-dim">Initializing VR runtime environment...</span>`,
        `<span class="tl-ok">  ✓ Unreal Engine 5.5 loaded</span>`,
        `<span class="tl-ok">  ✓ Meta Quest SDK connected</span>`,
        `<span class="tl-dim">Mounting PathFinder.apk to device...</span>`,
        `<span class="tl-ok">  ✓ APK verified (23.4 MB)</span>`,
        `<span class="tl-dim">Launching VR session...</span>`,
        `  <span class="tl-acc">▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓</span> 100%`,
        `<span class="tl-ok">  ✓ PathFinder v1.0.0 running</span>`,
        `  🥽 Put on your headset. The journey begins.`,
      ];
      outputLines.forEach((line, i) => {
        setTimeout(() => this.addLine(line), i * 180);
      });
      return null;
    },
    clear: () => { this.lines.set([]); return null; },
    exit: () => { this.close(); return null; },
    quit: () => { this.close(); return null; },
  };

  constructor() {
    effect(() => {
      if (this.isOpen()) {
        if (!this.booted) {
          this.booted = true;
          this.BOOT_MSG.forEach((msg, i) => { setTimeout(() => this.addLine(msg), i * 55); });
          setTimeout(() => this.runCmd('help'), (this.BOOT_MSG.length * 55) + 300);
        }
        setTimeout(() => this.termInput?.nativeElement.focus(), 460);
      }
    });
  }

  close() { this.nav.terminalOpen.set(false); }

  onKeyDown(e: KeyboardEvent) {
    if (e.key === 'Enter') {
      const val = this.inputValue();
      this.inputValue.set('');
      this.runCmd(val);
    } else if (e.key === 'ArrowUp') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx < this.history.length - 1) { this.histIdx++; this.inputValue.set(this.history[this.histIdx] || ''); }
    } else if (e.key === 'ArrowDown') {
      e.preventDefault(); e.stopPropagation();
      if (this.histIdx > 0) { this.histIdx--; this.inputValue.set(this.history[this.histIdx]); }
      else { this.histIdx = -1; this.inputValue.set(''); }
    }
  }

  private runCmd(raw: string) {
    const cmd = raw.trim().toLowerCase();
    if (!cmd) return;
    this.history.unshift(raw);
    this.histIdx = -1;
    this.addLine(`<span class="tl-dim">jaren@portfolio:~$</span> <span class="tl-cmd">${this.esc(raw)}</span>`);
    const fn = this.COMMANDS[cmd];
    if (fn) { const result = fn(); if (result) result.forEach(l => this.addLine(l)); }
    else { this.addLine(`<span class="tl-err">bash: ${this.esc(cmd)}: command not found. Type 'help'.</span>`); }
    this.addLine('');
  }

  private addLine(html: string) {
    this.lines.update(l => [...l, { html }]);
    setTimeout(() => {
      if (this.termOutput?.nativeElement) {
        this.termOutput.nativeElement.scrollTop = this.termOutput.nativeElement.scrollHeight;
      }
    }, 10);
  }

  private esc(s: string) { return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;'); }
}

--- DIRECTORY: app\components\testimonials\testimonials.component.css ---
:host {
  display: block;
  min-height: 100vh;
  width: 100%;
  background: var(--surf);
}

.page-testimonials {
  width: 100%;
  min-height: 100vh;
  padding: 5rem 5rem 3rem;
}

.pin { width: 100%; max-width: 1100px; margin: 0 auto; }

.slabel { display: flex; align-items: center; gap: 1rem; margin-bottom: 3rem; animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .04s ease both; }
.sn { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--acc); letter-spacing: .1em; font-weight: 600; }
.st { font-family: 'JetBrains Mono', monospace; font-size: .85rem; color: var(--muted); letter-spacing: .07em; text-transform: uppercase; font-weight: 500; }
.shr { flex: 1; height: 1px; background: var(--bord); }

.exph {
  font-family: 'Cormorant Garamond', serif;
  font-weight: 600;
  font-size: clamp(3rem, 5vw, 4.5rem);
  letter-spacing: -.04em;
  color: var(--white);
  margin-bottom: 4rem;
  animation: fadeUp .55s cubic-bezier(.22,1,.36,1) .10s ease both;
}

.tmg { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; align-items: stretch; }

.tmc {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  border: 1px solid var(--bord);
  border-radius: 8px;
  background: var(--bg);
  padding: 2.5rem;
  transition: border-color .25s;
  position: relative;
  overflow: hidden;
  animation: scaleIn .5s cubic-bezier(.22,1,.36,1) ease both;
  cursor: none;
}
.tmc:nth-child(1) { animation-delay: .18s; }
.tmc:nth-child(2) { animation-delay: .26s; }
.tmc:nth-child(3) { animation-delay: .34s; }

.tmc::before {
  content: '"';
  position: absolute;
  top: -1rem; right: 1.5rem;
  font-family: 'Cormorant Garamond', serif;
  font-weight: 400;
  font-size: 8rem;
  color: var(--bord);
  line-height: 1;
  pointer-events: none;
  user-select: none;
}

.tmc:hover { border-color: rgba(139,92,246,.4); }

.tmq {
  font-size: 1.1rem;
  color: var(--white);
  line-height: 1.75;
  font-weight: 400;
  margin-bottom: 2rem;
  font-style: italic;
  position: relative;
  z-index: 1;
}

.tma {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-top: auto;
  border-top: 1px solid var(--bord);
  padding-top: 1.5rem;
  position: relative;
  z-index: 1;
}

.tmav {
  width: 46px; height: 46px;
  border-radius: 50%;
  background: var(--surf);
  border: 1px solid var(--bord);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  color: var(--white);
  font-size: 1.1rem;
  flex-shrink: 0;
}

.tmname { font-size: 1rem; font-weight: 600; color: var(--white); }
.placeholder-note { font-size: .75rem; color: var(--muted); font-weight: 300; }
.tmrole { font-family: 'JetBrains Mono', monospace; font-size: .75rem; color: var(--muted); letter-spacing: .04em; margin-top: .2rem; }

@keyframes fadeUp  { from { opacity: 0; transform: translateY(40px); } to { opacity: 1; transform: translateY(0); } }
@keyframes scaleIn { from { opacity: 0; transform: scale(.88); }       to { opacity: 1; transform: scale(1); } }

@media (max-width: 960px) {
  .page-testimonials { padding: 6.5rem 1.5rem 4rem; }
  .tmg { grid-template-columns: 1fr; }
}

--- DIRECTORY: app\components\testimonials\testimonials.component.html ---
<section class="page-testimonials" [class.active-page]="active">
  <div class="pin">
    <div class="slabel au d0">
      <span class="sn">06</span><span class="st">/ what people say</span>
      <div class="shr"></div>
    </div>
    <h2 class="exph au d1">Testimonials.</h2>
    <div class="tmg">
      @for (tm of data.testimonials; track tm.name; let i = $index) {
        <div class="tmc as" [ngClass]="i === 0 ? 'd2' : i === 1 ? 'd3' : 'd4'">
          <p class="tmq">{{ tm.quote }}</p>
          <div class="tma">
            <div class="tmav">{{ tm.initial }}</div>
            <div>
              <p class="tmname">
                {{ tm.name }}
                @if (tm.placeholder) {
                  <span class="placeholder-note">(placeholder)</span>
                }
              </p>
              <p class="tmrole">{{ tm.role }}</p>
            </div>
          </div>
        </div>
      }
    </div>
  </div>
</section>

--- DIRECTORY: app\components\testimonials\testimonials.component.ts ---
import { Component, inject, Input } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PortfolioDataService } from '../../data/portfolio.data';

@Component({
  selector: 'app-testimonials',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './testimonials.component.html',
  styleUrl: './testimonials.component.css',
})
export class TestimonialsComponent {
  @Input() active = false;
  data = inject(PortfolioDataService);
}

--- DIRECTORY: app\data\navigation.service.ts ---
import { Injectable, signal, computed } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class NavigationService {
  readonly currentPage = signal<number>(0);
  readonly isAnimating = signal<boolean>(false);
  readonly theme = signal<'dark' | 'light'>('dark');
  
  // Added terminal state management
  readonly terminalOpen = signal<boolean>(false);

  readonly isDark = computed(() => this.theme() === 'dark');

  constructor() {
    const saved = localStorage.getItem('jk-theme') as 'dark' | 'light' | null;
    if (saved) this.theme.set(saved);
  }

  goTo(next: number, total: number): void {
    if (next === this.currentPage() || this.isAnimating() || next < 0 || next >= total) return;
    this.isAnimating.set(true);
    this.currentPage.set(next);
    setTimeout(() => this.isAnimating.set(false), 520);
  }

  toggleTheme(): void {
    const next = this.theme() === 'dark' ? 'light' : 'dark';
    this.theme.set(next);
    localStorage.setItem('jk-theme', next);
  }

  // Added method to toggle terminal
  toggleTerminal(): void {
    this.terminalOpen.update(v => !v);
  }
}

--- DIRECTORY: app\data\portfolio.data.ts ---
import { Injectable } from '@angular/core';
import { Project, Certificate, BlogPost, Testimonial, NavItem } from '../models/portfolio.models';

@Injectable({ providedIn: 'root' })
export class PortfolioDataService {

  readonly navItems: NavItem[] = [
    { label: 'Home', short: 'Home', index: 0 },
    { label: 'About', short: 'About', index: 1 },
    { label: 'Work', short: 'Work', index: 2 },
    { label: 'Experience', short: 'Exp.', index: 3 },
    { label: 'Blog', short: 'Blog', index: 4 },
    { label: 'Testimonials', short: 'reviews', index: 5 },
    { label: 'Contact', short: 'Contact', index: 6 },
  ];

  readonly projects: Project[] = [
    {
      num: '01', name: 'PathFinder',
      subtitle: 'VR self-discovery app · Unreal Engine 5.5 + Unity',
      tags: ['Unreal Engine 5.5', 'Unity', 'C#', 'VR'],
      desc: 'A VR self-discovery app built in Unreal Engine 5.5 using Blueprints for gameplay systems. Users interact with metaphorical cards, with Wit.ai handling voice-to-text. Also deployed an Android companion app built in Unity.',
      purpose: 'Engineered immersive, privacy-first VR mechanics and a scalable Android companion app for VirtuIntelligence.',
      features: ['Interactive card mechanics in VR', 'AI voice journaling via Wit.ai', 'Local data persistence for privacy', 'Cross-platform deployment'],
      live: [{ name: 'Meta Store ↗', url: 'https://www.meta.com/experiences/' }, { name: 'Google Play ↗', url: 'https://play.google.com/store/' }],
      github: null,
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(167,139,250,.05)']
    },
    {
      num: '02', name: 'Symposium',
      subtitle: 'Real-time AI-mediated chat platform · Angular + Ionic + Node.js',
      tags: ['Angular', 'Ionic', 'Node.js', 'PostgreSQL', 'Prisma', 'WebSockets'],
      desc: 'A full-stack real-time chat application with an AI mediator powered by xAI\'s Grok. Features WebSocket-based messaging, streaming AI responses, configurable turn management, and a full @mention system. Built with Angular 21 + Ionic 8 for cross-platform support and a Node.js/Express backend with PostgreSQL via Prisma ORM.',
      purpose: 'Architected a complete real-time communication platform with AI integration, demonstrating proficiency in WebSocket protocols, streaming APIs, and full-stack TypeScript development.',
      features: ['Real-time WebSocket messaging with auto-reconnect', 'Streaming AI responses via xAI Grok', 'Turn management system (strict & round-robin)', '@mention system with live-filtered dropdown', 'Cross-platform via Ionic + Capacitor'],
      live: [{ name: 'Live Site ↗', url: 'https://symposium.jarenkendrick.com/' }],
      github: null,
      imgs: ['rgba(56,189,248,.12)', 'rgba(56,189,248,.08)', 'rgba(56,189,248,.05)']
    },
    {
      num: '03', name: 'Travel Atelier',
      subtitle: 'Full-stack travel booking platform · Vue.js + Node.js',
      tags: ['Vue.js 3', 'Node.js', 'Express', 'JWT'],
      desc: 'Full-stack travel booking platform featuring secure JWT authentication, protected routing, and real-time destination searches. Built a robust REST API backend for user management and booking workflows.',
      purpose: 'Architected a complete full-stack application demonstrating secure data flow from database to frontend UI.',
      features: ['JWT-based authentication system', 'Protected routing and navigation', 'Backend-connected dynamic forms', 'Real-time destination filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://travel-atelier.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/travel-atelier',
      imgs: ['rgba(96,165,250,.12)', 'rgba(96,165,250,.08)', 'rgba(96,165,250,.05)']
    },
    {
      num: '04', name: 'Dropify',
      subtitle: 'E-commerce platform · Vue.js + MongoDB + Express',
      tags: ['Vue.js 3', 'MongoDB', 'Express', 'Node.js', 'Pinia'],
      desc: 'Comprehensive e-commerce platform for a streetwear brand, featuring a full shopping experience with persistent cart, checkout flow, JWT authentication, and a dedicated admin dashboard for managing products, users, and orders.',
      purpose: 'Designed a scalable MongoDB schema with embedded cart architecture and integrated it with a reactive Vue 3 + Pinia frontend.',
      features: ['Persistent cart with server-synced state', 'Full CRUD admin dashboard (products, users, orders)', 'JWT authentication with role-based access', 'Product search, sort, and paginated filtering'],
      live: [{ name: 'Live Site ↗', url: 'https://dropify.jarenkendrick.com/' }],
      github: 'https://github.com/jarenkendrick14/dropify',
      imgs: ['rgba(52,211,153,.12)', 'rgba(52,211,153,.08)', 'rgba(52,211,153,.05)']
    },
    {
      num: '05', name: "Einstein's Art",
      subtitle: 'Dynamic company website for a hand-painted bag brand',
      tags: ['Angular', 'TypeScript', 'Netlify'],
      desc: "Company website for a local brand built in Angular. Implemented client-side routing, dynamic rendering components, and a custom employee directory.",
      purpose: "Built a fast, scalable frontend structure using Angular best practices and structural directives.",
      features: ['Angular client-side routing', 'Dynamic product rendering', 'Conditional component styling', 'Deployed directly to Netlify'],
      live: [{ name: 'Live Demo ↗', url: 'https://69359c7c680ce1935fafb306--prelim-project-einsteins-art.netlify.app/' }],
      github: 'https://github.com/jarenkendrick14/einsteins-art',
      imgs: ['rgba(167,139,250,.12)', 'rgba(167,139,250,.08)', 'rgba(124,58,237,.06)']
    }
  ];

  readonly certificates: Certificate[] = [
    { name: 'CompTIA IT Fundamentals (ITF+)', issuer: 'CompTIA', abbr: 'COMP', color: '#c8202f', date: 'Nov 2023', url: 'https://www.comptia.org/certifications/it-fundamentals' },
    { name: 'Responsive Web Design', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Sep 2024', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-rwd/responsive-web-design' },
    { name: 'JavaScript Essentials 1', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Oct 2024', url: 'https://skillsforall.com/course/javascript-essentials-1' },
    { name: 'Introduction to Figma', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Sep 2024', url: 'https://www.simplilearn.com/' },
    { name: 'Introduction to PHP', issuer: 'Simplilearn', abbr: 'SL', color: '#5c67f2', date: 'Feb 2025', url: 'https://www.simplilearn.com/' },
    { name: 'Back End Development & APIs', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-bedaa/back-end-development-and-apis' },
    { name: 'Legacy JS Algorithms & Data Structures', issuer: 'freeCodeCamp', abbr: 'fCC', color: '#0a0a23', date: 'Jul 2025', url: 'https://www.freecodecamp.org/certification/jarenkendrickyambao-ljaads/javascript-algorithms-and-data-structures-v8' },
    { name: 'Digital Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jul 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Endpoint Security', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'Aug 2025', url: 'https://skillsforall.com/' },
    { name: 'CCNA: Introduction to Networks', issuer: 'Cisco', abbr: 'CSCO', color: '#1ba0d7', date: 'May 2025', url: 'https://skillsforall.com/' },
    { name: 'Content Marketing', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Sep 2025', url: 'https://academy.hubspot.com/' },
    { name: 'Digital Advertising', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Oct 2025', url: 'https://academy.hubspot.com/' },
    { name: 'SEO', issuer: 'HubSpot Academy', abbr: 'HS', color: '#ff7a59', date: 'Jan 2026', url: 'https://academy.hubspot.com/' },
  ];

  readonly blogPosts: BlogPost[] = [
    {
      date: 'January 2025',
      title: 'From Game Dev to Full Stack: What Shipping a VR App Taught Me',
      excerpt: 'When I shipped PathFinder to the Meta Quest store, I learned more about software development than any tutorial ever taught me. Here\'s what directly translates to web development.',
      content: `<p>When I joined VirtuIntelligence as a part-time Game Developer, I wasn't sure how working in Unreal Engine and Unity would shape me as a web developer. After shipping PathFinder to the Meta Quest store, I figured it out — a lot transfers, and some of it surprised me.</p><h3>Users don't read. They react.</h3><p>In VR, if a UI isn't immediately intuitive, users just stop. They look around confused. There are no tooltips, no second chances, no "skip tutorial" button. I learned to design every interaction for the first <em>three seconds</em> — no assumptions about prior knowledge.</p><p>That thinking now applies to every web interface I build. Before writing a single line of frontend code, I ask: what does a user see first? What do they click? Why would they scroll? What would make them leave? Questions I used to skip. I don't anymore.</p><h3>State management is state management</h3><p>Unity's component system is event-driven and reactive. A player picks up an item — the inventory updates, the HUD reflects it, the game state records it. Sound familiar? When I picked up Vue.js and its Composition API, the mental model was <strong>already there</strong>. Data changes, view updates. The syntax was different. The thinking wasn't.</p><p>C# gave me a very clear sense of data flow and component responsibility before I ever wrote a composable. I think that's a genuine advantage — understanding architecture before you're handed a framework that handles it for you.</p><h3>Performance is non-negotiable</h3><p>In VR, a frame drop makes you nauseous. Literally. That obsession transferred directly to web development. I check bundle sizes. I lazy-load images. I debounce API calls. Not because a tutorial told me to — because I've felt the pain of ignoring performance and watched someone rip a headset off their face.</p><h3>Shipping teaches more than studying</h3><p>PathFinder had to pass Meta's content review. It had to work on multiple headset hardware configurations. It needed a separate Android companion app in Unity — a completely different toolchain, different lifecycle, different debugging process. None of that is in any course.</p><p>Real software has constraints that force you to problem-solve in ways tutorials never reach. I now expect the last 30% of any project to be the hardest. I plan for it instead of being surprised by it.</p><p>If you're a web developer who hasn't shipped something to a real user or a real store — do it once. The clarity it gives you about what "done" actually means is worth every painful hour.</p>`
    },
    {
      date: 'December 2024',
      title: 'Vue.js vs Starting from Scratch: Why I Chose a Framework',
      excerpt: 'When building Travel Atelier, I faced a real choice: vanilla JavaScript or Vue.js. Here\'s the honest breakdown — when frameworks earn their place, and when they don\'t.',
      content: `<p>When I started building Travel Atelier, I had a real choice: vanilla JavaScript or Vue.js. Here's the honest breakdown — why I picked a framework, when that's the right call, and what I'd do differently now.</p><h3>The case for vanilla first</h3><p>I've done it. I've built projects with pure HTML, CSS, and JavaScript. No build step. No dependencies. Total control. I knew exactly what every line did because I wrote every line. For a showcase site — a product page, a portfolio — vanilla is often the right answer.</p><p>It's fast, it's transparent, and it forces you to understand fundamentals. If you've never built a simple SPA without a framework, you don't really know what frameworks are solving for you. I mean that seriously.</p><h3>But Travel Atelier wasn't a showcase</h3><p>It needed <strong>JWT authentication</strong>, <strong>protected routing</strong>, reactive form state, live destination search, a customer messages dashboard, and a Node.js REST API backend — all wired together. Building that in vanilla JS wasn't a learning exercise. It was reinventing wheels that Vue already shipped, tested, and documented.</p><p>Vue's Composition API handles reactivity, component state, and routing with patterns tested by millions of developers. I wasn't going to out-architect that in a weekend. The framework earned its place.</p><h3>Why Vue specifically, not React?</h3><p>The job market clearly favors React. I know that. But Vue's Composition API fits how I think. Logic lives in <code>setup()</code>. You compose behavior in reusable composables. The template syntax doesn't feel like a fight. For someone learning reactivity for the first time — especially coming from Unity's component-based architecture in C# — Vue's mental model clicked faster.</p><p>I'm not against React. I'll learn it. But for a solo developer building a real app with a deadline, you pick the tool you can move fastest with while still writing code you're proud of.</p><h3>What I'd do differently</h3><p>Spend two weeks in vanilla JavaScript before touching any framework. Not because frameworks are bad — but because you don't fully appreciate what they give you until you've felt the pain of doing it yourself. Reactivity, component lifecycle, two-way binding — these are harder concepts when a framework is handling them invisibly for you.</p><p><em>Understanding the "why" behind your tools makes you significantly better at using them.</em></p><p>Pick the right tool. But understand what the tool is doing for you. That understanding is the difference between a developer who can use a framework and a developer who can build without one.</p>`
    }
  ];

  readonly testimonials: Testimonial[] = [
    { quote: '"Jaren\'s ability to ship functional, well-designed applications is impressive. The architecture in his VR app shows real care for the end user and system stability."', name: 'Carlos M.', role: 'Senior Developer · VirtuIntelligence', initial: 'C', placeholder: true },
    { quote: '"Working with Jaren is refreshing. He takes ownership, communicates effectively, and the code he submits is clean and functional on the first pull request."', name: 'Mikaella T.', role: 'Classmate & Collaborator · HAU', initial: 'M', placeholder: true },
    { quote: '"The web application Jaren built exceeded expectations. Clean database structure, solid API design, and delivered on time. A strong full-stack candidate."', name: 'Prof. [Name]', role: 'IT Instructor · Holy Angel University', initial: 'P', placeholder: true },
  ];
}

--- DIRECTORY: app\directives\decrypt.directive.ts ---
import { Directive, ElementRef, Input, OnChanges, SimpleChanges, inject } from '@angular/core';

@Directive({
  selector: '[appDecrypt]',
  standalone: true
})
export class DecryptDirective implements OnChanges {
  @Input('appDecrypt') targetText = '';
  @Input() active = false; // Trigger from parent component
  @Input() delay = 600;    // Wait for page slide (default 600ms)

  private el = inject(ElementRef);
  private readonly GLYPHS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*<>/[]{}|';
  private originalHTML = '';
  private plainText = '';
  private running = false;

  ngOnChanges(changes: SimpleChanges) {
    if (changes['targetText']) {
      this.originalHTML = this.el.nativeElement.innerHTML;
      this.plainText = this.targetText || this.el.nativeElement.textContent.trim();
    }

    // Only run if the component is physically active AND not already running
    if (changes['active'] && this.active) {
      if (!this.running) {
        this.running = true; // Lock execution immediately 
        setTimeout(() => this.runEffect(), this.delay);
      }
    }
  }

  private runEffect() {
    const el = this.el.nativeElement;
    
    // 1. Lock Dimensions to prevent layout jumping
    const rect = el.getBoundingClientRect();
    el.style.width = `${rect.width}px`;
    el.style.height = `${rect.height}px`;
    el.style.display = 'block'; 
    el.style.whiteSpace = 'nowrap'; 
    el.style.overflow = 'hidden';

    if (!this.originalHTML) this.originalHTML = el.innerHTML;
    el.classList.add('decrypting');

    const len = this.plainText.length;
    let frame = 0;
    const duration = 800;
    const totalFrames = Math.round(duration / 40);
    
    const iv = setInterval(() => {
      const progress = frame / totalFrames;
      let scrambled = '';
      
      for (let i = 0; i < len; i++) {
        const ch = this.plainText[i];
        if (ch === ' ' || ch === '\n') { 
          scrambled += ' '; 
        } else if (progress > i / len) { 
          scrambled += ch; 
        } else { 
          scrambled += this.GLYPHS[Math.floor(Math.random() * this.GLYPHS.length)]; 
        }
      }
      
      el.textContent = scrambled;
      frame++;
      
      if (frame >= totalFrames) {
        // 2. Restore Original HTML (tags and all)
        el.innerHTML = this.originalHTML;
        
        // 3. Unlock Dimensions
        el.style.width = ''; 
        el.style.height = '';
        el.style.display = '';
        el.style.whiteSpace = '';
        el.style.overflow = '';
        
        el.classList.remove('decrypting');
        this.running = false; // Release lock
        clearInterval(iv);
      }
    }, 40);
  }
}

--- DIRECTORY: app\models\portfolio.models.ts ---
export interface Project {
  num: string;
  name: string;
  tags: string[];
  desc: string;
  purpose: string;
  features: string[];
  live: { name: string; url: string }[] | null;
  github: string | null;
  imgs: string[];
  subtitle: string;
}

export interface Certificate {
  name: string;
  issuer: string;
  abbr: string;
  color: string;
  date: string;
  expires?: string;
  url: string;
}

export interface BlogPost {
  date: string;
  title: string;
  excerpt: string;
  content: string;
}

export interface Testimonial {
  quote: string;
  name: string;
  role: string;
  initial: string;
  placeholder?: boolean;
}

export interface NavItem {
  label: string;
  short: string;
  index: number;
}


--- DIRECTORY: index.html ---
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Jaren Kendrick — Full Stack Developer</title>
  <meta name="description" content="20-year-old IT student at Holy Angel University building full-stack web apps with Angular, Vue.js & Node.js. Part-time Game Developer at VirtuIntelligence." />
  <base href="/" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
</head>
<body>
  <app-root></app-root>
</body>
</html>


--- DIRECTORY: main.ts ---
import { bootstrapApplication } from '@angular/platform-browser';
import { provideZonelessChangeDetection } from '@angular/core';
import { AppComponent } from './app/app.component';

bootstrapApplication(AppComponent, {
  providers: [
    // ✦ Angular 21 Zoneless Mode — no zone.js needed
    provideZonelessChangeDetection(),
  ],
}).catch(err => console.error(err));

--- DIRECTORY: styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}


--- DIRECTORY: src\styles.css ---
/* ══════════ GOOGLE FONTS ══════════ */
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');

/* ══════════ DESIGN TOKENS ══════════ */
:root {
  --bg: #09090f;
  --sb: #0d0d16;
  --surf: #111120;
  --bord: #2a2a40;
  /* Toned-down from #a78bfa → less pastel, more solid violet */
  --acc: #8b5cf6;
  --acc2: #6d28d9;
  --white: #ffffff;
  --text: #e0def0;
  --muted: #a4a1c6;
  --sub: #c2c0dc;
  --sbbrd: #1a1a2e;
}

[data-theme="light"] {
  --bg: #f4f2f9;
  --sb: #ebe8f5;
  --surf: #e0ddf0;
  --bord: #c8c3e0;
  --acc: #7c3aed;
  --acc2: #6d28d9;
  --white: #1a1826;
  --text: #2d2b3b;
  --muted: #5c597a;
  --sub: #44425e;
  --sbbrd: #d0cceb;
}

/* ══════════ RESET ══════════ */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  height: 100vh;
  width: 100vw;
  transition: background-color .35s ease, color .35s ease;
  cursor: none;
}

a { text-decoration: none; color: inherit; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

*, *::before, *::after {
  transition: background-color .35s ease, border-color .35s ease, color .35s ease;
}

a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible {
  outline: 2px solid var(--acc);
  outline-offset: 2px;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-thumb { background: var(--bord); border-radius: 4px; }

@media (max-width: 960px) {
  body { overflow: auto; height: auto; cursor: auto; }
  button, a { cursor: pointer !important; }
}

.decrypting { color: var(--acc) !important; }

/* ══════════ GLOBAL ANIMATIONS — more prominent distances ══════════ */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(40px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeLeft {
  from { opacity: 0; transform: translateX(-44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes fadeRight {
  from { opacity: 0; transform: translateX(44px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes scaleIn {
  from { opacity: 0; transform: scale(.88); }
  to   { opacity: 1; transform: scale(1); }
}

/* ══════════ BASE STATE (hidden) ══════════ */
.au, .al, .ar, .as {
  opacity: 0;
  transition: opacity 0.1s ease 0.6s;
}

/* ══════════ ACTIVE STATE ══════════ */
.active .au, .active-page .au { opacity: 1; transition: none; animation: fadeUp  .55s cubic-bezier(.22,1,.36,1) both; }
.active .al, .active-page .al { opacity: 1; transition: none; animation: fadeLeft .55s cubic-bezier(.22,1,.36,1) both; }
.active .ar, .active-page .ar { opacity: 1; transition: none; animation: fadeRight .55s cubic-bezier(.22,1,.36,1) both; }
.active .as, .active-page .as { opacity: 1; transition: none; animation: scaleIn .5s cubic-bezier(.22,1,.36,1) both; }

/* Stagger Delays */
.active .d0, .active-page .d0 { animation-delay: .04s !important; }
.active .d1, .active-page .d1 { animation-delay: .10s !important; }
.active .d2, .active-page .d2 { animation-delay: .18s !important; }
.active .d3, .active-page .d3 { animation-delay: .26s !important; }
.active .d4, .active-page .d4 { animation-delay: .34s !important; }
.active .d5, .active-page .d5 { animation-delay: .44s !important; }

/* ══════════ SEAMLESS SLIDE-OUT (both directions) ══════════
   When a page slides OUT — either up (.prev) or down (.next) —
   its content must stay fully visible for the whole 0.6s slide.
   
   .prev  = page that slid up and off (scroll DOWN)
   .next  = page that slid down and off (scroll UP)
   
   Both are off-screen via translateY(±100%), so opacity:1 here
   causes zero visual bleed. The !important beats Angular's
   higher-specificity attribute-scoped component styles.
══════════════════════════════════════════════════════════════ */
.page-section.prev .au,
.page-section.prev .al,
.page-section.prev .ar,
.page-section.prev .as,
.page-section.next .au,
.page-section.next .al,
.page-section.next .ar,
.page-section.next .as {
  opacity: 1 !important;
  animation: none !important;
  transition: none !important;
}

--- DIRECTORY: tsconfig.app.json ---
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}


--- DIRECTORY: tsconfig.json ---
{
  "compileOnSave": false,
  "compilerOptions": {
    "baseUrl": "./",
    "outDir": "./dist/out-tsc",
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "noImplicitOverride": true,
    "noPropertyAccessFromIndexSignature": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": false,
    "downlevelIteration": true,
    "experimentalDecorators": true,
    "moduleResolution": "bundler",
    "importHelpers": true,
    "target": "ES2022",
    "module": "ES2022",
    "useDefineForClassFields": false,
    "lib": ["ES2022", "dom"]
  },
  "angularCompilerOptions": {
    "enableI18nLegacyMessageIdFormat": false,
    "strictInjectionParameters": true,
    "strictInputAccessModifiers": true,
    "strictTemplates": true
  }
}

